# Test Run Log

**Run ID:** RUN-2026-08-03-001
**Date:** 2026-08-03 (drafted — execute after the Neuro Navigator redesign deploys)
**Branch:** `main`
**Preview URL:** https://app.neuroplans.app
**Tester:** Claude Code for Chrome
**Runbook version:** 2.0
**Test cases version:** 2.0

---

## Mission Brief

> Delta for the /triage visual redesign (redesign brief Parts 2–3, "nn" design
> system). Presentation-layer only — triage logic, rubric, weights, thresholds,
> and red-flag overrides are unchanged. Partner demos: Mayo Aug 7, Novant Aug 12.

**Feature/Fix:**
- New shared clinical design system (src/styles/neuro-navigator.css, `.nn` scope) — light palette, lightness-ordered tier colors, AA-verified contrast pairs
- /triage restructured to two panes (input left, recommendation right; stacked <860px); input stays visible beside the result
- Result pane is rubric-forward: red flags above scores, tier card shows "Tier N of 7" + name + timeframe, five dimension bars with published weights, weighted total → tier line, stability caveat
- Staged progress display during the 50–60s model call (TriageProgress) with elapsed clock and Cancel
- Emergent takeover restyled (light t1 treatment, same acknowledge flow)
- Actions (Copy Report / View Algorithm / Try Another) moved below the result

**Feature/Fix (added same-day — Part 4, /patient/historian):**
- Historian restyled to the same nn system: 4-step flow with visible indicator (Your visit → Consent & identity → Interview → Summary), 17px patient type scale, large targets
- Consent + identity gate now renders as an in-page step (modal presentation preserved for /consult embed); mic still unreachable before confirm
- Interview screen: current question always shown as text (Question N of up to 23), live "What we heard" transcript, unmistakable mic state (listening/assistant speaking/wrapping up), correction hint, End Interview always visible
- Voice-engine selector hidden behind ?internal=1 (the ?voice=nova deep link still works); MD View / localizer panel removed from the patient surface (enableLocalizer off — physician-only content must not render here)
- Report view: patient surface now defaults to the plain-language Patient Report tab; clinical (light) theme on the patient page, dark unchanged on /consult

**Focus test cases:**
- Full smoke suite (S1–S7)
- Triage flows (all T-series cases in TEST_CASES.yaml): paste → tier; demo cards; PDF upload parity; blank-PDF hold; insufficient-data case; emergent case incl. acknowledge; cancel mid-run
- Historian flows (D/H-series): scenario select → consent gate blocks until checkbox+name+DOB → live interview → completion report defaults to Patient Report tab; safety-escalation screen unchanged; /consult embedded historian consent modal unchanged (dark)
- E1 375px mobile check on BOTH /triage and /patient/historian — no horizontal scroll
- Keyboard-only pass: tab order through input → sample loaders → submit → result actions; visible focus rings on every control; Esc closes the Algorithm modal

**Risk areas:**
- State machine rendering after the two-pane restructure (input stays mounted through triaging/result; extraction review takes the full-width row — regressions would show as blank panes or lost input text on Back/Try Another)
- Long-packet extraction progress line rendering inside the new TriageProgress card
- Legacy dark-styled surfaces that must be UNCHANGED by the redesign: /triage/validate*, batch panels (compact tier chips keep TIER_DISPLAY colors)
- HistorianConsentDisclosure/consult embed unaffected (no historian changes in this release)

---

## Smoke Suite (Required)

> Executed 2026-08-03 against https://app.neuroplans.app (deploy f1bdaee, Amplify job 385).
> Tester: Claude Code driving the in-app browser + the user's Chrome (Steve directed Claude
> Code to execute this run directly; the usual planner/executor split was collapsed for it).

| ID | Title | Result | Notes |
|----|-------|--------|-------|
| S1 | App loads (Desktop) | PASS* | `/` renders the 6-card homepage. *Runbook v2.0 expects a redirect to `/dashboard` — that expectation predates the homepage and is stale, not a regression. Bump runbook. |
| S2 | App loads (Mobile) | DEFERRED | Real device required per runbook (mobile UA redirect). |
| S3 | Login | PASS | Clicked "Sign In with Sevaro SSO" → Cognito hosted-UI session cookie completed silent SSO (no credential entry) → authenticated as steve@sevaro.com, /dashboard renders the Operations Dashboard. |
| S4 | Tabs render | PASS | /ehr: History, Imaging/Results, Physical Exam, Recommendation each render without error. (Runbook label "Exams" = "Physical Exam" in current UI.) |
| S5 | Patient portal | PASS | `/patient` renders Intake / Messages / Historian tabs. |
| S6 | Mobile app | DEFERRED | Real device required per runbook. |
| S7 | Build passes | PASS | Amplify job 385 SUCCEED on f1bdaee (production build green). |

## Delta Results — /triage (redesign)

| Check | Result | Evidence |
|-------|--------|----------|
| Two-pane nn layout renders on prod | PASS | `.nn-panes` present; page bg = `#f4f6f7` token; Referral/Recommendation headings |
| Character counter vs 50,000 | PASS | "0/50,000 characters" |
| Over-limit paste blocks without truncating | PASS | 54,000 chars: red `54,000/50,000` counter, alert with full message, submit disabled |
| Unauthenticated submit fails clean | PASS | "Please sign in to continue." in the nn alert; page returns to input state; no broken UI |
| Demo loader opens with categories | PASS | "Try a Demo" menu renders with scenario category counts (10/12/4) |
| 375px stack, no horizontal scroll | PASS | scrollWidth == 375 |
| Console errors | PASS | zero |
| Live scoring → tier render | PASS | Synthetic weakness/CK note, full flow: staged progress (named stages + elapsed + "typically 50–60 seconds"), extraction review, approve, scoring, result. Tier card "Tier 2 of 7 / Urgent / Within 1 Week"; weighted total "3.75 / 5 → Tier 2 of 7 — Urgent" WITH the red-flag-override explanation rendered ("· Red-flag override" tag + "the override supersedes the weighted score") — the design's override-honesty case exercised by a real result. 5 dimension bars, stability caveat, confidence, Safety Workflow panel (Clinician Confirmation hold + critical unknowns), referral visible beside the result. |
| Emergent takeover on live scoring | PASS | Thunderclap note → fail-closed INGRESS emergency screen within seconds ("Emergency evaluation now", "Missing extraction data does not weaken this action"), approval button disabled, scheduling + scoring blocked, safety workflow minted (35b7e0f0…). The post-scoring EmergentAlert overlay was verified via static fixture; live emergent handling correctly terminates earlier, at ingress. |
| Insufficient/thin referral | PASS* | Content-free referral → extraction review → scoring branch declined with "Human review required — scoring blocked" hold; independent safety result preserved; scheduling blocked. *Terminates at the fail-closed hold, not the Tier 7 card (Tier 7 render verified via fixture) — engine behavior, correct "decline to guess". Progress panel stayed live ~2.5 min through a scorer retry (elapsed clock reset visible) — no dead button even on the worst path. |
| Algorithm modal Esc-close on result | PASS | Opens from result, `role="dialog"` + title, Tier 7 row present, Esc closes. |

## Delta Results — /patient/historian (redesign)

| Check | Result | Evidence |
|-------|--------|----------|
| 4-step flow + indicator renders on prod | PASS | "Step 1 of 4 — Your visit"; nn theme; scenario cards |
| Consent gate is an in-page step, not a modal | PASS | No `role="dialog"`; "Step 2 of 4 — Consent & identity" |
| Gate blocks until checkbox + name + DOB | PASS | Begin disabled → still disabled with checkbox alone → enabled after all three |
| Microphone never requested pre-consent | PASS | permissions.query(microphone) never prompted during the whole gate flow |
| Emergency box with symptom list | PASS | "THIS IS NOT FOR EMERGENCIES" + 911/ED instruction visible |
| Cancel returns to step 1 | PASS | verified stepwise |
| Engine selector hidden by default | PASS | absent without `?internal=1`, present with it |
| 375px, no horizontal scroll | PASS | scrollWidth == 375 |
| Console errors | PASS | zero |
| Live voice interview (H3–H6, I-series) | DEFERRED | Requires a human speaker; run as a manned session |
| Safety-escalation overlay | NOT RE-TESTED | Deliberately untouched by the redesign (code-verified unchanged) |

## Findings

1. **Runbook staleness (not a bug):** runbook v2.0 (2026-02-06) predates both /triage and the
   current homepage — it has no triage section and S1's pass criteria expects a `/dashboard`
   redirect. Next runbook revision should add T-series triage cases, historian-redesign cases,
   and bump `runbook_version`.
2. **No defects found** in the executed set.

## Remaining to close this run

1. ~~Auth-gated cases~~ — DONE (silent SSO; all four triage checks + S3/S4 executed, see above).
2. Manned historian voice session for H3–H6.
3. Real-device pass for S2/S6.

## Cleanup / follow-ups from execution

- QA created three synthetic triage sessions in prod under steve@sevaro.com (Cases A–C, all
  prefixed "SYNTHETIC QA CASE"). Case B minted an OPEN emergency safety workflow
  (ID 35b7e0f0-2e81-4674-a925-e39750f62da7) — needs a clinician disposition via the
  Emergency Action panel to close the loop (deliberately not closed by QA: that flow records
  clinical ownership).
- Case A left a Clinician Confirmation review hold; Case C left a human-review hold. Both are
  synthetic; resolve or leave per whatever the review queue policy is.
