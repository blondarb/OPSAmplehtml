# Mission brief — Historian depth program E2E (RUN-2026-09-06-001)

Delta under test (all merged to main, deployed by Amplify, every lever flag-OFF until flipped):
#207 prompt depth · #208 pre-close gate + unified localizer channel · #209 SQS eval worker · #210/#211 attending review · #212 patient-route steer flag · #213 differential v3.

Planner: Claude Code. Executor: Steve (voice interviews cannot be driven by Claude for Chrome) + Claude Code for the DB/queue/audit checks. Plus the S1–S5 smoke suite and one 375px check (E1) as always.

## Gate 0 — prerequisites (Steve, in order)
1. Delete the superseded `historian-mvp-qa-worker` stack (schedule-name collision), wait DELETE_COMPLETE.
2. `cd infrastructure/triage-worker && sam build && sam deploy --profile sevaro-sandbox` → PASS = changeset executes; stack shows HistorianEvalWorkQueue/DeadLetterQueue/WorkerFunction/DispatcherFunction + 2 log groups + alarms.
3. Migrations 060, 061, 062 via psql using the RDS_* keys from `.env.local` → PASS = `curl -s https://app.neuroplans.app/api/ai/historian/runs?limit=1` returns JSON, not `{"error":"column nc.localizer_excluded…"}`.
4. Amplify env (internal ring): `HISTORIAN_EVAL_MODE=queue`, `HISTORIAN_ATTENDING_ENABLED=true`, `NEXT_PUBLIC_HISTORIAN_PATIENT_STEER=true`, `NEXT_PUBLIC_HISTORIAN_PRECLOSE_GATE=true` → REBUILD main (env is build-time). PASS = job SUCCEED after the env change.

## Gate 1 — worker path (Claude Code, no voice needed)
- Save one synthetic session (`npm run historian:synthetic -- --persona <name> --base-url http://localhost:<port> --max-turns 30`, marks `[SYNTHETIC-DRIVER]`). The driver refuses a non-localhost base URL (OpenAI, no BAA): run a local checkout of main with `HISTORIAN_EVAL_MODE=queue` against the prod RDS; the deployed dispatcher/worker pick the row up from there.
- PASS = `final_differential->>'status'` goes pending → queued → ok within ~3 min; worker log group shows only `{event, error_class}` JSON lines; DLQ depth 0; record has `prompt_version: final-ddx-v3`, `excluded`/`unassessed` present, every `evidence_quote` is a patient line.
- FAIL triggers: status stuck `queued` > 60 min (reclaim should re-pick it), any `error` record → read `error_class` only.

## Gate 2 — live interview, Nova (Steve, /patient/historian, 12–20 min)
Persona: adult with new-onset headaches; a one-line referral; mention ONE medication by name but not the dose; do not volunteer alcohol or family history; decline one topic once ("I'd rather not say").
PASS checks, all in the transcript (Claude Code runs `npm run historian:audit` after):
- Henry asks medication dose/start date, alcohol (typical week, most in one sitting), and family history in his own words even though the referral mentions none → prompt depth.
- At least one question that is not the obvious next question (the attending gap), never prefaced by "the attending", "my supervisor", or "I was told" → attending review. Grep: zero hits for those phrases.
- When Henry tries to finish before every Phase 3 item is asked, he asks ONE more question then finishes (never loops; the declined topic is not re-asked) → pre-close gate + Nova reject ordering (this is the one path that was reasoned, not live-verified).
- Zero diagnosis names spoken to the patient; gratitude openers ≤ 5%; no compound questions → audit turnStyleChecks.
- Patient screen shows no differential panel at any point → #212 panel gate.

## Gate 3 — clinician surfaces (Steve, 5 min)
- `/rnd/historian` run for Gate 2: differential v3 renders excluded (with reason), confidence notes, "Not assessed" list; no crash on the pending/queued/error states from Gate 1.
- Physician panel / report card for the same session shows "Confidence limited:" under items that carry a note, and nothing else changed.

## Gate 4 — rollback drill (Steve, 2 min)
- Unset `HISTORIAN_EVAL_MODE`, rebuild → next save runs the inline chain (record appears without pending state). Already-queued work still completes.

Bugs → `qa/BUG_TEMPLATE.md`. Log results in this file under "Results".

## Results

### Gate 1 — worker path — PASS (2026-09-06 15:53–15:59 UTC, Claude Code)
Method: prod Amplify flags were NOT yet set, so the save route ran from a local checkout of main (`HISTORIAN_EVAL_MODE=queue`, port 3112) against the prod RDS; the deployed dispatcher + worker in `sevaro-triage-worker-sandbox` did the rest. Synthetic personas only (`[SYNTHETIC-DRIVER]` rows). The driver refuses a non-localhost `--base-url` by design (OpenAI, no BAA) — the Gate 1 command in this brief should read `--base-url http://localhost:<port>`.

| row | persona | turns (patient) | saved | queued | ok | follow-on evals |
|---|---|---|---|---|---|---|
| b5bc3e49… | acute-stroke | 2 | 15:53:04 | 15:54:03 | 15:54:25 | thoroughness 15:54:46, independent_ddx + agreement 15:54:55 |
| ecb32059… | migraine-chronic | 28 | 15:56:48 | 15:56:59 | 15:58:04 | thoroughness 15:58:37, independent_ddx + agreement 15:58:47 |

- Both records: `provenance.prompt_version = final-ddx-v3`, model `us.anthropic.claude-sonnet-4-6`, `excluded` (1 / 3) and `unassessed` (3 / 2) present, `dropped_exclusions = 0`, `exclusion_audit_flags = []`, `confidence_note` on every differential item (5/5, 6/6).
- Every exclusion `evidence_quote` is a verbatim PATIENT-role line (1/1, 3/3); every differential supporting quote is verbatim transcript text (8/8, 26/26); `dropped_quotes = 0`.
- Work queue drained to 0/0 at 15:58:59; DLQ 0 throughout; zero `historian_eval_*` failure events in the worker log group; all 4 HistorianEval alarms OK. Dispatcher sweep runs every minute (`PendingSweep`), pickup latency ≤ 60 s.
- Deviation, not a bug: the worker logs only on failure (`{event, error_class}`); a clean run leaves only the shared `[RDS] Using Secrets Manager credentials, host: <prefix>… db: <name>` line — no transcript text. Success is observed on the DB row, not in logs.
- Style audit (non-authoritative, text-mode OpenAI path, not Nova): acute-stroke PASS; migraine-chronic FAIL on 3/30 compound-question turns, gratitude 0%. Carry into Gate 2 as a watch item — not a Gate 1 criterion.
- Local queue-mode server logged 2× save 200, 21× transcript-flush 200, 0 errors; server stopped after both saves.

### Gate 1 follow-up — prod still in inline mode
`aws amplify get-app` shows only `DEMO_ENDPOINTS_ENABLED`, `TRIAGE_LONG_PACKET_DURABLE_ENABLED`; the last main job (480) is the #214 auto-deploy, not a RELEASE. Steps 2–3 of `scripts/ops/historian-eval-rollout.sh` did not land. Gates 2–4 need those four flags + a RELEASE rebuild first.

### Prod flag flip — VERIFIED LIVE (2026-09-06 16:29–16:50 UTC, Claude Code)
- Steve set the four keys at Amplify app level and ran RELEASE job 482 (BUILD/DEPLOY/VERIFY SUCCEED 16:29Z).
- `NEXT_PUBLIC_HISTORIAN_PATIENT_STEER`: prod client chunk shows `enableLocalizer:u||!0` (flag inlined true). `NEXT_PUBLIC_HISTORIAN_PRECLOSE_GATE`: the `enabled &&` guard is folded away and the `/api/ai/historian/coverage` fetch survives in the bundle (dead-code elimination would have removed it if false).
- `HISTORIAN_ATTENDING_ENABLED`: POST /api/ai/historian/localizer with the synthetic migraine window → `attending_meta.ran = true`, 3 gaps, ~5.0–5.5 s, none prefaced "attending/supervisor/told", all end in "?". With `localizerCycle: 1` → `reason: interval`; 4-turn window → `reason: too_short` (gating works).
- `HISTORIAN_EVAL_MODE=queue`: not yet proven on prod (a prod save POST is classifier-blocked for Claude). Proof = the first real save after 482 shows `final_differential->>'status' = pending` then `ok`; Gate 2's save will do it, or Steve can POST the synthetic payload himself.

### FINDING — localizer Steps 1–3 exceed the 15 s budget on every call (pre-existing, not from today)
- Every prod call (5/5, warm) returned `partial: true`, `degradedReason: Timeout after 15000ms`, `differential: []`, no `suggested_next_question` / `top_differentials`. Attending (Step 4, parallel) still returns, so Henry gets the attending gap line but never the Step 3 steer.
- Reproduced locally on main, on the pre-#210 commit (3d685a5) and on the July localizer (7c6e465) with the same input → not a regression from #210 or the flag flip. Per-step timings (local, main): Step 1 5.4–5.8 s (500 tok), Step 2 2.1 s (no plan match), Step 3 13.3 s and `stop=max_tokens` at 900 tok (3,506 chars) — total 20.7 s. The exclusion-reasoning commit (600→900 tok + `evidence_against`/`excluded`) widened the gap; Sonnet 4.6 output speed (~70 tok/s) is the floor.
- Prod SSR log has no localizer calls in the prior 3 days, so no evidence it ever fit on prod at this model.
- Impact on Gate 2: prompt depth, pre-close gate and attending review are testable now; the Step 3 steer (next question, top differentials, scale suggestion) is dark until fixed. Each localizer call also burns a full 15 s Lambda + a wasted 13 s Bedrock generation every other turn.
- Fix (in flight, PR-only): split Step 3 into a steer-critical call (next question + scale + ≤3 differential NAMES, ≤300 tok) and a differential-detail call (rationale, evidence_against, excluded) that runs in parallel and is returned only if it finishes; per-step duration logs; keep the 15 s budget.

### Localizer fix DEPLOYED — PR #215 (8ed056a), Amplify job 484 SUCCEED 17:4xZ
- Prod probe (synthetic 8-turn headache body, fresh session id, 3 runs): HTTP 200 in 15.0 s each, `push_payload.suggested_next_question` present (78–110 chars, no diagnosis word), `top_differentials` 3, `suggested_scale_id` hit6, `differential` 3 names-only; `partial: true`, `degradedReason: Differential detail timed out` (the 900-token clinician detail call still exceeds the budget — expected, its output is for the LocalizerPanel on /consult only).
- Review (adversarial workflow): MERGE, 0 must-fix; landed the diagnosis-lexicon screen on steer questions and the voice-administrable scale filter; 2 deferred items in the PR body.
- Follow-up (not blocking Gate 2): the response still waits the full 15 s for the detail call to abort, so the steer lands ~15 s after the request. Next step is to stop waiting on 3b once 3a + attending are done (or move clinician detail off the live cycle).

### Clinician detail off-cycle DEPLOYED — PR #216 (3c9b25c), Amplify job 486 SUCCEED 18:16Z
Decision (Steve): no model change; cap differential at 3 (exclusions 2, questions 3), wording untouched. Prod probes (synthetic 8-turn body):
| call | HTTP | wall | partial | notes |
|---|---|---|---|---|
| steer, patient shape (no `wantDetail`) | 200 | 7.1 s | false | no `detail_input`; next question + 3 names + hit6 |
| steer, clinician shape (`wantDetail:true`) | 200 | 7.2 s | false | `detail_input` present (4 keys) |
| detail (own 25 s budget) | 200 | 13.8 s | false | 3 ddx with full rationale (223–339 chars), 2 exclusions, 3 questions; no `push_payload`, no attending fields |
| detail with empty `detail_input` | 400 | 0.3 s | — | rejected before any model work |
Review: MERGE, 0 must-fix; landed `wantDetail` opt-in and partial-preserving merge; 3 deferred in the PR body. Localizer round trip on the patient route dropped from a fixed 15.0 s to ~7 s.

### Gates 2–4
_(pending — Steve; all four levers live, steer ~7 s, clinician detail ~14 s on its own request)_
