# Triage tier bake-off — 2026-08-04

**Supersedes `BAKEOFF_2026-07-12.md`, which is INVALID. See "Why the July bake-off cannot be used" below.**

| | |
|---|---|
| Model | `us.anthropic.claude-sonnet-4-6` |
| Study | `default` (current demo content — the notes the app actually serves) |
| Runs | 26 cases × (3 standard @ temp 0.2 + 1 baseline @ temp 0) = 104; **103 succeeded, 1 transient failure** (Delgado run 2 — 3 good runs remain) |
| Command | `npx tsx scripts/run-validation.ts us.anthropic.claude-sonnet-4-6 --study=default` |
| Analysis | `npx tsx scripts/tier-floor-breakdown.ts --study default` (offline, free, repeatable) |
| Labels | `expectedTier` in `src/lib/triage/demoScenarios.ts`, as of `f54e391` |

---

## Why the July bake-off cannot be used

`BAKEOFF_2026-07-12.md` scored a **different case set than the app serves**. `scenario_id` is
reused across validation studies, and all 208 runs from that night joined to study
`default_prescramble_20260720` — superseded content. Concretely: its
`outpatient-02-gutierrez` row is *"Postpartum visit with bilateral hand numbness"* (Women's
Health Partners of Nashville), while the card the app shows today is *"Transformation from
episodic to chronic daily migraine"* (Lakeshore Internal Medicine). Different patient, same ID.

Every match/OVER verdict in that document — including the widely-quoted
"routine_priority → urgent over-triage band" — describes content that is no longer served.
**Do not cite it.** Two defects that made this easy to miss are now fixed:

- `run-validation.ts` had no study filter, and `case_number` is not unique across studies, so
  it selected all 52 cases from both. Now takes `--study=` (defaults to `default`).
- `tier-floor-breakdown.ts` read `validation_cases.ai_dimension_scores`, which
  `reseed-validation-cases.ts` deliberately NULLs. Scores are written to `validation_ai_runs`.

Before this run, study `default` had **zero** recorded runs. This is the first measurement
ever taken against the current demo content.

---

## Results

21 cases are scorable. 5 expect `emergent`, which cannot be reconstructed offline (see
Limitations).

| | count | of scorable |
|---|---|---|
| **Agree with label** | **10** | 48% |
| **Disagree — OVER-called** | **8** | 38% |
| **Disagree — UNDER-called** | **3** | 14% |
| Floor-driven tier (floor, not weighted score, chose the tier) | 5 / 26 | — |
| Not scorable offline (`emergent` expected) | 5 | — |

Full table: run the analysis command above. Key rows:

### Under-calls — the direction that matters most

| case | expected | weighted | final tier | note |
|---|---|---|---|---|
| `cross-04-patel` | routine_priority | **1.80** | routine | scored **5.00 → urgent** on the superseded content |
| `cross-05-kowalczyk` | routine_priority | **2.40** | routine | scored **5.00 → urgent** on the superseded content |
| `cross-02-vasquez` | semi_urgent | **2.40** | routine | two tiers below label |

The engine is **not uniformly conservative** — it is unstable in both directions. The two
5.00 → sub-2.50 swings are on different content, so they are not proof of engine drift, but
they are the largest movements in the set and warrant a look at the notes themselves.

### Over-calls — one floor does most of the work

`outpatient-01-jennings`, `outpatient-03-patterson`, `outpatient-06-caldwell` and
`cross-03-mcallister` share an identical shape: the weighted score maps to `semi_urgent`, and
**`red_flag_presence >= 4` floors it to `urgent`**. That single floor accounts for 4 of the
5 floor-driven tiers. It is a narrower and more tractable target than the tie-breaking rules.

### Gutierrez

`outpatient-02-gutierrez` now **AGREES**: 3.00, `semi_urgent`, no floor applied. The card was
relabelled `routine_priority → semi_urgent` on 2026-08-04 (`e1c01b8`) on the evidence of 14
production runs, and confirmed by Steve.

---

## Limitations — read before quoting any number

1. **This measures engine vs. these labels, not engine vs. truth.** The labels were set by
   Steve (DO) on 2026-07-20 (`f54e391`) — a real clinical pass, but the commit records that it
   was done "against his own clinical judgment **and an offline run of each note through the
   real triage engine**." The rater saw the engine's answer while labelling. That is
   **unblinded**, and it is a single rater with no inter-rater reliability. Until the labels
   are re-graded blind by two independent raters, "11 disagree" bounds the *disagreement*, not
   the *error*.
2. **The 5 `emergent` cases are unmeasured.** `emergent_override` is not stored in
   `validation_cases`, so the offline replay cannot reconstruct them and reports N/A. That is
   the highest-acuity fifth of the corpus — including the stroke alerts — and it is currently
   invisible to this method. Fixing it is a measurement change, not an engine change.
3. **n = 21 is small.** One case moves the agreement rate by ~5%. Treat differences under
   ~3 cases as noise.
4. **The engine is nondeterministic on borderline notes.** The same note produced 3.00
   (`semi_urgent`) and 3.50 (`urgent`) three minutes apart in production on 2026-08-04. This
   table uses the temperature-0 baseline run per case where available; a re-run will not
   reproduce it exactly. That is a property of the engine, documented on `/triage`, not a
   defect in this measurement.
5. **The `red_flag_override` floor cannot fire in offline replay** (that field is not stored),
   so floor-driven counts are a **lower bound**. The four dimension-score floors are faithful.

---

## What to do with this

**Blocking, before any calibration change:** re-grade the 21 scorable labels **blind** and
**independently** (see the review protocol handed to Steve). Inter-rater agreement between two
humans is the ceiling on what the engine can be measured against — if two clinicians disagree
on 6 of 21, then an engine disagreeing on 11 of 21 means something very different.

**Then, two separable experiments — not one:**
1. Is `red_flag_presence >= 4 → urgent` too blunt? It drives 4 of 5 floor-driven over-calls.
2. Why did three cases land two tiers below their label? Under-triage is the failure mode with
   no acceptable rate.

**Not before the partner demos (Mayo Aug 7, Novant Aug 12).** Over-triage is the designed
fail-safe direction; the under-call finding makes a rushed calibration change *more*
dangerous, not less. Presentation fixes shipped 2026-08-04 (`e1c01b8`) so a floor-driven tier
is now disclosed on screen rather than contradicting the published arithmetic.

## Reproducing

```bash
# Offline, free — reads recorded runs, no Bedrock calls
npx tsx scripts/tier-floor-breakdown.ts --study default

# Live re-baseline — COSTS MONEY, ~104 Bedrock calls, 20-35 min
npx tsx scripts/run-validation.ts us.anthropic.claude-sonnet-4-6 --study=default
```
