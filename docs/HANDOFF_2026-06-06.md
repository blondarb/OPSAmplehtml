# Sevaro Clinical (OPSAmplehtml) Handoff — June 6, 2026

## Audience
Next Claude session continuing the **Nova 2 Sonic voice migration validation** on `/consult` (AI Historian) and `/follow-up` (voice agent). Demo-only feature; no live patients.

## Current State
- **Branch**: `feat/nova-sonic-voice` — working tree **clean, all commits pushed** (HEAD `398138a`).
- **Build**: app `npx tsc --noEmit` clean; relay tsc clean; relay vitest 6/6. One *pre-existing* lint error remains (`src/hooks/useFollowUpRealtimeSession.ts:321` `catch (err: any)`) — not ours, untouched.
- **Local stack** (must be re-launched next session — processes were running this session):
  - Relay: `cd services/nova-sonic-relay && AWS_PROFILE=sevaro-sandbox AWS_REGION=us-east-1 NOVA_SONIC_REGION=us-east-1 RELAY_TRACE=1 npm run dev` (port 8081, `/healthz`).
  - App: `npm run dev` (port 3000).
  - **Requires** `aws sso login --profile sevaro-sandbox` first (SSO ~8h TTL).
- **Open PR**: [apps#120](https://github.com/blondarb/OPSAmplehtml/pull/120) — prod-RDS fix → `main`, **awaiting Steve's review/merge** (not auto-merged).
- **Bedrock**: `amazon.nova-2-sonic-v1:0` access GRANTED in us-east-1 (AUTHORIZED/AVAILABLE, verified 2026-06-05).

## Work Completed (this session)

### 1. Original task — follow-up AI escalation reaches dashboard (`520ee71`)
Fixed the pre-existing bug: `useFollowUpRealtimeSession.handleToolCall` read a non-existent `args.escalation_flags` array; the `save_followup_output` schema emits flat `escalation_triggered`/`escalation_tier`/`escalation_reason`. Added pure `escalationFlagFromToolOutput()` in `escalationRules.ts` (7 unit tests). **Not yet voice-E2E verified** (needs spoken audio).

### 2. Nova historian freeze — root-caused & fixed (`a86ef61`, `c56afef`, `6dc0279`, `398138a`)
- Real error surfaced via new relay error logging: **`Duplicate SYSTEM content. SYSTEM content can only be provided once per prompt.`** Nova allows ONE SYSTEM block per prompt; `pushSystemText` (localizer/scale/early-end) was sending more.
- Fix: added `userText()` builder (role USER) + routed `pushSystemText` through it. Verified by trace: a full 11-turn interview ran to completion + `save_interview_output` fired on End Interview, zero stream errors.
- Historian prompt tuned from live testing: plain ~8th-grade language, translate (don't recite) the referral, one/two questions max, never name a diagnosis to the patient, probe the differential deeper via plain symptom questions. Localizer push reframed as an "[INTERNAL CLINICAL NOTE — do not speak aloud]" USER turn.

### 3. Local-dev environment fixes (`.env.local`, gitignored — persists)
Steve's `.env.local` (dated Mar 13) was stale; fixed three things so the app runs locally:
| Fix | Detail |
|-----|--------|
| Cognito | Repointed to post-OAuth-migration pool `us-east-2_9y6XyJnXC` / client `6rahc3cs4846f05gf7fbucqi4d` + added `COGNITO_CLIENT_SECRET` (old pool `Owfb1zpgM` had no Hosted UI domain → `invalid_request`). |
| Bedrock creds | Removed expired temp SSO keys; added `AWS_PROFILE=sevaro-sandbox` so the default chain uses live SSO. |
| Nova relay | Added `VOICE_PROVIDER=nova`, `NOVA_SONIC_RELAY_URL`/`NEXT_PUBLIC_NOVA_SONIC_RELAY_URL=ws://localhost:8081`, `RDS_DATABASE=ops_amplehtml`. |

### 4. DB target fix (`c74e94e` on this branch; superseded by PR #120)
The `sevaro/rds/credentials` secret points at stale `github_showcase` (21 tables, missing `neurology_consults` etc.); full 57-table schema is in `ops_amplehtml`. This branch has a **dev-only** `RDS_DATABASE` override; PR #120 (off `main`) makes it authoritative in **all** envs and is the stronger fix.

| File | Change |
|------|--------|
| `src/hooks/useFollowUpRealtimeSession.ts` | escalation flag from tool output |
| `src/lib/follow-up/escalationRules.ts` (+ `__tests__`) | `escalationFlagFromToolOutput()` |
| `src/lib/historianPrompts.ts` | language + cadence + no-diagnosis-leak + deeper-probing |
| `src/hooks/useRealtimeSession.ts` | localizer push reframed as internal/MD-only |
| `services/nova-sonic-relay/src/{server,novaSonicSession,eventBuilders}.ts` (+ test) | `userText()`, error visibility, `RELAY_TRACE` |
| `src/lib/secrets.ts` | dev-only RDS_DATABASE override |

## What Was NOT Done
- **Voice E2E not fully verified** — needs Steve to speak into the mic. The follow-up escalation (VP5) and the latest historian prompt refinements have NOT been confirmed by a fresh voice run yet.
- **PR #120 not merged** (awaiting Steve).
- **`feat/nova-sonic-voice` not merged to main** — and its `secrets.ts` (dev-only override) will conflict with PR #120's all-envs version on rebase.

## Known Risks / Watch Items
1. **`secrets.ts` reconciliation**: when `feat/nova-sonic-voice` rebases on `main` after PR #120 merges, take PR #120's all-envs version (delete the dev-only `NODE_ENV !== 'production'` guard).
2. **Spawned tasks ran in the SHARED working tree** — the prod-RDS spin-off switched this dir to `fix/rds-database-override` mid-session, reverting the tree to pre-Nova and killing the relay. Future spin-offs must use isolated worktrees (see `feedback_parallel_claude_isolated_worktrees`).
3. **Latency/repetition**: trace showed Nova re-emitting assistant text with 7–14s gaps — feels like a freeze even though alive. May warrant turn-handling investigation.
4. `RELAY_TRACE=1` is on in the local run command — fine for dev; the trace is `RELAY_TRACE`-guarded so prod is unaffected.

## Required Next Steps
1. `aws sso login --profile sevaro-sandbox`, relaunch relay + app (commands above).
2. **Fresh `/consult` voice interview** — verify: one/two questions at a time, never names a condition (plain symptoms only), deeper differential probing, no freeze. Then read `/tmp/nova-relay.log` (trace) to confirm. Log in with Steve's real Sevaro creds (pool `9y6XyJnXC`), NOT the old `test-pipeline@` creds.
3. **Fresh `/follow-up` voice interview** — trigger an urgent symptom; confirm the AI escalation hits the red `EscalationAlert` banner + dashboard "Escalation Flags" (VP5).
4. Review/merge [apps#120](https://github.com/blondarb/OPSAmplehtml/pull/120).

## Files to Review First
- This handoff, then `services/nova-sonic-relay/src/novaSonicSession.ts` + `eventBuilders.ts` (Nova SYSTEM-content constraint), `src/lib/historianPrompts.ts` (prompt tuning), CLAUDE.md "Known Gotchas" (Nova single-SYSTEM rule, DB target).

## Prompt for Next Session
```
Repo: ~/dev/repos/OPSAmplehtml  (branch: feat/nova-sonic-voice)
Read docs/HANDOFF_2026-06-06.md first. Demo-only Nova 2 Sonic voice validation.
Setup: aws sso login --profile sevaro-sandbox; then start the relay
  (cd services/nova-sonic-relay && AWS_PROFILE=sevaro-sandbox AWS_REGION=us-east-1 NOVA_SONIC_REGION=us-east-1 RELAY_TRACE=1 npm run dev)
  and the app (npm run dev). Both must be up (ports 8081 + 3000).
Goal: voice-verify the unverified fixes. (1) /consult historian — one/two questions
  max, never names a diagnosis, probes the differential deeper, no freeze; read
  /tmp/nova-relay.log to confirm. (2) /follow-up — urgent symptom triggers the AI
  escalation onto the dashboard (VP5). Log in with Steve's real Sevaro creds.
Then check PR #120 (prod-RDS → main) status. Note: secrets.ts will need reconciling
  to PR #120's all-envs version when this branch rebases on main.
```
