# OPSAmplehtml Handoff — June 6, 2026

## Audience
Next dev session (Steve / Claude). Topic: prod DB data-source fix + Bedrock credential security remediation. (NOT the Nova-Sonic voice feature, despite the checked-out branch. A prior handoff for the voice work is in git history.)

## Current State
- **Branch (checkout)**: `feat/nova-sonic-voice` (working tree clean) — but this session's work landed on `main` + a separate PR branch, not here.
- **Prod**: healthy. Amplify app `d3ietjwgco4g2t` (app.neuroplans.app); 4 deploys this session (jobs 176–178 all SUCCEED). Prod DB = `ops_amplehtml`, verified.
- **Deploy status**: `main` deployed clean; compiles clean.
- **AWS**: profile `sevaro-sandbox`, us-east-2, account 873370528823.

## Work Completed

### 1. DB data-source fix (stability) — SHIPPED
PR **#120 merged** to `main` (commit `21c7ce7`) + deployed. `getRdsCredentials()` now honors `RDS_DATABASE` in **all** envs (was gated to non-prod). The shared `sevaro/rds/credentials` secret's `database` is a stale default (`github_showcase`, a different app's 21-table DB); the real 57-table schema is `ops_amplehtml`. Prod had only been correct by accident (no compute role → secret fetch failed → env-var fallback). Now correct-by-construction.

### 2. Bedrock static-key security remediation — SHIPPED
Retired the static long-lived IAM access key (the $96k-incident class).
| Step | Action |
|------|--------|
| Compute role | Attached existing `OPSAmple-AmplifySSR` to the app (was `computeRoleArn: null`); widened `BedrockInvokeAccess` to match the proven static-key set (foundation-model/* + inference-profile/* + KB Retrieve/RetrieveAndGenerate on `T4W8S8RNMN`) |
| Env vars | Removed `BEDROCK_ACCESS_KEY_ID` + `BEDROCK_SECRET_ACCESS_KEY` from Amplify `main` (19 vars remain; Amplify REPLACES whole map) |
| Key | Deactivated (NOT deleted) the IAM access key (id starts `AKIA…7BMK`) on user `opsample-bedrock` |
| Verified | Routes 200, `[RDS] Using Secrets Manager credentials … db: ops_amplehtml`, 0 credential/authz/relation errors |
Side benefit: RDS/OpenAI/Twilio creds now resolve from Secrets Manager (env kept as fallback).

### 3. `bedrock:RetrieveAndGenerate` IAM — defensive grant added
Added least-privilege `BedrockRetrieveAndGenerateModel` statement on `OPSAmple-AmplifySSR` (model `us.anthropic.claude-sonnet-4-6`). The simulator `implicitDeny` was a **tool artifact**, not a real gap.

### 4. RDS TLS validation — PR OPEN (not merged)
PR **#121** (`fix/rds-tls-validation`): replaces `ssl:{rejectUnauthorized:false}` with validation against a vendored AWS RDS CA bundle on both pools; `RDS_SSL_INSECURE=true` escape hatch. Verified against live DB (valid CA connects, bad/empty CA rejected). Worktree at `~/dev/repos/.worktrees/rds-tls`.

| File | Change |
|------|--------|
| `src/lib/secrets.ts` (via #120) | honor `RDS_DATABASE` in all envs |
| `src/lib/rds-ca-bundle.ts` (#121, new) | vendored RDS global CA bundle string |
| `src/lib/db.ts` (#121) | TLS validation on both pools |
| `CLAUDE.md` (#120, #121) | Known Gotchas updates |

### 5. AWS/Bedrock glossary → Slack
Sent shared terminology cheat-sheet to Riya Miglani's DM (U0B6C2YKHD5). Corrects "S3 = database" (S3 = object storage; RDS = the DB), spells out KB = Knowledge Base, etc.

## What Was NOT Done
- PR #121 not merged (touches prod DB connection — left for human verification).
- `opsample-bedrock` access key only **deactivated**, not deleted (instant rollback retained).
- `RetrieveAndGenerate` end-to-end not confirmed in-app (needs an authenticated `/consult` run).

## Known Risks / Watch Items
1. **Bedrock now runs on the compute role.** If AI features misbehave: reactivate the key + re-add the 2 BEDROCK env vars (Amplify replaces whole map). Reactivate with:
   `KID=$(aws iam list-access-keys --user-name opsample-bedrock --query 'AccessKeyMetadata[0].AccessKeyId' --output text --profile sevaro-sandbox); aws iam update-access-key --user-name opsample-bedrock --access-key-id "$KID" --status Active --profile sevaro-sandbox`
2. RDS CA bundle (#121) is a point-in-time snapshot — refresh if AWS rotates the root CA.
3. Static-key deactivation means no Bedrock fallback if the role path has a subtle issue — caught only by smoke test.

## Required Next Steps
1. **Smoke test prod** (login → `/triage` + `/consult`): confirms Bedrock runs on the role and the localizer KB works.
2. **Merge PR #121** when comfortable; then `git worktree remove ~/dev/repos/.worktrees/rds-tls`.
3. Once Bedrock confirmed in-app: **hard-delete** the `opsample-bedrock` key + user to fully retire it.

## Files to Review First
- `src/lib/secrets.ts`, `src/lib/db.ts` (+ `src/lib/rds-ca-bundle.ts` on #121)
- `CLAUDE.md` "Known Gotchas"
- Memory: `project_opsamplehtml_db_target.md`

## Prompt for Next Session
```
Repo: ~/dev/repos/OPSAmplehtml (AWS: --profile sevaro-sandbox, us-east-2)
Read docs/HANDOFF_2026-06-06.md first.
Context: prod DB data-source + Bedrock static-key security remediation shipped to main; prod healthy on ops_amplehtml.
Do next:
1. Smoke-test prod (login → /triage + /consult) to confirm Bedrock runs on the OPSAmple-AmplifySSR compute role and the /consult localizer KB works.
2. If good: merge PR #121 (RDS TLS validation), then `git worktree remove ~/dev/repos/.worktrees/rds-tls`.
3. If Bedrock confirmed working in-app, hard-delete IAM user opsample-bedrock's (currently deactivated) access key — look up its id via `aws iam list-access-keys --user-name opsample-bedrock` — and optionally the user.
Rollback if AI breaks: reactivate that key + re-add BEDROCK_ACCESS_KEY_ID/SECRET to Amplify main env (Amplify replaces the whole env map).
```
