# Sevaro Clinical - AI-Powered Clinical Documentation

> **MEMORY PROTOCOL:** Read HANDOFF.md first and update it last every session — it is the shared source of truth with ChatGPT/Codex.

Neurology outpatient web app: clinical notes, AI assistance, voice dictation, dot phrases, clinical scales, patient management.

## Deploy Workflow

**Push-to-deploy is enabled.** After making code changes:
1. Run local dev server (`preview_start`) and verify (no console errors, feature works)
2. Commit and push to `main` — Amplify auto-deploys
3. Do NOT wait for user approval — test locally first, then ship it

**AWS Amplify is the sole deploy target.** Do not add Vercel (or other host) build
config to this repo. A stray `vercel.json` was removed on 2026-05-29 after a Vercel GitHub
integration started posting deploy checks on PRs, contradicting the Amplify-only setup.
Note: removing `vercel.json` stops Vercel from reading repo build config but does **not**
disconnect the Vercel↔GitHub integration — that must be removed in the Vercel/GitHub
dashboard (Vercel project settings → Git, or the GitHub Vercel app's repo access). If you
still see a "Vercel" status check on PRs, the integration is still connected.

## Tech Stack

| Component | Technology |
|-----------|------------|
| Framework | Next.js 15.1.x with App Router, TypeScript |
| Styling | Tailwind CSS v3 + Inline Styles |
| Database | AWS RDS (PostgreSQL) via node-postgres |
| Auth | AWS Cognito OAuth + PKCE via Hosted UI at `auth.neuroplans.app` |
| AI | AWS Bedrock (Claude Sonnet 4.6) + OpenAI Whisper + Realtime API (WebRTC) |
| SMS/Voice | Twilio (SDK v5) for live patient follow-up demos |
| Deployment | AWS Amplify (push-to-deploy from main) |

## Key Patterns

- **Database:** All queries use `from()` / `wearableFrom()` from `@/lib/db-query` (node-postgres pools to RDS). Auth via `@/lib/cognito/server`.
- **Middleware:** `src/middleware.ts` — session refresh, simplified pass-through (avoids edge function issues).
- **AI Models:** gpt-5-mini (Q&A, chart prep, field actions), gpt-5.2 (visit extraction, assessment, briefings), Bedrock Sonnet 4.6 (triage, localizer), Realtime API (historian).
- **Auth:** Cognito pool `us-east-2_9y6XyJnXC`, client `6rahc3cs4846f05gf7fbucqi4d`. httpOnly cookies (id_token 1h, refresh_token 30d, 50-min proactive refresh).
- **Smart Recommendations:** 127 clinical plans synced from `blondarb/neuro-plans` via `npm run sync-plans`. ICD-10 scored matching links diagnoses to plans.

## Key Routes

| Route | Purpose |
|-------|--------|
| `/` | Homepage — 6-card demo platform |
| `/physician` | Clinical Cockpit (schedule + time-phased briefing) |
| `/dashboard` | Operations Dashboard (5-zone command center) |
| `/ehr` | Patient chart workspace |
| `/wearable` | Wearable monitoring dashboard |
| `/triage` | AI-assisted referral triage |
| `/consult` | Neuro Intake with AI Historian |
| `/follow-up` | Patient Follow-Up Agent (SMS + voice) |
| `/sdne` | SDNE XR assessment integration iframe |

## Commands

```bash
# Local dev
npm run dev

# Type check
npx tsc --noEmit

# Build
npm run build
```

## Known Gotchas

- `@/lib/db-query` `from()` auto-stringifies plain objects but passes arrays through raw — JSONB array columns (e.g. `transcript`, `red_flags`) must be `JSON.stringify()`'d at the call site before insert.
- **DB target = `ops_amplehtml` (57 tables), set via `RDS_DATABASE`.** The shared `sevaro/rds/credentials` secret's `database` field is a stale default (`github_showcase`, a *different* app's 21-table DB missing `neurology_consults`/`notifications`/`historian_sessions`/`scale_results`/`consult_reports`). `getRdsCredentials()` now honors `RDS_DATABASE` in **all** environments so the app stays on `ops_amplehtml`. Prod (Amplify `main`) sets `RDS_DATABASE=ops_amplehtml`; historically it only stayed correct because the SSR Lambda has no compute role, so the Secrets Manager fetch *fails* and the env-var fallback wins — meaning **if a compute role is ever attached to this Amplify app, the secret fetch would start succeeding and (pre-fix) flip prod to `github_showcase`.** The override defuses that. Do NOT repoint the shared secret to fix this — it's consumed by many apps (SDNE, SevaroMonitor, neurocrit-care-v2, evidence-engine, …); they all override the DB field, but it's a cross-app blast radius.
- Amplify SSR env vars require `next.config` inline block for runtime access (not just `process.env`).
- Triage route uses 202+poll pattern — do not attempt SSE streaming (reverted in PR #111 due to ~28s Amplify gateway timeout).
- `/consult` Historian uses WebRTC Realtime API — requires HTTPS and browser mic permission.
- **ASR vocabulary biasing** (`src/lib/asr/clinical-lexicon.ts`) is on by default for all voice surfaces (Realtime `prompt` + Deepgram `keyterm`). Hot-revert with `ASR_VOCAB_BIASING=false` if a transcription endpoint rejects the biasing field. Whisper's prompt is capped ~224 tokens, so the lexicon is budget-trimmed (session-specific patient/provider/med terms are hoisted first).
- **RDS connections validate TLS** against the vendored AWS RDS CA bundle (`src/lib/rds-ca-bundle.ts`, the full `global-bundle.pem` embedded as a string so it survives Amplify SSR Lambda bundling — loose `.pem` files get dropped). Both pools in `src/lib/db.ts` use `ssl: { ca: RDS_CA_BUNDLE, rejectUnauthorized: true }` (replacing the old `rejectUnauthorized: false`). **Emergency escape hatch:** set `RDS_SSL_INSECURE=true` to fall back to no-validation without a code revert (e.g. if AWS rotates the root CA before the bundle is refreshed). NOTE: Amplify env vars **REPLACE** the whole map on update — adding `RDS_SSL_INSECURE` means re-supplying the **full** existing env map, not just the one var. Refresh the bundle by re-downloading `https://truststore.pki.rds.amazonaws.com/global/global-bundle.pem` and replacing the template literal.

## Recent Changes (Summary)

- **Historian Validation Suite Task 6: synthetic patient conversation driver — BLOCKED on live gate (2026-07-21)**: Sixth and final build task of the sprint (branch `feature/historian-validation-suite`) — a synthetic patient (Bedrock Sonnet, persona-conditioned) converses with the REAL historian agent (OpenAI Realtime, text modality, raw WebSocket) through the REAL app endpoints (`/session` textMode, `/transcript-flush`, `/scales`, `/evidence-query`, `/save`), so a driver session exercises flush→save→the Task 2-4 evaluator pipeline exactly like production. **Code is complete and fully tested; the mandated 5/5 live-session gate could not be completed — see BLOCKED finding below.** New `src/lib/historian/synthetic/realtimeTextClient.ts` — a from-scratch WS client (the `ws` package, added as a devDependency; not previously a direct dependency) speaking the SAME Realtime event protocol already live-verified in `src/lib/voice/providers/openaiWebrtcProvider.ts` (its WebRTC data channel and this raw WebSocket carry identical event JSON — only the transport differs): waits for `session.created` → sends `session.update` (`{type:'realtime', modalities:['text'], instructions, tools, turn_detection:null}` — `session.type` is REQUIRED, confirmed by that file's own prior fix) → resolves on `session.updated`. `response.done`'s `response.output[]` array is the authoritative source for both assistant text and function calls — function-call extraction genuinely mirrors `openaiWebrtcProvider.ts`'s own `response.done` handler; the text-message shape does NOT (that file only handles AUDIO-modality transcript events, unrelated to text-modality message items) and, as of this writing, has never been exercised against a real live response — see the post-review fix-round addendum below for the correction and the defensive guard added because of it; `response.output_text.delta` is accumulated only as a best-effort fallback, never load-bearing. 30s per-exchange timeout, 10-minute whole-session wall-clock deadline, one automatic transport-level reconnect on an unexpected close (a fresh WS is a brand-new Realtime session with no history — the in-flight exchange at drop time is failed, not silently replayed). New `src/lib/historian/synthetic/patientAgent.ts` — pure `buildPatientSystemPrompt`/`toBedrockMessages` (TDD, no I/O) + a thin `generatePatientReply` Bedrock wrapper (plain `invokeBedrock`, persona answers are free text); persona loading via a new additive `loadPersonaProfile` export on `personaFixtures.ts` (demographics/historyResponses/structuredHistory — the Task 5 module already had `buildPersonaTranscript`/`listPersonaFiles`, extended rather than duplicated via a shared `readPersonaJson` helper). New `src/lib/historian/synthetic/cliArgs.ts` — pure arg parsing (`--persona`/`--all-personas`/`--base-url`/`--max-turns` [default+hard-cap 25, matching the historian's own CRITICAL RULE 13]/`--i-know-this-is-not-localhost`/`--verbose`) + `assertBaseUrlAllowed` (refuses any non-localhost `--base-url` unless explicitly overridden — OpenAI direct has no BAA). `scripts/historian-synthetic-run.ts` — the orchestrator (mint textMode session → greeting → turn loop → tool dispatch [`scale_step`→`/scales`, `query_evidence`→`/evidence-query` (this route requires physician/Cognito auth the driver deliberately lacks; its 401 body is itself a valid `QueryEvidenceResponse` error shape the historian prompt already handles gracefully — mirrors the production client's own "parse JSON regardless of status" behavior), `save_interview_output`→captures structured output + ends] → flush every 3 entries [fail-open, tolerates the expected 42P01 pass-through since migrations aren't applied here] → `/save` → local `validateTranscript`). **SYNTHETIC-DATA MARKER**: `referral_reason` and (when captured) `structured_output.chief_complaint` are both prefixed `"[SYNTHETIC-DRIVER] "` before the `/save` POST; `patient_name` is set exactly as the persona JSON has it — so any UI list is unmistakably marked. One whole-session retry on failure, per-persona summaries (sessionId/turns/duration/save status/local transcript validity/`evalAutorunExpected` — the exact `HISTORIAN_EVAL_AUTORUN !== 'false' && saveOk` condition the save route itself checks, explicitly NOT a confirmation the fire-and-forget evaluators actually completed). `textMode` param added to `session/route.ts` (Task 6 item 4): `body.textMode === true` or `?textMode=1`/`?textMode=true` short-circuits to `{sessionId, flushToken}` ONLY — sessionId/flushToken minting was hoisted above the mode branch (was previously minted after the provider branch) so textMode returns it without duplicating the mint logic; zero behavior change for every existing (non-textMode) caller. TDD: `tests/historian-eval/patientAgent.test.ts` (12 tests, pure prompt assembly), `tests/historian-eval/realtimeTextClient.test.ts` (2 tests — happy-path + function-call-path — against a REAL local `ws.WebSocketServer` scripted to speak the Realtime protocol, not an injected fake, per the task's explicit "integration-tested live, not unit-mocked to death" scoping), `tests/historian-eval/syntheticCliArgs.test.ts` (14 tests), `src/app/api/ai/historian/session/__tests__/route.test.ts` (5 tests — textMode returns exactly `{sessionId, flushToken}` with none of the 12 voice-credential fields and never calls OpenAI/getConsult; default path unchanged). Verified: tsc clean; full vitest suite 2,685 passed / 0 failed (2,652 + this task's 33 new tests; same pre-existing `simulated-patients/runner.test.ts` live-dev-server skip as every prior task); lint clean on every new/touched file (one pre-existing `catch (error: any)` in `session/route.ts` confirmed unrelated/unchanged by diff, same convention as every prior task's lint scoping). — **LIVE-GATE FINDING (BLOCKED): the task brief's specified `OpenAI-Beta: realtime=v1` header is REJECTED by the current API** — `{"type":"invalid_request_error","code":"beta_api_shape_disabled","message":"The Realtime Beta API is no longer supported. Please use /v1/realtime for the GA API."}` — even though the connection URL already targets `/v1/realtime` (the GA API); the stale beta header itself is what triggers rejection. Root-caused via an isolated WS-handshake-only probe (no Bedrock calls) that toggled just this one header: removing it changes the failure from `beta_api_shape_disabled` to a COMPLETELY DIFFERENT, later-stage error — `insufficient_quota` ("You exceeded your current quota, please check your plan and billing details") — proving the connection/session.update schema is itself correct and accepted by the GA API once the header is dropped (fixed in the shipped code; this is a narrow, evidence-based correction, not the "audio workaround" the brief's escalation clause warned against). **The `insufficient_quota` error is on the PRODUCTION OpenAI key** (`sevaro/openai/ops-amplehtml` in Secrets Manager, `LastAccessedDate` 2026-07-15 — confirmed via SHA-256 hash comparison against `.env.local`'s different `OPENAI_API_KEY`, so this is not a local/test-key artifact) — a billing/plan-tier account state, not a request-rate issue, and not fixable from code; needs Steve or whoever manages the OpenAI account billing to check platform.openai.com. **This may mean the LIVE production Historian voice feature is also currently non-functional** — worth an independent, urgent check outside this task. Live gate result: 0/5 (single-persona smoke attempted twice — once reproducing the header rejection, once confirming the fix reaches the quota wall cleanly; did not proceed to `--all-personas` since every persona would fail identically at the same account-level block, and further attempts would only consume API calls against an exhausted quota with no chance of success). Every other Task 6 deliverable (code, both textMode + realtimeTextClient tests, the arg-parsing tests, tsc/lint/full-suite) is complete and green — only the live-execution acceptance criterion is blocked, by an infrastructure/billing constraint outside this task's control. **Post-review fix round (same day):** reviewer flagged two Important defensive gaps that had to land before any live-gate re-run. (1) **Silent false-PASS path**: `status` was parsed but never checked, and the text-message shape (unlike function-call parsing, which does mirror `openaiWebrtcProvider.ts`) has never been exercised live — if that shape assumption is wrong, every turn would silently resolve empty, the loop would run on patient monologue to the cap, `validateTranscript` would still pass (structural checks only), `/save` would still succeed, and the summary would misreport PASS. Fixed: `RealtimeTurnResult` gained `id`; `historian-synthetic-run.ts`'s `processTurn()` (the single choke point every turn passes through) now unconditionally warns (never `--verbose`-gated — no utterance text in these lines) and counts against a new `consecutiveBadTurns` state whenever a turn's `status !== 'completed'` OR it resolves with neither text nor a function call; 2 consecutive throws a new `HistorianTurnFailureError` through the existing session-failure/retry path — silent-empty becomes loud-fail, never a false PASS. Corrected the realtimeTextClient.ts module doc's overclaim (see above) into an honest WIRE-SHAPE PROVENANCE + DEFENSIVE PARSING split. (2) **Zero coverage on reconnect + delta-fallback**: 3 new tests in `realtimeTextClient.test.ts` (still against the real local `ws.WebSocketServer`, no injected fakes) — a scripted mid-response `ws.terminate()` verifying exactly-one auto-reconnect (detected via the 2nd `session.updated` wire event) then a SECOND `ws.terminate()` on the reconnected socket confirming that's terminal (no 3rd connection); a `response.output_text.delta`-then-empty-output `response.done` confirming the delta-fallback AND (doubling up) that a non-`'completed'` status round-trips; a fully-empty `response.done` confirming the client resolves a well-formed empty result rather than crashing/fabricating content. (3) **Minors**: `close()` now rejects a pending `connectSettle` (previously a real dangling-promise bug if called mid-reconnect); `assertBaseUrlAllowed` now accepts `'[::1]'` (bracketed — what `URL.hostname` actually returns for IPv6 loopback; the bare `'::1'` check was dead code), with a test asserting on `new URL(...).hostname` directly so it would have caught the original bug. Verified: tsc clean, lint clean, full vitest suite **2689 passed / 0 failed** (2685 + 4 new tests). No live gate attempted this round (still quota-blocked; the rollout runbook owns the re-run). **Post-review fix round 2 (same day):** re-review found the fix-round-1 guard had been reopened at a second call site. **(1) IMPORTANT**: the turn-cap/wall-clock force-end block's nudge-to-save attempt caught `HistorianTurnFailureError` (fix-round-1's hard-fail signal, thrown from `processTurn`) in the SAME generic `catch { log non-fatal }` used for genuine network hiccups — so a hard-fail firing on the forced FINAL turn got swallowed and the session proceeded to `/save` and reported PASS anyway, the identical bug class fix-round-1 closed, reopened one call site over. Fix: extracted `processTurn`/`HistorianTurnFailureError`/`DriverState`/`TurnContext`/`appendTranscriptEntry`/`newState` out of `scripts/historian-synthetic-run.ts` into new `src/lib/historian/synthetic/turnProcessor.ts` (a new narrow `RealtimeTurnClient` interface replaces the concrete `RealtimeTextClient` class as `processTurn`'s parameter type, so tests can inject a fake — a real client still satisfies it structurally, zero change at the live call site); new `attemptNudgeToSave(nudge, log, personaId)` re-throws `HistorianTurnFailureError` specifically while still swallowing+logging any other error. **(2) MINOR**: new `tests/historian-eval/turnProcessor.test.ts` (10 pure tests, no WS/network) covering bad-status-increments, empty-turn-increments (unconditional warning, verbose:false included), good-turn-resets, 2-consecutive-throws-`HistorianTurnFailureError`, a function-call-without-text correctly NOT treated as empty, and both directions of `attemptNudgeToSave`'s error split. Verified: tsc clean, lint clean, full vitest suite **2699 passed / 0 failed** (2689 + 10 new tests). Still no live gate attempted — quota-blocked; the rollout runbook owns the re-run. Full detail: `.superpowers/sdd/task-6-report.md`.

- **Historian Validation Suite Task 5: batch eval harness + QI/IRB report + release gates (2026-07-21)**: Fifth task of the 6-phase sprint (branch `feature/historian-validation-suite`) — ties Tasks 2-4's evaluators (`generateFinalDifferential`, the thoroughness judge, the independent R1 differential, cross-model agreement) into one versioned command producing the artifact Steve will use for QI/IRB prep. `npm run historian:eval` (`scripts/historian-eval.ts`, thin wrapper mirroring `scripts/triage-sentinel.ts`'s runtime-injection style) → `src/lib/historian/eval/cli.ts` (argument parsing + orchestration, `HistorianEvalCliRuntime` injecting fs/env/stdout/`runCase` for testability) → `src/lib/historian/eval/report.ts` (pure aggregation + gate evaluation + md/json rendering, zero Bedrock/DB imports). **Modes**: `--fixtures` (default, the 5 sprint personas, no DB) and `--sessions <ids…|--since ISO>` (real `historian_sessions` rows, persists via `persistEvaluation`/a direct `final_differential` column UPDATE — both tolerate 42P01/42703 quiet-skip). **Live gating**: `runCase` (the only thing that can reach Bedrock/DB) is invoked only with `--live`; without it, both modes render a structure-only dry run (every gate `NOT EVALUATED`, every evaluator `skipped`) — safe for CI/pre-rollout at zero cost. Module-load discipline mirrors `liveRunner.ts`'s `loadDefaultLiveDependencies`: finalDifferential/thoroughnessJudge/independentDdx/agreement/persistEvaluation/`@/lib/db` are all behind dynamic `await import(...)` inside the default case runner, never statically imported by cli.ts. **Output**: `qa/historian-eval/results/<yyyy-mm-dd>[-vN]/historian-eval-report.{json,md}` — `resolveOutputDirectory()` auto-bumps to `-v2`/`-v3`/… on collision (checked via an injected `pathExists`), but only for the computed default; an explicit `--out` is used as-is. **Report** (8 required sections, all present in both md and json): investigational banner + headline; a self-label line ("developer baseline — not clinician-vetted") shown whenever any case's rubric is unvetted; an honest-n line computed from the actual case count (`n=5 development-set personas; tuning permitted; no held-out claims` for fixtures, a distinct sessions-mode framing for real data); a provenance table (model ids/prompt versions/rubric versions+vetted status/inference params, aggregated as distinct-value sets across cases — provenance fields are carried on the `HistorianEvalRunResult<T>` wrapper itself rather than read out of each evaluator's own result type, since `AgreementResult` has no embedded provenance field at all); per-case scorecards (thoroughness overall/confidence/missed-criticals/coverage_disagreement, pipeline + R1 top-3, agreement metrics, ground-truth hits for both differentials, fidelity findings when present); aggregates as min/mean/max ranges, never point estimates; a gate table (value vs threshold, pass/fail, `NOT EVALUATED` in a dry run rather than an incidental pass on zero data); cost/latency per evaluator per case + totals (cost is genuinely `unknown`, never coerced to zero, for `final_differential`/`independent_ddx`/`agreement`, none of which expose token usage on their public API — only `thoroughness` has a real `cost_usd`, via a new export of the previously-private `generateThoroughnessEvaluationWithUsage`, purely additive). **Release gates** (`qa/historian-eval/release-gates.json`, own hand-rolled validator in report.ts mirroring `catalog.ts`'s isRecord/boundedString/enumValue/fail convention): `thoroughness-floor` (mean overall ≥70), `zero-diagnosis-leaks` (deterministic leak count eq 0), `ddx-top3-ground-truth` (pipeline GT top-3 rate ≥0.6), `independent-agreement-top3` (top3Overlap≥1 rate ≥0.5) — every gate `scope: synthetic_software_release_only`, top-level `clinicalValidationClaim: false`. **Persona fixture relocation**: `buildPersonaTranscript`/`listPersonaFiles` moved to `src/lib/historian/eval/personaFixtures.ts` (cli.ts, under `src/`, cannot depend on `tests/` — wrong dependency direction, and the `@/*` alias only maps to `./src/*`); `tests/historian-eval/fixtures/personaTranscripts.ts` is now a one-line `export *` re-export shim so every existing test import keeps working unchanged. Extended with an optional `narrativeSummary` field (the persona's pre-authored physician-style write-up) — the harness passes it as `reports.narrative_summary` to the thoroughness judge so the fidelity screen can run in fixtures mode; `structuredOutput` is deliberately left `null` for every fixture case (personas' `structuredHistory` is a similarly-shaped but distinct object missing the required `hpi` field), so the deterministic structured-output check honestly reports "structured_output is missing" rather than a synthesized false positive — documented in cli.ts's docstring as the known extension point (`--sessions` mode already has the real `narrative_summary` column). TDD: `tests/historian-eval/report.test.ts` (24 tests — banner, self-label both branches, honest-n computed not hardcoded, gate math incl. exactly-at-threshold, ranges render distinctly, the release-gates JSON validator) and `tests/historian-eval/cli.test.ts` (22 tests — unknown flag → usage + exit 1, `--sessions` with neither ids nor `--since` → usage + exit 1, `-v2`/`-v3` collision bump, `--help`, dry-run vs live call counts, explicit `--out` bypasses bumping, gate-failure exit code) — all via injected fakes, no real Bedrock/DB touched by any unit test. **Live baseline run** (`--fixtures --live`, AWS_PROFILE=sevaro-sandbox/us-east-2, one pass, ~20 Bedrock calls, no retry loop beyond each evaluator's own built-in single retry): committed at `qa/historian-eval/results/2026-07-21/`. Gate result: 3/4 PASS — `zero-diagnosis-leaks` 0 (eq 0), `ddx-top3-ground-truth` 0.8 (≥0.6, 4/5 personas), `independent-agreement-top3` 1.0 (≥0.5, 5/5 personas); `thoroughness-floor` **FAILED** at mean 61.6 vs the ≥70 threshold (range 58-67 across the 5 personas) — recorded as data per the task's explicit instruction, nothing tuned to force a pass. Every case's rubric is unvetted (Task 3's `vetted_by: null` until Steve signs off), so the report's self-label correctly reads "developer baseline — not clinician-vetted." **Known limitation, not fixed (materiality judged low)**: the "agreement" evaluator's cost/latency row measures only the `computeAgreement()` call itself; the separate `scoreAgainstGroundTruth()` calls (same module, same Haiku adjudicator, invoked for ground-truth scoring) aren't independently time-tracked, so total latency is a slight undercount of true wall-clock — does not affect any gate's correctness, only the granularity of the cost/latency section. Verified: tsc clean; full vitest suite 2,652 passed / 0 failed (same pre-existing `simulated-patients/runner.test.ts` live-dev-server skip as every prior task in this sprint); lint clean on every new/touched file. Not yet merged; migrations 057/058 not yet applied to RDS (this task's `--fixtures` live run never touches `historian_sessions`/`historian_evaluations` at all — only `--sessions` mode with `--live` would, and that path wasn't exercised here for lack of a rolled-out DB to query).

- **Historian Validation Suite Task 4: independent DDx scorer (DeepSeek-R1) + agreement metrics + GET-auth security fix (2026-07-21)**: Fourth task of the sprint (branch `feature/historian-validation-suite`) — the cross-family half of the validation design: a SECOND, independent differential from a DIFFERENT model family (DeepSeek-R1), blind by construction to Task 2's Sonnet pipeline (`generateIndependentDdx(transcript, chiefComplaint?)` takes only those two params — structurally impossible to leak structured HPI, localizer output, `final_differential`, rubrics, or persona ground truth into it), plus deterministic-first agreement metrics between the two. New `src/lib/historian/eval/agreement.ts` (pure, zero Bedrock/DB imports, adjudicator always an injected function): `normalizeIcd10` (3-char ICD-10-CM category reduction); `computeAgreement` (ICD-10 category match as the deterministic fast path — two valid-but-different categories are a confident non-match, never adjudicated; everything else batched into exactly ONE `adjudicate()` call; `top1Match` read directly off the truth table, independent of the top-3 assignment so a rank-1 mismatch can never be masked by overlap found elsewhere); `scoreAgainstGroundTruth` (case-insensitive exact-string fast path against a persona's hand-authored `expectedDDx` text, same batched-adjudication fallback). New `src/lib/historian/eval/independentDdx.ts`: R1 requires the native Bedrock `InvokeModel` wire format, NOT `invokeBedrockJSON`'s Anthropic Messages shape (confirmed live — an Anthropic-shaped request returns a hard `ValidationException`, not a parse quirk) — a small dedicated `invokeDeepSeekR1Json` adapter speaks R1's actual `{prompt, max_tokens, temperature}` / `{choices:[{text, stop_reason}]}` format, strips the `<think>` chain-of-thought prefix, and mirrors `invokeBedrockJSON`'s lenient parse/repair policy as a documented local duplicate (never touching the shared, clinically-live `bedrock.ts`); one retry on a thrown error or shape-invalid response, then fail-closed; reuses `finalDifferential.ts`'s `sanitizeDifferential` (newly exported, zero behavior change for existing callers) so both differentials are cleaned by identical logic instead of a second hand-rolled copy. Production adjudicator `adjudicateEquivalence` is Haiku 4.5 tool-use, batched, defensively padded to `false` on a short/malformed model response. New `DdxComparisonCard.tsx` (side-by-side pipeline-vs-independent columns + agreement badge) on `HistorianReportView`'s Physician Report tab and a new `compare` sub-tab on `HistorianSessionPanel`; `save/route.ts`'s GET handler enriched with a `historian_evaluations` LEFT JOIN LATERAL for `independent_ddx`/`agreement` (fail-open to the base query on `42P01` — migration 058 not yet applied). **Security fix (commit `9f1490d`, same-day review round) — CRITICAL: `GET /api/ai/historian/save` had NO auth check at all**, despite returning patient names, MRNs, full transcripts, and (as of this task) both differentials — `getUser` was imported at the top of the file but never called in `GET`. Fixed: `getUser()` → 401 gate as the first line of the GET handler's try block, zero DB touch (`getPool` never called) when unauthenticated; confirmed both real callers (`ClinicalNote.tsx`, `home/HistorianSessionsList.tsx`) are physician-authed pages, and documented the auth boundary in a code comment (POST = intentionally unauthenticated patient-portal pattern, same bar as `/session`/`/transcript-flush`; GET = physician-only, Cognito-authenticated). Same review round fixed two more: R1 was running at temperature 0 (greedy decoding) with the instructional prompt placed before a nonexistent system-role slot, both contradicting AWS's own canonical InvokeModel example for this model and DeepSeek's own published guidance (greedy decoding "can lead to performance degradation and endless repetitions") — corrected to DeepSeek's recommended temperature 0.6, prompt structure now matching AWS's example exactly; and `computeAgreement`'s greedy bipartite matching could under-count `top3Overlap`/`jaccardTop3` (classic greedy failure mode: one item matching two candidates while another matches only one of them) — replaced with an exact `maxBipartiteMatching` brute-force search (trivial at TOP_N=3; `top1Match` unaffected, since it was already independent of the assignment). **Live gate** (`HISTORIAN_EVAL_LIVE=1`, all 5 personas, re-run post-fix with fresh pipeline differentials too): R1 succeeded 5/5 (zero failures) · top1Match **3/5** (was 5/5 at temperature 0 — expected, sampling temperature directly changes R1's own top-3 content) · pipeline ground-truth **5/5** top1/top3 (was 4/5 top1) · R1 ground-truth 3/5 top1, 5/5 top3 — numbers reported as-is, not tuned to a target. Verified: tsc clean; full `npx vitest run` 2,606 passed / 53 skipped / 1 pre-existing unrelated failure (`simulated-patients/runner.test.ts`, live-dev-server gap predating this task); lint clean on every new/touched file. Not yet merged; migration 058 not yet applied to RDS. Commits `c9d2500` (feature), `9f1490d` (security fix + correctness review round).

- **Historian Validation Suite Task 3: thoroughness judge + fidelity screen + vetted rubric system (2026-07-20)**: Third task of the 6-phase sprint (branch `feature/historian-validation-suite`) — an AI judge that scores whether a completed AI Historian interview gathered clinically appropriate data, feeding Task 5's batch reports and release gates. **Rubric system**: `qa/historian-eval/rubric/` — one base rubric (`base-neuro-hpi.json`, OLDCARTS-generic) plus 5 syndrome rubrics matching the sprint's persona set (acute-stroke, first-seizure, migraine-chronic, ms-relapse, peripheral-neuropathy), each a small `critical_questions` checklist (id/question/severity: critical|important|minor) drafted from clinical first principles per the plan (stroke: LKW, anticoagulant use, thrombolysis contraindications; seizure: first-vs-recurrent, tongue-biting/incontinence, alcohol/withdrawal, driving; migraine: thunderclap, fever/neck stiffness, papilledema sx, medication-overuse; MS: prior episodes/optic neuritis, bladder sx; neuropathy: distribution, diabetes, B12/alcohol, weight loss) — every file headers `"_note": "[training knowledge — Steve to vet]"` with `vetted_by`/`vetted_date` null until clinician sign-off. `src/lib/historian/eval/rubric.ts` loads + hand-validates (see below) + picks base+syndrome by explicit id or keyword-scored chief-complaint detection (`detectSyndrome`, ties/zero-hits fall back to base-only). **Interpretation flagged**: the brief pointed at `src/lib/triage/sentinel/catalog.ts` as the "ajv pattern" to follow for `rubric.schema.json` validation — that file actually validates entirely by hand (isRecord/boundedString/enumValue/fail helpers), not via ajv (an indirect, undeclared transitive dependency only); `rubric.ts` follows that ACTUAL hand-rolled pattern rather than adding ajv as a new direct dependency. Rubric JSON files (under `qa/`, outside `src/`) are pulled in via static `import … from '*.json'` rather than `fs.readFileSync`, to avoid the same Amplify-SSR-Lambda loose-file-bundling gotcha documented for `rds-ca-bundle.ts`; validation is lazy/memoized (throws only when `loadRubric()` is actually called, never at module-eval time) so a bad rubric file can't crash the save route's module load. **Deterministic pre-layer** (`deterministicChecks.ts`, always runs, findings always appended to the LLM result): a diagnosis-leak lexicon scanning ASSISTANT turns only (a do/does-guarded "you have" pattern plus assertion-prefix-plus-syndrome-disease-name combos — the bare `"you have"` seed phrase needed a question-form exclusion or it false-positived on every ordinary "Do you have any allergies?"); phase-marker presence derived AT RUNTIME from the real (newly-exported) `historianPrompts.ts` `PHASED_INTERVIEW_STRUCTURE` constant via marker-based extraction + signal-word overlap (never a copied/duplicated script string, and tolerant of live-model paraphrasing); the existing 25-patient-turn cap; and required-field presence on `structured_output`/`narrative_summary` (the latter is a separate `historian_sessions` column, not part of `HistorianStructuredOutput` — caught and fixed via a real tsc error mid-build). **Judge** (`thoroughnessJudge.ts`, prompt version `thoroughness-v1`): one schema-forced Sonnet tool call scoring 6 fixed dimensions (oldcarts, red_flags, pmh_meds_allergies, fh_sh, question_quality, closure) with cited `evidence_turns`, plus `missed_critical_questions`, `diagnosis_leak`, an optional `fidelity` screen (fabricated-claims/material-omissions, schema-included only when `reports` are supplied — currently just `narrative_summary`, threaded from the save route), `overall`, and `confidence`. Every LLM claim is defensively sanitized against ground truth rather than trusted: `missed_critical_questions[].rubric_id` must reference a real rubric id (severity is OVERRIDDEN from the rubric's own record, never the model's claim) and `overall` is capped at 85 if any surviving entry is severity `critical` (instructed to the model AND enforced as a deterministic backstop); `diagnosis_leak` quotes must be verbatim substrings of the cited assistant turn, with the deterministic scanner's own hits always folded in regardless of what the model reported; `fidelity.fabricated_claims[].report` must reference a real `reports` key. `unvetted: true` propagates whenever the loaded rubric(s) have `vetted_by: null` (currently always, until Steve vets). **Persistence**: migration 058 (`historian_evaluations`, verbatim per spec — **not applied**) + `persistEvaluation.ts` (raw parameterized insert, JSONB pre-stringified, 42P01 quiet-info per the Task 1/2 precedent) + a new `MODEL_PRICING`/`computeCostUsd` in `constants.ts` (Sonnet 4.6 at $0.003/$0.015 per 1k input/output tokens — cross-checked against AWS's historical Sonnet-tier pricing pattern, not independently verified against the live AWS pricing page; unknown model id → null cost, never throws; cache tokens not priced). **Save route**: the judge now runs sequentially after Task 2's final differential — chained via `.then()` off that call's own (already-recovered) promise rather than nested in one function, so Task 2's existing hook test/coverage is untouched; both stages fail open independently (a final-differential failure doesn't block the judge from still running). **Live gates** (both passed on the first run, no prompt tuning): stability — migraine-chronic judged 3x, overall scores [52, 58, 52], spread 6 (≤10 threshold); discriminant — acute-stroke with its 3 rubric-critical Q&A pairs deterministically stripped by keyword (onset-time, prior-stroke/TIA-history, medical-history/anticoagulant) scored 32 vs intact's 72, delta 40 (≥15 threshold) — missed-critical-questions count rose 3→7 stripped vs intact, a clean, honest discrimination signal. TDD RED→GREEN on rubric/deterministicChecks/thoroughnessJudge/persistEvaluation/the save-route hook (135 new unit tests total); deterministic checks tested against REAL `historianPrompts.ts` strings, not mocked constants. Verified: tsc clean, full vitest suite green (2,528 passed; same pre-existing `simulated-patients/runner.test.ts` live-server skip as Task 1), lint clean on every new file (3 pre-existing `save/route.ts` lint errors confirmed unrelated/unchanged by diffing against the unmodified file). Not yet merged; migration 058 not yet applied to RDS. **Post-review fix round (same day):** added a `coverage_hints` lexical screen (2-5 conservative substrings per severity:"critical" syndrome item — `computeCriticalCoverage`, scans ALL turns including patient answers) that surfaces unmatched criticals to the judge as `verify_specifically` and, if the judge still doesn't flag them, sets a `coverage_disagreement` audit flag — deliberately NEVER used to clamp `overall` by itself (a negative test asserts this trust boundary directly); `validateRubric` now enforces `additionalProperties:false` at both file and critical_questions-item level and cross-file version-uniqueness moved into `ensureLoaded()` (`assertUniqueRubricVersions`, independently unit-tested); explicit code comments added on `reports`' save-time-only scope; `computeCostUsd` gained a test against an injected fake pricing entry. Stability gate re-run with the new prompt logic live: identical [52, 58, 52], spread 6. Full details: `.superpowers/sdd/task-3-report.md`'s fix-report addendum.

- **Historian Validation Suite Task 2: final full-transcript differential + investigational card (2026-07-20)**: Second task of the sprint (branch `feature/historian-validation-suite`) — a post-session (never mid-interview), completely separate Bedrock evaluation pass over the COMPLETE historian transcript, producing a scoreable, citation-grounded differential for retrospective QA/audit review only — the historian agent itself never diagnoses (untouched by this task). New `migrations/057_historian_final_differential.sql` (+ `.down.sql`, **not applied**) adds `historian_sessions.final_differential JSONB`. `src/lib/historian/eval/finalDifferential.ts`'s `generateFinalDifferential(transcript, chiefComplaint?)`: a deterministic `MAX_TRANSCRIPT_CHARS=60_000` fail-closed guard (`TranscriptTooLargeError`) before any Bedrock call; shared symptom extraction (`SYMPTOM_EXTRACTOR_PROMPT`, lifted byte-identical out of `localizer/route.ts` into new `src/lib/consult/symptomExtractorPrompt.ts` — `route.ts` files can only export handler names, so extraction was required, not optional; live localizer behavior unchanged, verified via diff) applied to the FULL numbered transcript, not a slice; `neuro_plans`-grounded evidence via the same `retrievePlanEvidence()` the live localizer uses; one schema-forced Sonnet tool call (max 6 diagnoses); every `supporting_quotes`/`contradicting_quotes` entry verbatim-substring-validated against its cited turn, non-verbatim/out-of-range quotes dropped and counted (never logged) in a new `dropped_quotes` field. `runFinalDifferential()` is the fire-and-forget persistence wrapper wired into `save/route.ts` right after the session insert (gated by `HISTORIAN_EVAL_AUTORUN`, never awaited); a `42703` (column not yet migrated) failure logs one quiet `console.info`, mirroring Task 1's `historian_transcript_events` `42P01` precedent exactly. New `DifferentialCard.tsx` (banner from a new `INVESTIGATIONAL_BANNER` constant, ranked diagnoses, likelihood badge, ICD-10, rationale, clickable "Turn N" quote links, pending/empty/populated states, provenance + dropped-quote footer) wired into `HistorianReportView`'s Physician Report tab and a new `differential` sub-tab on `HistorianSessionPanel`; `HistorianTranscriptViewer` gained an additive `highlightIndex` prop so a clicked quote jumps the transcript viewer to that turn. Interim grading matcher `tests/historian-eval/ddxMatch.ts` (`tokenSetMatch` — casefold, hyphen/punctuation-as-word-boundary, negation-safe, single-token specificity floor) was built for this task's own live gate only; Task 4's ICD-10/adjudicated `scoreAgainstGroundTruth` (`agreement.ts`) is the authoritative matcher for release-gate purposes — cross-reference comments added 2026-07-21 in both files so the two non-comparable numbers are never confused. **Live gate** (`HISTORIAN_EVAL_LIVE=1`, real Bedrock + `neuro_plans`): first attempt 0/5 (every persona hard-failed on `max_tokens` — `FINAL_DDX_MAX_TOKENS` 2500→4000 and `SYMPTOM_EXTRACTION_MAX_TOKENS` 700→1200 fixed a mechanical token-budget bug, not a clinical one); post-fix runs landed 3/5 then 4/5 on IDENTICAL code (`peripheral-neuropathy.json` flipped MISS→HIT between runs — Bedrock isn't perfectly deterministic even at temperature 0); `first-seizure.json` missed both runs on a matcher limitation (hyphen/word-order defeating plain substring match against an otherwise clinically-correct "New-Onset Epilepsy" pick) — left unpatched rather than tuned post-hoc to flip the gate. Two same-day review-round fixes (`90cda13`, `96973e6`): live-gate matcher correctness (negation guard + subset-match direction) and symmetric provenance, then a single-token specificity floor (a bare "TIA"/"CIDP" ground-truth entry no longer trivially subset-matches an unrelated multi-word diagnosis that merely contains that word). Verified: tsc clean; full vitest suite 2,365 passed / 1 pre-existing unrelated failure (`simulated-patients/runner.test.ts`, live-dev-server gap predating this task); lint clean on every new/touched file. Not yet merged; migration 057 not yet applied to RDS. Commit `7c6e465` (feature), review-round fixes `90cda13`/`96973e6`.

- **Historian Validation Suite Task 1: durable transcript event log (2026-07-20)**: First task of a 6-phase sprint making AI Historian sessions auditable (branch `feature/historian-validation-suite`). Today a crashed tab or dropped WebRTC/WS transport before the interview reaches a clean end silently loses the whole conversation — `/save` only writes `historian_sessions.transcript` at the very end. New durable, append-only `historian_transcript_events` table (migration 056 + paired `.down.sql` — **not applied**, a later rollout task applies it) lets the client flush small batches continuously. **Server-minted session id**: `/session` (both Nova and OpenAI branches) now mints `crypto.randomUUID()` up front and returns it as `sessionId` alongside a new HMAC `flushToken` (`src/lib/historian/flushToken.ts`, mirrors `mintNovaRelayToken`'s pattern, env `HISTORIAN_FLUSH_SECRET` falling back to `NOVA_RELAY_SHARED_SECRET`); `/save` now accepts an optional `sessionId` and, when present, inserts it as the row's explicit `id` instead of leaving it to the column's `gen_random_uuid()` default — so the id transcript-flush events were keyed by matches the eventual `historian_sessions.id`. The OpenAI branch's old (dead — no client or test read it) `sessionId` field, which actually held OpenAI's own Realtime session id, was renamed to `providerSessionId` to free the name up; purely additive to the client contract otherwise. **Client flush** (`useRealtimeSession.ts`): transcript entries get a monotonic `seq` at append time, buffer and flush every 3 unflushed entries, on a provider transport error, and once more on `pagehide` via a **`fetch(..., {keepalive:true})`** (not `navigator.sendBeacon`, which can't set the required `Authorization` header) — all fire-and-forget, never blocking the UI; failures leave the batch pending for the next trigger (fail-open). **Flush endpoint** (`POST /api/ai/historian/transcript-flush`): bearer-token auth only (patient-portal bar, same as `/save`/`/session`), 403 on a missing/invalid token or a token/body sessionId mismatch, ≤50 entries/request and a 500-seq/session cap (413 beyond either), idempotent insert (`ON CONFLICT (session_id, seq) DO NOTHING`) so retries and the keepalive send can't duplicate rows — never echoes entry text in any response or log. `/save` also runs a new non-destructive integrity cross-check (`validateTranscript` — monotonic seq/timestamp, duplicate-seq detection, known roles, non-empty text) and logs an event-count-vs-transcript-length mismatch, both server-side and count/structure-only — patient utterance text is never logged. New collapsible `HistorianTranscriptViewer` (role chip, mm:ss offset, text) replaces the inline transcript block in `HistorianSessionPanel` and fills a prior gap in `HistorianReportView`'s physician tab (had no transcript display at all). TDD for `transcriptIntegrity`/`flushToken` (RED confirmed — both suites failed on missing modules — then GREEN). Verified: tsc clean, full vitest suite 2,285 passed (only the pre-existing `simulated-patients/runner.test.ts`, which requires a live dev server, doesn't run here), lint clean on every new file. Exports later sprint tasks depend on: `validateTranscript`, `mintFlushToken`/`verifyFlushToken`, the `sessionId`-before-save flow. Not yet merged; migration not yet applied to RDS.

- **Gateway status/cluster under-triage fix: recurrence-status and seizure clusters now fire `emergency_now` (2026-07-20)**: While building the over-fire non-regression battery (below) the `status_or_recurrent_seizure` rule was found to MISS two textbook seizure emergencies — the dangerous (under-triage) direction. (1) **Status by recurrence** phrased "seizures without **full/complete** recovery between events": a qualifier like "full" broke the without-recovery adjacency in `SEIZURE_EMERGENCY` (`(?:without|no) (?:recovery|...)` required the words adjacent). Broadened to `(?:without|no|not) (?:full(?:y)? |complete(?:ly)? |any )?(?:recovery|recovering|regaining consciousness|returning to baseline|waking)` with a slightly wider `.{0,60}` window. (2) **Acute repetitive seizures / seizure cluster** phrasing was absent from `RECURRENT_SEIZURE` entirely — added `multiple seizures`, `seizure cluster`, `cluster of <type> seizures` (hyphen-tolerant `[\w-]+` so "cluster of tonic-clonic seizures" matches), and `acute repetitive seizures`. **Over-triage-safe:** the cluster/multiple terms stay on the recurrent path, gated by `!STABLE_SEIZURE_CONTEXT` **and** an acute/uncertain marker in the **same segment** (the gateway evaluates rules per-segment), so a well-controlled history, an at-baseline chronic-epilepsy line, a "seizure-free for 5 years" note, or a seizure-cluster PMH line in a **separate** sentence from an unrelated acute complaint still does NOT fire (all six verified as non-firing). Same hyphen fix applied to the `INCOMPLETE_RECOVERY_OR_ONGOING` cluster term in the over-fire guard. Grounded in the AES acute-repetitive-seizure / status-epilepticus definitions. 15 new regression cases in `tests/triage/emergencyGateway.test.ts` (9 must-fire status/cluster + 6 must-not-fire stable/historical/PMH). Verified: tsc + lint clean; full triage suite 1,789 passed / 0 failed; offline sentinel all 5 gates PASS (`zero-emergency-under-triage` = 0, **`hard-negative-false-alert-rate` = 0** — no new false alerts, `releaseGateEligible: true`). Auto-pushed (under-triage-fixing / over-triage-safe direction; Steve pre-approved).
- **Gateway over-fire guards: subacute/resolved presentations no longer forced to `emergency_now` (2026-07-20)**: The engine cross-check from the 7/19 demo re-sync flagged the deterministic emergency gateway over-firing `emergency_now` on four real referral notes whose acute lexicon appears only in a non-acute context — Hargrove (foot drop over weeks reading as acute stroke via `acute_cerebrovascular` + a mechanical-fall `traumatic_neurologic_deterioration`), Williams & Reyes (first seizures that returned to baseline, firing `altered_mental_status_or_coma`), Vasquez (chronic/episodic psychogenic spells firing `acute_spinal_cord_or_cauda_equina`). Fix is a **note-level post-filter** `applyOverTriageGuards()` in `emergencyGateway.ts` (runs after per-segment signal collection, because the deferral context — subacute timeframe, resolved-ictal baseline, chronic-episodic frame — usually lives in a different sentence than the firing segment). Four guards, each suppressing ONE syndrome only when the whole note is unambiguously non-acute: `isSubacuteIsolatedFocalWeakness` (subacute timeframe + no genuinely-acute onset/worsening + no cortical stroke feature + no stroke/TIA dx), `isSubacuteFocalWeaknessWithoutTraumaEmergency` (+ no LOC/AMS and not anticoag∧head-trauma), `isResolvedIctalUnresponsiveness` (seizure + ictal context + returned-to-baseline + not status/>5min/incomplete-recovery/high-risk), `isChronicEpisodicLegComplaint` (bilateral-leg + chronic-episodic + no cauda/bladder/saddle/severe-back-pain/acute-escalation). **Strictly over-triage-safe:** every genuinely-acute onset marker, residual deficit, or red flag keeps the floor; truly recurrent seizures still fire via the separate `RECURRENT_SEIZURE → status_or_recurrent_seizure` rule. Two lexical false-positives found and fixed along the way: bare `today` (every note says "presents today") tightened to onset/worsening adjacency in `GENUINELY_ACUTE_ONSET`; bare `trauma`/`recurrent` false-matching "Level II **Trauma** Center" (ED letterhead) and "return to ED for any **recurrent** episode" (discharge precaution) — narrowed `SEIZURE_RESOLVED_BLOCKER` to genuine head-injury/anticoag/pregnancy/persistent-deficit and `INCOMPLETE_RECOVERY_OR_ONGOING`'s ongoing/recurrent/cluster to require an adjacent seizure noun. Grounded in the AAN/AES + ACEP first-seizure guideline (a first unprovoked seizure returned to baseline need not be emergent) and the stroke-mimics-radiculopathy literature. Deferred notes still carry their raw `lexicalHits` for audit. New regression test `tests/triage/emergencyGatewayOverfire.test.ts` asserts on the **real served PDFs** via the production parser (4 defer) + a 10-case emergent non-regression floor (acute stroke, SAH, status epilepticus, first-seizure-not-back-to-baseline, anticoag head trauma, cauda equina, meningitis, cord compression, seizure >5 min, found-down AMS). Verified: tsc + lint clean; full triage suite 1,774 passed / 0 failed; offline sentinel all 5 release gates PASS (`zero-emergency-under-triage` = 0 observed, `releaseGateEligible: true`). **Pre-existing under-triage gap noted, NOT changed here (opposite direction, needs its own battery):** the `status_or_recurrent_seizure` rule misses "recurrent seizures without recovery between events" (textbook status) and bare "seizure cluster" when phrased without an acute marker — flagged for Steve. **Held for Steve's sign-off before push** — a clinical safety-net narrowing, unlike an auto-pushable code fix.
- **Demo referral library re-synced to its served PDFs (2026-07-19)**: Steve reported a triage demo where the note he uploaded "wasn't the note that was reviewed." Root cause: `scripts/regenerate-demo-scenarios.ts` (a one-shot 7/10 migration) scrambled `demoScenarios.ts` note-derived fields (previewText via a REPAIR map + hardcoded Patterson→TIA) to match a "corrected validation_cases DB" but **never regenerated the served PDFs** — so ~15 of 26 demo cards described a different patient/condition than the PDF they actually load and triage (e.g. cross-10 card said "diabetic neuropathy/routine" while its real note is CNS vasculitis/high-priority; 07-kowalski said "headache/IIH" while its note is suspected MS with a STAT MRI). **The triage pipeline was never affected** — real uploads and demo "Load into Triage" both fetch + extract the real PDF bytes (`DemoScenarioLoader` L90-101); only the preview cards were wrong. Fix: regenerated **every** card's note-derived fields (previewText, patientName, age, sex, referringSpecialty, briefDescription, clinicalHighlight, demoPoints, expectedTier) directly from the real served PDFs (the single source of truth) via `pdftotext`; preserved id/category/docType/filename/path and the `DEMO_CATEGORIES` tail. `expectedTier` values finalized 2026-07-19 to Steve's own clinical calls (DO sign-off): 21 of 26 already matched the rubric; 5 upgraded to `emergent` (Washington TIA, Fletcher, Wei SAH, Donnelly, Jimenez — the acute/ED-routed cases). Engine cross-check (26 notes run through the real gateway+safety+scorer+fusion+adjudicator offline) surfaced two real findings, both being addressed separately: (a) the deterministic emergency gateway over-fires `emergency_now` on subacute notes (Hargrove back pain, Williams/Reyes resolved first-seizure, Vasquez psychogenic spells) — lexical patterns catching acute words in non-acute context; (b) the outpatient scorer's text-JSON path fails strict `JSON.parse` on a large fraction of notes (the false-holds root cause re-confirmed) → move to strict tool-output path. Demo cards teach the clinically-correct tier (Steve's call); engine miscalibration is a separate fix, not baked into the cards. Retired `scripts/regenerate-demo-scenarios.ts` to an abort-tombstone (its guard keyed on "Patterson===TIA" no longer holds → re-running would re-scramble). Verified: tsc + lint clean, 26/26 cards' preview contains their real patient. **Open follow-up:** `/api/triage/validate/cases/seed` seeds validation cases FROM previewText, so the Independent-Reviewers study was built on the scrambled text and needs re-alignment to the corrected notes.
- **Historian localizer: plan-grounded evidence + care-team suggestions + prior-studies capture (2026-07-19)**: Three linked changes after finding the localizer's Bedrock KB (`BEDROCK_KB_ID=T4W8S8RNMN`) was DELETED in prod (every run logged ResourceNotFoundException and shipped ungrounded differentials silently). (1) **Grounding moved off Bedrock KB onto the `neuro_plans` database** — the account SCP blocks `bedrock:CreateKnowledgeBase` even for admin, so instead of a KB the localizer now queries the 487-plan `neuro_plans` DB (a dedicated database on the same RDS instance; normalized `plans`/`plan_differential`/`plan_evidence`/`plan_icd10` — NOT `ops_amplehtml.clinical_plans`, which is a stale 124-row partial copy). New `getNeuroPlansPool()` in `db.ts` mirrors `getWearablePool()` (same creds, `database:'neuro_plans'`). New `src/lib/consult/planEvidence.ts` `retrievePlanEvidence()` ranks plans by ILIKE term-match (symptoms/red-flags/chief-complaint vs plan title + ICD-10 description + differential diagnosis/features; `status='active'`, fully parameterized), pulls the top ~3 plans' evidence + differential into the guideline text + citations fed to Step 3. Localizer Step 2 no longer reads `BEDROCK_KB_ID` at all; empty match → existing amber degraded notice. Live-verified: thunderclap headache → stroke/meningitis/headache-eval; ascending weakness → neuropathy/myelopathy; evidence rows are Class/Level-graded and actionable. (2) **Care-team "Suggested for Care Team" section** in `LocalizerPanel` (physician-only, never pushed to the patient-facing agent) — up to 4 plan-grounded next-step actions (studies/labs/referrals/monitoring) with per-action rationale + source-plan chip, "not orders" microcopy, NO drug dosages. (3) **Prior-studies interviewing** — `save_interview_output` schema + CRITICAL RULE 14 have the historian ask which studies the patient has had (modality/where/when/result), captured in `prior_studies`; the patient-facing agent NEVER advises which studies are needed — gaps surface only on the physician side (`IntakeReviewSection` "Prior Studies" card with a "not yet obtained" tag). Orphaned KB build artifacts (S3 corpus bucket, S3 Vectors index, IAM role) were created then torn down when the DB path was chosen. Open follow-up: the historian `query_evidence` tool (`evidence-query/route.ts`) still points at the dead KB id — could reuse `retrievePlanEvidence`. tsc/lint clean; planEvidence 7/7 + full suite green.
- **Triage false-holds fix: validator/prompt contradiction + scoring retry (2026-07-19)**: Steve's live runs put 2/2 referrals into "Human review required — scoring blocked" holds. Root causes (both reproduced against live Bedrock with the exact prod referral texts): (1) **Deterministic prompt/validator contradiction** — the safety-extraction prompt says non-time-sensitive critical unknowns may accompany `no_time_critical_signal`, but `validateModelSafetyExtraction` demanded `same_day_clinician_review` whenever ANY unknown was listed and threw on mismatch, discarding the whole safety branch → fusion `undetermined` → mandatory hold (the Gutierrez sample listed "MRI pending"-class unknowns and died exactly here; also affected quiet-but-`partial`-data outputs, which were forced to `undetermined` = treated as branch failure). The adjudicator can never rescue these — `applyAdjudicatorDecision` only accepts more-conservative pathways by design. Fix: the validator now **derives a floor from the model's own validated signals** (any `emergency_now` signal ⇒ `emergency_now`; else any `immediate_clinician_review` signal ⇒ at least `same_day_clinician_review`) and never downgrades the model's stated pathway; benign unknowns and data_quality no longer force escalation or kill the branch. An understated pathway is now FLOORED (previously the branch died into a generic hold — flooring turns a understated emergency into an actual emergency escalation, strictly safer). Structural checks (enums, exact-quote grounding, emergency-evidence requirements) unchanged; prompt gained a matching consistency rule. (2) **Intermittent scoring flake** — the outpatient scorer still uses the parse-JSON-from-text path (not the strict tool path) and occasionally emits unparseable output (`ClinicalModelOutputError`, Sandoval sample; reran clean); a single hold-producing flake now gets one retry (timeouts NOT retried — the deadline is the full branch budget). Clinical delta, prominently: **benign, non-time-sensitive unknowns no longer force a same-day hold** — this restores the 7/12 prompt's own stated intent; no tier definitions changed. Tests: contradiction cases inverted to floor semantics, 3 new floor tests; triage suite 1,760 passed; tsc/lint clean; offline sentinel `releaseGateEligible: true`; live sentinel + Bedrock cross-family review run same-session (results in commit/PR notes). Migration follow-up from same session: `clinical_access_memberships` was EMPTY in prod (migration 048 created it, nothing seeds it) → every user got 403 "Access denied" on all `/api/triage/*` routes; provisioned both steve@sevaro.com Cognito subs as `clinician` (tenant `default`) manually. Remaining gap: no provisioning path exists (no seed/admin UI) — teammates hit the same 403 until provisioned. UX addendum: all 13 triage-route denial responses now return reason-specific messages via `clinicalAccessDeniedMessage()` (401 "Please sign in to continue." / 403 "not provisioned… ask an administrator" / 503 retry) instead of one ambiguous "Access denied" — a signed-out browser profile previously looked identical to a provisioning failure. Same-day follow-up: `LocalizerPanel` now renders an amber partial-analysis notice when the localizer API returns `partial`/`degradedReason` (previously computed but consumed nowhere) — prompted by the discovery that prod's `BEDROCK_KB_ID` (T4W8S8RNMN) points at a DELETED knowledge base, so every localizer run silently lacked guideline citations; the only surviving KB (`ZHG5JIHX1U` sevaro-operations-kb, hospital ops protocols) is the WRONG corpus to repoint to — clinical KB rebuild is an open decision.
- **Referral triage safety architecture (2026-07-12)**: Landed the curated triage rebuild (branch `feat/neurology-triage-safety-architecture`, distilled from the Sol working tree; full uncurated state preserved on `sol/full-wip-snapshot-2026-07-12`). Deterministic scoring floors over the weighted average (any critical dimension=5 or red_flag>=4 → urgent; acuity/concern>=4 → semi_urgent) with missingness handled as data quality, never diluting urgency; strict Bedrock tool-output schema validation failing closed into governed holds; deterministic emergency review gateway with lexical backstop; canonical extraction-first intake (pasted text and PDF share one source-bound path with per-page provenance — replaces the silent 50k-char truncation); durable emergency-action workflows + alert outbox with delivery workers (migrations 048, 050–055 — **not yet applied**, manual psql per repo convention); clinician authorization on triage routes; referral-clarification prompt mode locked to clinician-approved question IDs. ~1,500 triage tests + sentinel release-gate harness (26/26 on the 2026-07-11 live run). Deliberately NOT included (preserved on the snapshot branch for separate review): app-wide API auth hardening, middleware Cognito audience validation, next.config secrets removal, nova-relay changes, patient-access capability system. Classifier model stays Sonnet 4.6 pending a sentinel bake-off before the Sonnet 5 swap.
- **Clara red-team + Gate 0 airway fix (2026-07-12)**: Adversarial steel-man of the LIVE classifier — 74 crafted transcripts across 6 attacker lenses fired at the deployed `/api/ai/clara/classify`, then independent clinical judging of every self-flagged under-triage (workflow-driven). **Manipulation surface held completely: 0 under-triages across 36 injection / fake-authority / downplay / ASR-homophone cases.** The weakness is lexicon coverage — hard neuro-emergency signs absent from Gate 0's banks bypass the deterministic floor and rely on the LLM. **S0 fixed + verified live (b8a9167):** a postictal patient "not protecting his airway", unrousable, with a "cluster of seizures" routed as a routine `ceribell-eeg` because no bank fired (seizure bank missed "cluster of seizures"; acute_emergency bank had no airway/apnea/rouse patterns). Added airway-compromise / apneic / stopped-breathing / desaturating / cyanotic / cannot-rouse / unrousable / can't-wake-up to the acute_emergency bank, "cluster of seizures"/"seizure cluster" to the seizure bank, and synced the `ACTIVE_SEIZURE_EMERGENCY` Ceribell-deferral guard to the same terms (so an airway sign without the bare word "airway" can't defer). Over-triage direction, NO tier definitions changed; 56 gate tests (8 new), re-fired the confirmed + 4 variant transcripts against the deployed classifier → all hard-fire emergent. Ported to production (sevaro-voice-agent PR #39, 94/94 gate tests). **3 under-triages reported for Steve's clinical sign-off (NOT unilaterally changed — tier decisions):** Wernicke's triad (confusion+ataxia+ophthalmoplegia) → non-emergent, NCSE-by-automatisms → eeg-read, acute cord compression (bladder+motor, "usual MS" framing) → STAT 1 (which the rulebook deliberately assigns). Full report: `qa/STEEL-MAN-2026-07-12.md`. S4 observation: non-English stroke terms + vitals-only emergencies (Cushing triad, anisocoria) are LLM-only with no Gate-0 backstop — scope separately.
- **Clara: EEG callback matches the real notification system (2026-07-12)**: Steve clarified how callbacks actually work — the physician is notified AUTOMATICALLY that a call/study is waiting; they answer or it lands in their visual voicemail, and a callback request travels WITH the notification as a message carrying the caller's number. There is no Sevaro callback number to hand out, so the old "say it will be provided" placeholder (and the long-open "need the real call-back number from Steve" item) modeled a problem that doesn't exist — both retired. `CLARA_VOICE_INSTRUCTIONS` EEG exception rewritten: doctor is being notified now → if they can't pick up, Clara attaches a call-me-back message → takes the CALLER's number → reminds them they can reach us again on this same line; NEVER invents/states any Sevaro number. Also confirmed by Steve: the dispatch layer ("goes to") is deliberately the NEXT tier — current goal is validating that Clara runs the conversation/classification correctly; and facility STAT coverage will be determined by contractual obligations + the inbound one-call number (spec input for the pending per-hospital coverage config). Prompt-only; tsc clean.
- **Clara: STAT 2 vs plain non-emergent discriminator (2026-07-12)**: Steve's clinical direction on the subacute/non-emergent bucket — the case rules' "(STAT 2)" annotations are the DEFAULT, adjusted by context: patient in the ER or provider asking for an "urgent" consult/callback → statLevel 2 (timed ≤60-min callback so the team can disposition out of the ER; most STAT 1/2 originate in the ER but inpatients can be STAT too); caller explicitly framing it non-emergent/routine or a stable floor patient with no urgency ask → statLevel null → the NON-EMERGENT PROVIDER (routine queue, no STAT SLA); neither signal → keep the default STAT 2 (never invent urgency, never strip expressed urgency). Changes: discriminator block in `claraRulebook.ts` under the STAT definitions; explicit `NON_EMERGENT + statLevel null` branch in `buildRouting()` ("non-emergent provider, routine queue" — previously fell through to a generic workflow label) + matching `describeRoutingTarget()` case; one-question disambiguator in `CLARA_VOICE_INSTRUCTIONS` ("Is the patient in the ER — and does the team need an urgent callback, or is this routine?", only when not already stated). No tier/threshold definitions changed — this refines statLevel selection WITHIN non-emergent per Steve's stated rules. STILL OPEN (Steve: "we still have to figure how to define those better"): crisper STAT 1 vs STAT 2 definitions, and the facility-dependent piece (whether a hospital even contracts a STAT 2 tier) which lands with the per-hospital coverage config. **SSOT: sevaro-voice-agent getSystemPrompt() needs the same discriminator block** (sync after PR #38 lands to avoid churning that open safety PR). tsc clean; clara tests 48/48; full vitest 206 passed/5 failed (same pre-existing).
- **Clara Gate 0: subacute-stroke deferral (2026-07-12)**: Sam's live finding — "LKW two days ago" with a completed left-MCA stroke still escalated as a code stroke. Root cause was TWO Gate-0 defects compounding: (1) `slurred speech` (stroke bank) fired the deterministic floor on an explicitly two-day presentation, pre-empting the rulebook whose signed-off stroke-timing rule already says >24h → NON_EMERGENT STAT 2 (≤60-min callback); (2) `hemorrhage` in the acute_emergency bank matched the NEGATED radiology idiom "with **no** hemorrhage". Fix mirrors the approved Ceribell/EEG-read seizure deferral: new pure `isSubacuteStrokeReport()` in `redFlagGate.ts` — a stroke-bank hit DEFERS to the rulebook only when the presentation is explicitly multi-day (≥2 days/weeks/months phrasing), with NO acute/worsening language ("code stroke"/"stroke alert", sudden/acute, worse/progressing, today/this morning, hour/minute timeframes, wake-up stroke, tPA/thrombectomy talk all keep the floor), and NO hit from any other bank after stripping negated "no/without hemorrhage|bleeding". Unknown timing, "yesterday", self-harm/seizure/thunderclap/acute-emergency hits always keep the floor — fail-safe direction preserved; no tier definitions changed (the rulebook decides the tier). Wired into `classify/route.ts` parallel to the Ceribell branch with `gate0.deescalated` + reason. 20 new unit tests incl. Sam's verbatim session transcript (defers) and the same-day EMS code-stroke transcript (keeps floor); redFlagGate suite 48/48, tsc clean, full vitest 206 passed/5 failed (same pre-existing). **SSOT note: sevaro-voice-agent's production Gate-0 needs this same policy layer.** Sam's other observation confirmed working as designed: Gate 0 resists "not emergent" downplay on acute stroke/status.
- **Clara: missing identifiers never block the connection (2026-07-12)**: From Sam's live test — he didn't have the patient's age and Clara refused to move forward. Clinically wrong: unknown identifiers are routine in hospitals, especially hyperacute cases. New "IDENTIFIERS NEVER BLOCK THE CONNECTION" section in `CLARA_VOICE_INSTRUCTIONS` (session/route.ts): ask each identifier ONCE; "I don't know" is accepted instantly and Clara moves on; no age/DOB → fine; no MRN/FIN → fine; no NAME → John/Jane Doe situation, proceed as normal; never stall/refuse/circle back on a missing item as a condition of connecting. Cross-referenced from the generic identifier-ask line ("helpful, not required") and reinforced in EMERGENT FLOW (hyperacute patients are often John/Jane Does — never slows the connection). Prompt-only, no routing/tier change; tsc clean. Same test also gave a cross-platform audio data point: **Samsung phone = zero static/crackle** on the same Clara call flow, further corroborating that the residual snapping is an iOS-dev-beta-27 output artifact, not an app bug (see crackle verdict below).
- **Clara rounding: group-census vs specific-patient branch (2026-07-12)**: From Steve's live clerical-caller test — he said "schedule rounding on a group of patients" and Clara still reflexively asked for a single patient's name/DOB/age/MRN (the generic HOW-TO-ROUTE identifier ask). New ROUNDING section in `CLARA_VOICE_INSTRUCTIONS` (session/route.ts): rounding comes in two shapes and Clara must find out which BEFORE asking for identifiers — if unclear, ask exactly one question ("your whole list there, or specific patients?"). GROUP/census round → NO per-patient identifiers (the rounding doctor goes over the list with the team on connect), just confirm facility/unit and connect; SPECIFIC patient(s) → collect name + MRN/FIN + location per patient. Same tier and destination either way (ROUNDING → MD2) — the branch changes only what's collected, never the routing; no threshold/tier change. Also cross-referenced the exception from the generic identifier-ask line. Prompt-only; tsc clean. Same session: **iOS crackle verdict from live data** — Steve's 46s rounding call on iOS dev beta 27 logged `underruns 3 / primes 3 / maxQueued ~8s @48kHz` (clara_test_sessions.metadata, instrumentation from f4f96ec): underruns exactly match the natural end-of-turn drains and the queue peaked at ~8s buffered — the playback queue NEVER starved mid-speech while snaps persisted → underrun hypothesis DEAD; the crackle is an iOS-dev-beta output-layer artifact below the Web Audio API (worklet renders clean; prod = Twilio, not WebAudio). Decision: accept as beta-only, demo on Mac/non-beta device, re-check at iOS 27 GA — no further app-side audio fixes. Also confirmed on this real prod call: zero duplicate transcript lines (relay stage-filter deployed same day). `/rnd/clara` has an iOS-beta-only audio "crackle" bug; 6 fix attempts have failed and the current underrun-hypothesis (worklet queue underruns → declick churn) had zero supporting data. This change adds counters only — no playback timing, priming, declick, or resampling behavior changed. `pcmStreamWorklet.ts`: new `StreamStats` (`underruns`/`primes`/`pushes`/`samplesIn`/`blocksRendered`/`maxQueued`) on `StreamState`, incremented in `renderBlock` (mirrored identically in the inline `PCM_STREAM_WORKLET_SRC` worklet string per the file's pure-code/string-must-stay-algorithm-identical convention); the worklet's port `push` handler additionally tracks `pushes`/`samplesIn`/`maxQueued` (no port on the pure side); new `getStats` port message and a `stats` payload attached to the existing `drained` message; `clear` (barge-in) does NOT reset stats — they're cumulative per-session. `player.ts`: new `PcmPlayer.getDiagnostics()` — round-trips a `getStats` request with a 300ms timeout falling back to the last-seen stats, never throws, always resolves in ≤~350ms; works across both the worklet and BufferSource fallback paths. `providerTypes.ts` gained an optional `VoiceProvider.getAudioDiagnostics?()` (OpenAI provider untouched — the method is optional). `novaSonicWsProvider.ts` implements it and stashes a diagnostics snapshot in `stop()` *before* `player.close()` so diagnostics survive teardown. `useClaraVoiceSession.ts`'s `endSession()` captures diagnostics before `provider.stop()`, exposes new `audioDiagnostics` state (cleared in `startSession`/`resetSession`), and includes it in the `POST /api/ai/clara/log` body as `metadata: { audioDiagnostics }` (the route already accepts an arbitrary `metadata` field into a JSONB column — verified via `route.ts`, no route/migration change needed). `ClaraVoiceTestView.tsx` renders one small muted debug line in the post-call results section when diagnostics are present, e.g. `Audio: worklet · 48000 Hz · underruns 3 · primes 5 · maxQ 61,440` (fields absent are omitted gracefully). 4 new unit tests added to `pcmStreamWorklet.test.ts` (prime/underrun counting, not-yet-primed early return excluded, blocksRendered, contiguous-join non-underrun); `pushes`/`samplesIn`/`maxQueued` and the `clear`-preserves-stats behavior are worklet-string-only (no port on the pure side) and intentionally not unit tested. `npx tsc --noEmit` clean; `npx vitest run` unchanged at 188 passed/5 failed/30 skipped (same pre-existing unrelated failures — VAD eagerness defaults ×3, triage red-flag scoring, PDF-parse mock — plus the simulated-patients runner file error), all 8 worklet tests (4 original + 4 new) green; `npm run lint` clean; `npm run build` clean. Purpose: the next on-device iOS test now produces hard numbers (underrun count, max queue depth, prime count) to decide the underrun-hypothesis vs an iOS-beta-only rendering artifact with data instead of a 7th guess.
- **Duplicate transcript ROOT CAUSE found + fixed at the relay (2026-07-11)**: The stubborn duplicate-transcript bug on `/rnd/clara` (and, unnoticed, on the Nova historian) was **not** a client dedup problem — it's the relay double-forwarding assistant text. Verified against a LIVE Nova Sonic stream (new `RELAY_TRACE_RAW=1` raw-event trace + new `services/nova-sonic-relay/scripts/frame-probe.mjs`, which mints the HMAC subprotocol token, streams real synthesized speech WAVs at mic pace, and logs every frame): Nova Sonic emits every ASSISTANT turn's text **twice** — `generationStage: SPECULATIVE` (on `contentStart`) as the audio starts, then a byte-identical `FINAL` copy when the turn ends. `novaSonicSession.ts` ignored `contentStart` entirely and forwarded every `textOutput`, so both copies reached the browser. The FINAL copy lands **after the audio finishes** — beyond the client's 8s dedup window on any reply longer than ~8s of speech, which is exactly why both shipped dedup attempts (3302377, d5be336) still duplicated on live calls. USER (ASR) text arrives once per segment, always FINAL — never duplicated. **Relay fix**: track `contentStart.additionalModelFields.generationStage` per TEXT contentId; forward USER text only at FINAL, ASSISTANT text only at SPECULATIVE (the early copy — UI keeps showing text as audio starts), fail OPEN on unknown stage (better a dup than dropped content); barge-in `{"interrupted":true}` routing unchanged; map cleaned on contentEnd. Pure `shouldForwardText()`/`parseGenerationStage()` + 11 unit tests built from the captured live event shapes (relay: 17 tests green, tsc clean). Re-ran the identical probe against the fixed relay: each assistant turn forwarded exactly once (raw log confirms FINALs seen and dropped). **Client backstops** (prod relay is stale until redeployed — relay is a standalone ECS service, NOT Amplify): `useClaraVoiceSession.ts` and `useRealtimeSession.ts` now also skip an assistant transcript identical to the *most recent* assistant turn with **no time window** (the windowed check stays for user turns). Historian bonus fix: the duplicate was also double-incrementing `questionCountRef` per Nova reply, burning the interview turn budget at 2x. Root: tsc clean, build clean, vitest 184 passed/5 failed (same pre-existing unrelated failures). **Relay redeploy required** for the root fix to reach prod.
- **Clara demo loop: repeatable multi-scenario + preset scenarios + mobile-first (2026-07-11)**: Turned the `/rnd/clara` browser voice test into a smooth, repeatable, mobile-first demo loop so Steve can send a link to a group and they can run multiple synthetic scenarios back-to-back, rate each, and generate eval data — no code changes to the classify/log/feedback pipeline, purely UI/flow. **Repeatable loop**: new `resetSession()` in `useClaraVoiceSession.ts` — a pure state reset (turns/transcript/lastClassification/loggedSessionId back to idle) distinct from `startSession()`'s own reset, so a finished call's results can be cleared without immediately reopening the mic. `ClaraVoiceTestView` shows a prominent **"Test another scenario"** button (both inline atop the results section and as the primary CTA in a new sticky bottom action bar) once a call finishes and its decision card(s) + 👍/👎 render; tapping it clears the finished call and returns to the cold-start chip view. Every call already produced its own `clara_test_sessions` row (each `startSession()` resets `turnsRef`/`turns` and each `endSession()` POSTs a fresh `/api/ai/clara/log` row — never an update) — the reset button doesn't change that, it just cleans up the UI between calls so nothing overwrites the prior session. **One-tap preset scenarios**: new `src/lib/clara/presetScenarios.ts` — 7 inpatient/facility teleneurology consult scripts (this line is consult-intake, not outpatient scheduling): Stroke alert, Status epilepticus, Non-emergent consult, New consult, Rounding, EEG read, Ceribell/rapid EEG, each a short synthetic physician-caller line mapping to Clara's real taxonomy (EMERGENT/NON_EMERGENT/ROUNDING/EEG_READ/CERIBELL_EEG). Tapping a chip (shown only in the idle/no-turns "cold start" state) sets a display-only `activeScenario` and calls `startSession()` — Clara still classifies live via the real `/api/ai/clara/classify` pipeline; nothing bypasses Gate 0 or the classifier. While a call with an active scenario is connecting/active/ending, a purple banner stays pinned above the transcript showing the scenario label + full script so the tester can read it aloud; tapping the freeform mic button instead clears `activeScenario`. **Mobile-first**: preset chips render in a responsive `auto-fill minmax(150px,1fr)` CSS grid (2 columns at 375px, more on wider screens, no media queries needed) with ≥44px tap targets; the control bar is a `position: fixed` sticky bottom bar (safe-area-inset-aware) that is always thumb-reachable and shows exactly one primary control at a time (Start / Stop / Test another). `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` clean (`/rnd/clara` in the manifest, no new routes); `npx vitest run` unchanged at 176 passed/5 failed/30 skipped (same pre-existing unrelated failures). Verified live in dev at 375px: password gate works, chips render 2-up and show the correct script + label on tap, `POST /api/ai/clara/session` is hit and returns 200, the missing-local-relay case (`NOVA_SONIC_RELAY_URL` unset) fails gracefully into the existing error banner (mic stays retryable, no crash), and tapping the freeform mic after an error correctly clears the scenario banner. **Not verifiable headlessly**: a full mic→Clara→classify→log→feedback round trip needs a live Nova Sonic relay + real mic input (same limitation as the rest of `/rnd/clara`'s voice loop) — the reset-to-fresh-session and decision-card-then-"Test another" behavior is implemented and code-reviewed but needs an on-device pass with the real relay to see rendered end-to-end. No PHI — synthetic test data only; same feature flag/password gate as the rest of `/rnd/clara`.
- **Clara results + feedback page (2026-07-11)**: Added a post-call results/review layer on top of the `/rnd/clara` browser voice test harness (PR #155) so triage decisions can be rated and turned into labeled eval data. New migration `migrations/049_clara_test_feedback.sql` — `clara_test_feedback` table (session_id FK → `clara_test_sessions`, turn_index, decision snapshot, `verdict` up/down, `reason`, optional `corrected_consult_type`); **not applied**, per repo convention (manual `psql` run). New `POST/GET /api/ai/clara/feedback` (`src/app/api/ai/clara/feedback/route.ts`) — same testGate cookie re-verification pattern as the other `/api/ai/clara/*` routes; detects Postgres `42P01`/"relation does not exist" and returns a clear "apply migration 049" 503 instead of a raw DB error. `GET /api/ai/clara/log` added (session list, same pending-migration handling for 048) to power the new review page. New `ClaraDecisionCard` (`src/components/clara/ClaraDecisionCard.tsx`) renders one decision — urgency/confidence/rationale/red flags/Gate-0, plus a human routing target via new `describeRoutingTarget()` (`src/lib/clara/routingDisplay.ts`, e.g. emergent → "MD1 / on-call neurologist — URGENT transfer") — with inline 👍/👎 + reason + optional corrected-consultType feedback form (confirm-on-submit). `ClaraVoiceTestView` now shows a post-call "Call results" section (one card per classified turn) once `useClaraVoiceSession`'s new `loggedSessionId` resolves from the `/log` POST response — feedback is disabled until the call has actually been logged (FK requires a real session row). New review page `src/app/rnd/clara/results/page.tsx` (`ClaraResultsHistoryView.tsx`, same `ClaraPasswordGate`) lists past sessions with all decisions + any feedback, expandable, feedback can also be added retroactively. `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` clean (`/rnd/clara/results` + new API routes in the manifest); `npx vitest run` unchanged at 176 passed/5 failed/30 skipped (same pre-existing unrelated failures — VAD eagerness default, PDF-parse mock, triage red-flag scoring, simulated-patients runner). Verified live in dev: password gate, both pages render, auth-gated 401s on unauthenticated API calls, verdict/correctedConsultType validation 400s before touching the DB, and — since this sandbox has no RDS network path at all — the DB-touching calls fail closed with a clean JSON 500 (`ECONNREFUSED`/`AggregateError`) rather than crashing; the specific "migration 049 not applied" 42P01 message path is implemented but couldn't be exercised end-to-end here without real RDS connectivity. Feature-flagged the same way as the rest of `/rnd/clara`: `NEXT_PUBLIC_RND_SECTION_ENABLED` gates the homepage link, `CLARA_TEST_PASSWORD` gates both pages; unset ⇒ site unchanged. No PHI — synthetic test data only.
- **Nova Sonic silent-until-spoken-to fix (2026-07-09)**: On the AI Historian, the Nova Sonic voice engine sat silent at session start instead of greeting the patient — Henry (OpenAI) greets on its own via a `response.create` fired internally on `session.created` (`openaiWebrtcProvider.ts`), but Nova Sonic is pure speech-to-speech and only responds to input turns; nothing ever triggered an opening assistant turn, so the interview stalled until the patient spoke first. Fix (relay-side only): `NovaSonicSession.start()` (`services/nova-sonic-relay/src/novaSonicSession.ts:154-193`) now calls a new `sendGreetingKickoff()` right after the response loop is kicked off, which enqueues one USER-role text turn — `"[The interview has now started. Please greet the patient warmly by beginning the conversation and asking your first question.]"` — via the existing `pushSystemText()` path (role USER, not SYSTEM, since Nova hard-fails the whole stream on a second SYSTEM block). Guarded by a new `kickoffSent` flag so it fires exactly once per session. Investigated whether the client needed a matching change: `useRealtimeSession.ts`'s `startSession()` never calls `provider.requestResponse()` at session start for either engine — Henry's greeting is entirely internal to `openaiWebrtcProvider.ts`, and `requestResponse()` on `novaSonicWsProvider.ts` is a documented no-op used only for the mid-conversation scale-administration and early-end-flush paths, neither of which touches session start. So the greeting trigger is symmetric across both providers (internal to the provider/session, not hook-driven) — **no client-side change needed.** `npx tsc --noEmit` clean at repo root; `services/nova-sonic-relay` (own `tsconfig.json`/`package.json`, npm not pnpm) `npm run build` clean; relay's own `npm run test` (vitest) 6 passed/0 failed; root `npx vitest run` unchanged at 143 passed/6 failed/30 skipped (same pre-existing unrelated failures — VAD eagerness default, PDF-parse mock, triage red-flag scoring, simulated-patients runner, historian turn-budget wording). **Not verifiable headlessly** — there's no way to open a real bidirectional Bedrock stream in this environment; needs the owner's live device test on an actual Nova Sonic historian session (open `/consult` or `/patient/historian` on the Nova voice, confirm it starts speaking within ~3s without the patient speaking first). **Also needs a relay ECS redeploy to take effect** — `services/nova-sonic-relay` is a standalone service, not on Amplify, and does not auto-deploy with the rest of the app; this fix is inert in production until that service is rebuilt and redeployed.
- **AI Historian post-interview report page with Physician/Patient tabs (2026-07-09)**: On the standalone `/patient/historian` surface, `phase === 'complete'` previously only rendered duration/question-count stats (`HistorianSessionComplete.tsx`) and threw away the interview content. New `src/components/HistorianReportView.tsx` (wired into `NeurologicHistorian.tsx` in place of the old component, which is now unused and removed) shows a two-tab report instead: **Physician Report** reuses `IntakeReviewSection` (the same OLDCARTS/HPI card UI the `/consult` pipeline uses) plus a red-flags banner — loosened `IntakeReviewSection`'s prop from `consult: NeurologyConsult | null` to a new exported `IntakeReviewData` shape (`historian_structured_output`/`historian_summary`/`interview_completion_status?`) so it works without a real `neurology_consults` row; `NeurologyConsult` still satisfies it structurally, so the existing `/consult` caller (`PatientToolsStepPanel`) is unaffected. **Patient Report** calls a new `POST /api/ai/historian/patient-report` (Bedrock Sonnet 4.6 via `invokeBedrock`, no auth — matches the sibling `/save` and `/session` routes' patient-facing, unauthenticated pattern) that generates a warm, ~6th-grade-reading-level recap of what the patient shared; the system prompt explicitly forbids diagnosis, clinical interpretation, treatment advice, and any mention of red flags/severity/urgency, and frames the text as "your neurologist will review this before your visit" with a 911/contact-your-doctor closing line. Shows a spinner while generating; **fails open** on any Bedrock error or malformed response by returning `{ patientReport: narrativeSummary }` with a 200, so the tab can never render blank or crash. `npx tsc --noEmit` clean; `npx vitest run` unchanged at 143 passed/6 failed/30 skipped (same 6 pre-existing unrelated failures); `npm run build` clean, new route confirmed in the build manifest. **Not verifiable headlessly** — the full flow requires a live voice interview through OpenAI Realtime; owner should device-test end-to-end (interview → complete → both report tabs) and review the Patient Report guardrails/tone before wider use.
- **Henry closing-statement clipping regression fix (2026-07-09)**: PR #150 (Fix 3 below) fixed "Henry never auto-ends" but introduced a regression — Henry now auto-ends, but his closing statement gets clipped mid-sentence. Root cause: on OpenAI, `sendToolResult()` (`openaiWebrtcProvider.ts`) always issues a follow-up `response.create` after acking the `save_interview_output` tool call, so the closing line can be spoken either before the tool call (same-turn — `response.audio_transcript.done` fires before `response.done`/the tool call) or after it, via that follow-up response (tool-first). The tool-call branch in `useRealtimeSession.ts` scheduled a fixed 250ms auto-end timer right after firing that follow-up `response.create`, and the `aiSpeechStart` handler never cancelled a pending timer — so if the follow-up closing response's audio started within that 250ms window, the stale timer fired anyway mid-utterance and tore the session down. Fix (flow-independent, handles both orderings): `aiSpeechStart` now clears any pending `autoEndTimerRef` (a new utterance is starting — never end under it); the `save_interview_output` tool-call branch now schedules a generous 2500ms **fallback** via `maybeScheduleAutoEnd(2500)` instead of a short one, giving a follow-up closing response time to start and cancel it; `aiSpeechStop` now schedules a short 400ms end via `maybeScheduleAutoEnd(400)` when `interviewCompleted`, which is what actually ends the session right after the closing audio finishes. `maybeScheduleAutoEnd` gained a `delayMs` parameter (default 250, unchanged for the `assistantTranscript` call site) so the three call sites can tune independently. `npx tsc --noEmit` clean; `npx vitest run` unchanged at 143 passed/6 failed/30 skipped (same 6 pre-existing unrelated failures as below). **Not verifiable headlessly** — this is a real-time audio-timing race; needs on-device verification against the live OpenAI Realtime session to confirm the closing line plays in full under both same-turn and tool-first orderings.
- **AI Historian close/barge-in fixes (2026-07-09)**: Four fixes to the voice-intake close and interrupt behavior, spanning both engines. (1) **Henry (OpenAI) never auto-ended** after his closing statement — `save_interview_output` surfaces via `response.done`, which fires AFTER `response.audio_transcript.done` (the closing transcript itself), so the old auto-end (which only checked `interviewCompletedRef` at the moment the transcript arrived) never saw it true in time. `useRealtimeSession.ts` now has an order-independent `maybeScheduleAutoEnd()` — gated on `interviewCompletedRef` + `!finalizingRef` + a new `isAiSpeakingRef` (mirrors `isAiSpeaking` via a `setAiSpeaking()` wrapper so it's readable synchronously) — called from the `assistantTranscript` handler, the `save_interview_output` tool-call branch (which now also sets `interviewCompletedRef.current = true` synchronously, not just via the state-sync effect), and the `aiSpeechStop` case, so whichever signal lands last is the one that schedules the 250ms auto-end. The existing component-level 1500ms fallback (`NeurologicHistorian.tsx`/`EmbeddedHistorian.tsx`) is untouched as a backstop. (2) **Nova Sonic's closing statement audio was getting cut off** — the client treated "closing TEXT arrived" as "done speaking" and tore the session down immediately, but Nova's closing AUDIO streams as separate PCM chunks still queued in the player, and `PcmPlayer.close()` hard-stops them. Added `PcmPlayer.whenDrained()` (`src/lib/voice/audio/player.ts`) — resolves once all currently-scheduled sources finish (immediately if nothing queued); `interrupt()`/`close()` semantics unchanged. `novaSonicWsProvider.ts` now defers emitting the `aiSpeechStop` VoiceEvent (from both the relay's `aiSpeechStop` message and the `completion` fallback) until `player.whenDrained()` resolves — barge-in (`bargeIn` message) still emits immediately since `interrupt()` already silenced playback. This is a general drain-before-signal fix (not turn-specific), so Fix 3's `!isAiSpeakingRef.current` gate now only clears once the closing audio has actually finished playing. (3) **Nova prompt**: added CRITICAL RULE 10 to `historianPrompts.ts` — never call `save_interview_output` in the same turn as a question; wait for and acknowledge the patient's answer first. (4) **Barge-in over-deflection**: added CRITICAL RULE 6 — the "wonderful question for your neurologist" deflection (rule 5) is now explicitly scoped to actual medical-advice/diagnosis requests; genuine off-topic interruptions get a brief acknowledgment and a steer back to the current question. `npx tsc --noEmit` clean; `npx vitest run` unchanged at 143 passed/6 failed/30 skipped (all 6 failures pre-existing on `main`, unrelated to voice/historian — VAD eagerness default, PDF-parse mock, triage red-flag scoring, simulated-patients runner). Bug 2 (Nova audio drain) needs on-device verification against the live relay — not verifiable headlessly.
- **AI Historian patient consent/disclosure gate (2026-07-06)**: Added a pre-interview consent/disclosure step to both historian entry surfaces — `NeurologicHistorian.tsx` (`/patient/historian`) and `EmbeddedHistorian.tsx` (`/consult`). New shared `src/components/HistorianConsentDisclosure.tsx` renders a modal ("Before you begin" — AI-not-a-clinician disclosure, 911 emergency notice, recording notice, "You can end the interview at any time") that must be confirmed before `startSession()` (the OpenAI Realtime session/mic start) can run — the click handler on the "Start Voice Interview" button now always routes through a `consentAcknowledged` gate check first; `startSession()` is only ever called from the disclosure's confirm handler. Approved by Steve Arbogast, DO (2026-07-06): "patient consent needs to be built in" — all patients today are artificial/synthetic, but the disclosure ships ahead of any real-patient use. UI gate only — no DB migration, no schema changes, no new API routes (the existing `historian_sessions` insert is an explicit column allowlist with no consent-timestamp column, so audit-grade consent capture is a deliberate follow-up decision, not silently dropped).
- **Speech biomarkers — multi-engine bake-off harness + SDNE capture spec (2026-06-30)**: Following the "capture once, score with every engine, decide later" call for the normalization/reproducibility trials (Riya + Steven), added the analysis half of the acoustic-biomarker work. `src/lib/voice/engines.ts` = engine registry + `scoreAllEngines()` **parallel fan-out** (pure-TS in-process + `parselmouthEngine` HTTP adapter to a `VOICE_PRAAT_URL` Praat sidecar, unavailable-safe — so one recording is scored by every engine simultaneously and stored per `engine+version`). `src/lib/voice/stats.ts` = `icc21` (ICC(2,1) absolute agreement), `blandAltman`, `withinSubjectCv`, `iccLabel`. `src/lib/voice/bench.ts` = `buildBenchReport()` (per task/feature/engine: engine-agreement ICC, test-retest ICC, within-subject CV, profile separation). `src/lib/voice/wav.ts` = lossless WAV reader. `scripts/voice-bench.ts` = CLI over a manifest+audio dir → markdown/CSV. Route gained `?allEngines=1`. +16 tests (23 voice tests total; tsc+eslint clean). The **headset-side capture protocol is a separate repo (`blondarb/SDNE`)** — cross-repo plan written at `docs/plans/2026-06-30-sdne-speech-trial-capture-spec.md` (lossless 44.1/48 kHz capture, calibration tone, T08 + sustained-vowel + reading tasks, Speech/Bulbar domain payload, reproducibility trial design). Trial-grade capture must be **lossless** — the web demo's Opus@16 kHz is not sufficient for jitter/shimmer/HNR.
- **Acoustic speech biomarkers — Phase A scaffold (2026-06-30)**: Reviewing `menelly/AI_Ears` (an MCP server that analyzes audio *beyond* transcription — voice tremor, breath, dynamics) surfaced a capability we lack: every voice surface today (Historian/Intake/Follow-Up/dictation/visit) uses audio only for ASR and **discards the waveform**, throwing away clinically measurable neuro speech signs (hypophonia, monopitch, vocal tremor, DDK rate, jitter/shimmer/HNR → MDS-UPDRS speech item / Frenchay). Scoped + **scaffolded a testable MVP** behind `VOICE_BIOMARKERS_ENABLED` (default off; route 503s until enabled): pure-TS, zero-dep, time-domain engine `src/lib/voice/acoustic.ts` (autocorr F0/monopitch, loudness+decay, voiced/pause timing, max phonation time, DDK rate+regularity, 4–8 Hz tremor; jitter/shimmer/HNR are **approximate** pending a Praat-grade engine), `POST /api/ai/voice-biomarkers` (browser decodes→16 kHz mono Int16 PCM→route, so **no ffmpeg/Python in the Node Lambda**; best-effort `scale_results` write as `mds_updrs_speech_<task>`), provisional `THRESHOLDS` config in `src/lib/voice/flagging.ts`, `VoiceTaskCapture`/`SpeechBiomarkerCard` (reuses `SDNEFlagChip`), `/voice-biomarkers` runner, 7 vitest cases (all pass; tsc+eslint clean). **Test week:** engine bake-off (Parselmouth/Praat, openSMILE eGeMAPS, DisVoice vs pure-TS) on labeled profiles (James Morrison=PD, Eleanor Wright=ET, Maria Santos=normal) + threshold tuning + go/no-go on a Python sidecar for real jitter/shimmer/HNR. SDNE already runs Pa-Ta-Ka DDK (T08, `sdneTypes.ts`) on external XR — full acoustic scoring at that head-mic source is a hand-off to the SDNE team (path 2). Screening only, not diagnostic. Spec: `docs/plans/2026-06-30-acoustic-speech-biomarkers-spec.md`.
- **Scale results now link to a patient (2026-06-29)**: the two historian scale-write paths (`/api/ai/historian/scales` submit + step) inserted `consult_id`/`historian_session_id` but **never set `patient_id`**, so every historian-administered scale landed unlinked (`scale_results` was 5 rows, 0 with a patient) — blocking longitudinal scale-trend work. Added `resolveScalePatientId()` (looks up `patient_id` from `historian_sessions` first, then `neurology_consults`; fail-safe → null, never throws) wired into both inserts. **Caveat:** correct plumbing but insufficient alone — upstream `neurology_consults` is 0/23 patient-linked (demo sessions aren't tied to real patients), so real longitudinal data still needs real patient-linked usage; near-term the historian-DDx longitudinal work uses synthetic trajectories. See [[project_historian_ddx_longitudinal]].
- **Historian background-noise fix (2026-06-29)**: Riya reported the `/consult` AI Historian froze/stuttered in noisy rooms and felt less smooth than a clean OpenAI Realtime demo. Root cause: the Realtime session set turn detection + transcription but **no input noise reduction**, so background noise reached the VAD and false-triggered turn-taking, repeatedly interrupting the AI. Fix: added `audio.input.noise_reduction` to the session (`src/app/api/ai/historian/session/route.ts` + `getNoiseReductionConfig()` in `historianTypes.ts`), default `far_field` (laptop/speakerphone/open-room; `near_field` for headsets, `off` to disable via `HISTORIAN_NOISE_REDUCTION`). OpenAI filters this audio *before* the VAD, cutting the false positives. **Fail-open safety:** if OpenAI ever rejects the field, the route retries once without it, so the historian can never break from this change. Voice quality note: FAQ POC voice-out also upgraded Joanna-neural → **Ruth-generative** (Polly's most natural tier).
- **Triage custom-note crash fix (2026-06-26)**: Custom (non-sample) referral notes crashed the result screen with `TypeError: i.toFixed is not a function`; sample notes worked. Root cause: the async poll endpoint `/api/triage/[id]` returned `data.weighted_score` straight from Postgres, and **node-postgres returns NUMERIC/DECIMAL columns as strings** — so `weighted_score` arrived as e.g. `"3.50"`, slipped past the UI's `!== null`-only guard, and `.toFixed()` threw. Sample stroke notes escaped it because red-flag-override cases set `weighted_score` to `null` (badge skips rendering). Fix: added `parseWeightedScore()` at the API boundary to coerce to a finite number or null (honoring the typed `weighted_score: number | null` contract), and hardened the two render sites (`TriageTierBadge`, `CopyReportButton`) to check `typeof === 'number' && Number.isFinite()` as a backstop. Found via Riya's 2026-06-26 demo smoke-test (`qa/RIA-DEMO-SMOKE-2026-06-26.md`).
- **Triage latency reduction (2026-06-26)**: Cut `/triage` response time without changing the scoring algorithm or model (stays on Sonnet 4.6 — Haiku rejected to preserve clinical quality). Three changes: (1) **Prompt caching** — `invokeBedrock()` gained an opt-in `cacheSystem` flag that sends the ~5.4k-token static triage system prompt as a `cache_control: ephemeral` block; enabled in `route.ts` + `runTriage.ts` so back-to-back calls reuse the prefill (faster TTFT, lower input cost). (2) **Output brevity directive** in `systemPrompt.ts` — new "OUTPUT BREVITY" section caps rationale length + workup/reasons list size (wording only; scores, red flags, overrides, redirects, and safety findings explicitly unaffected). Verified on 5 representative referrals at temp 0: **all dimension scores, tiers, and override/redirect/insufficient flags identical** baseline→trimmed, output tokens **−32%** (7,145→4,871); one case's soft `confidence` label shifted high→moderate (informational, no decision impact). (3) **Poll interval** 1.5s→1s (`pollClient.ts`) for snappier perceived completion. Next-week follow-up (Riya): output-trim depth + Haiku A/B via `BEDROCK_TRIAGE_MODEL` are noted in `qa/RIA-DEMO-SMOKE-2026-06-26.md`.

- **ASR vocabulary biasing implemented (2026-06-07)**: New `src/lib/asr/clinical-lexicon.ts` biases all five transcription surfaces toward a neurology lexicon (curated symptom/anatomy/scale terms interleaved with the full `NEURO_FORMULARY` brand+generic drug list). Realtime surfaces (Historian, Intake, Follow-Up) get a budget-trimmed Whisper `prompt`; the two Deepgram **Nova-3** paths — dictation (`/api/ai/transcribe`) and visit recording (`/api/ai/visit-ai`, diarized) — get a `keyterm` array (Nova-3 Keyterm Prompting; if ever downgraded to nova-2, switch `keyterm`→`keywords`). Follow-Up hoists the patient's own name/provider/meds first so they survive the ~224-token Whisper cap. Gated by `ASR_VOCAB_BIASING` (default on; `false`/`0`/`off` reverts without a deploy). Unit tests in `src/lib/__tests__/clinical-lexicon.test.ts` (13 cases). Spec updated to Implemented: `docs/plans/2026-06-07-asr-vocabulary-biasing-spec.md`.

- **Deploy hygiene + ASR biasing spec (2026-06-07)**: Removed a stray `vercel.json` and documented that **AWS Amplify is the sole deploy target** (a Vercel GitHub integration had been posting deploy checks on PRs — the integration itself must be disconnected in the Vercel/GitHub dashboard, not just the repo). Investigated ASR vocabulary biasing across all voice surfaces and confirmed **we pass zero domain biasing today** (Historian/Intake/Follow-Up all bare `whisper-1`); wrote a scoping spec at `docs/plans/2026-06-07-asr-vocabulary-biasing-spec.md`. Also audited the crisis-handoff path — confirmed a clean patient-facing handoff exists for active emergencies (scripted 911/988/Crisis-Text message + interview stop), while historical clinical red flags remain clinician-review-only by design.

- **Reference doc: voice AI agent field lessons (2026-05-29)**: Captured a practitioner's six hard-won lessons from shipping a production phone-based voice scheduling agent (latency as the product, ASR failing on high-stakes words like names/meds, integration being the hard 80%, barge-in handling, graceful human handoff, compliance-first architecture) in `docs/references/voice-agent-field-lessons.md`. Annotated each point with relevance to our voice surfaces — AI Historian (`/consult`) and Live Follow-Up Agent (`/follow-up`). Reference only, not a spec; use as a pre-mortem checklist when scoping phone/voice work.

- **AI Historian Realtime API + Harness Upgrade (2026-05-27)**: Demo-only `/consult` Step 2 historian migrated to OpenAI's current GA Realtime API (`client_secrets` + `/v1/realtime/calls` + `gpt-realtime-2` + `semantic_vad`). Tool surface consolidated to 3 (save_interview_output, query_evidence, scale_step — paginated). New Localizer push channel via re-serialized `session.update` (base prompt + delta, every 3 turns). Phased prompt structure (turns 1-3 open, turns 4+ tool-augmented) with explicit neurology focus + 15-25 turn budget. Migration 047 added paginated state to `scale_results` + relaxed NOT NULL on `patient_id`/`responses`/`raw_score` (legacy submit flow had been silently failing). Env flags `OPENAI_HISTORIAN_REALTIME_MODEL` / `HISTORIAN_TURN_DETECTION_MODE` for hot-revert. Spec + plan + 2-round cross-check audit trail + pre/post eval rubric in `docs/superpowers/` and `qa/historian-baselines/`. (Demo-only; multi-modal + prior-visits explicitly future agents.)

- **Triage async+polling (PRs #112-113, May 2)**: `/api/triage` + `/api/triage/extract` now return 202 immediately and clients poll `/api/triage/[id]/result`; migration 046 dropped NOT NULL on result columns so pending rows insert cleanly.
- **Neuro Intake depth pass (PR #109, Apr 25)**: Scale-trigger pipeline wired in `EmbeddedHistorian`; AI Assessment+Plan synthesis via `assessment-generator.ts`; physician corrections persisted; 7 safety fields extracted from referrals; historian fully embedded inline.
- **SSO error surfacing on `/login` (2026-04-22)**: Tester reported "I click the sign in with Sevaro SSO and it gives me an error" with no further detail — because the app was showing none. The callback route (`src/app/api/auth/callback/route.ts`) was collapsing every Cognito failure into generic `no_code` / `token_exchange` codes, discarding the `error` + `error_description` params Cognito sends back. The login page (`src/app/login/page.tsx`) then rendered one of three canned strings. Fix: callback now (a) checks for Cognito's `error` param before `code` and propagates both `error` and `error_description` to `/login`, and (b) on token-exchange failure, parses the error body for `error_description` (falling back to the raw body truncated to 200 chars). Login page now shows a specific headline per code (including `access_denied` → "Sign-in was cancelled.") plus the actual description, and a hint to contact the administrator. Feedback session `ad9e5708-476a-470c-a38f-b0f209526dae` marked addressed.

- **Consult save + mobile differential fixes (2026-04-17)**: Fixed "Failed to save interview data" error on `/consult` — root cause identical to the triage `2d1e445` fix: the shared `buildInsert` in `src/lib/db-query.ts` auto-stringifies plain objects but passes arrays through raw (for `text[]` compat), so `historian_sessions.transcript` and `red_flags` (both JSONB arrays) hit node-postgres as JS arrays-of-objects and the insert 500'd. Pre-stringified those fields at the call site in `src/app/api/ai/historian/save/route.ts`, and applied the same fix to `historian_red_flags` inside `linkHistorianToConsult` (`src/lib/consult/pipeline.ts`) so the consult advances to `historian_complete`. Also addressed mobile physician differential visibility: `LocalizerPanel` was a fixed 340px wide (clipped on 322px viewports) → now `width: 100%` with `maxWidth: 340px`; `EmbeddedHistorian` auto-opens the panel once the first localizer result arrives, and the toggle label changed from "MD View"/"Hide" to "Differential"/"Hide Dx" so mobile users can actually find it. Feedback session `9f2a87dc-ac02-42f9-b1ba-34ac161e0403` marked addressed.

- **Consult interview early-end fix (2026-04-17)**: Fixed critical `/consult` bug where clicking "End Interview" before the AI called `save_interview_output` silently dropped the entire intake — both `historian_structured_output` and `historian_summary` were written as null and the review step rendered "No interview data available" despite the patient answering. `useRealtimeSession.endSession` now (a) nudges the AI to flush via the open data channel and waits up to 4s for refs to populate, and (b) falls back to storing the raw transcript as `narrative_summary` so data is never lost. Hardened `EmbeddedHistorian` to check `res.ok` on the save POST (fetch doesn't throw on 4xx/5xx). Added an `interview_completion_status` column (`complete` | `ended_early`, nullable) to both `historian_sessions` and `neurology_consults` via migration 044 (applied to `ops_amplehtml`); `IntakeReviewSection` shows an amber "partial intake" banner when `ended_early`. Feedback session `027f134f-b383-4134-aec6-a7fdaa3fbc52` marked addressed.

- **Consult page feedback fixes (2026-04-17)**: Addressed 5 tester-reported issues on `/consult`. Voice-interview reliability: raised server_vad threshold 0.5→0.65 and silence_duration_ms 700→1200 in `/api/ai/historian/session` to stop speakerphone echo from interrupting the AI mid-sentence; attached remote `<audio>` element to DOM (iOS Safari routing requirement) and added explicit `echoCancellation`/`noiseSuppression`/`autoGainControl` mic constraints in `useRealtimeSession`. Mobile layout: responsive padding on `/consult` page container, compact `FeatureSubHeader` (hide link labels <640px, title ellipsis), pipeline bar labels shrink then hide <380px, actor side panel stacks below main <900px, `overflow-wrap: anywhere` on streaming text and transcript entries in `EmbeddedHistorian`. Auto-end: `useRealtimeSession` now exposes `interviewCompleted` which flips on `save_interview_output` tool call; `EmbeddedHistorian` auto-ends the session 1.5s after the AI finishes its closing message. Feedback session `18250867-ca49-460c-bd85-df91dbdb3f55` marked addressed.

- **OAuth SSO Migration (2026-03-27)**: Migrated auth from direct Cognito SDK (USER_PASSWORD_AUTH) to OAuth + PKCE via Cognito Hosted UI at `auth.neuroplans.app`. Removed `amazon-cognito-identity-js` dependency. Created new OAuth API routes (login, callback, logout, refresh, me). Replaced AuthContext with cookie-based auth (httpOnly cookies, proactive 50-min refresh). All 80+ API routes work unchanged via `getUser()`. Login page now redirects to SSO. Signup handled by Hosted UI. Evidence Engine pool `us-east-2_9y6XyJnXC`, client `6rahc3cs4846f05gf7fbucqi4d`.

- **Integrated Neuro Intake Engine — Phases 1–7 (2026-03-20)**: Built the complete 7-phase clinical pipeline: (1) Triage & Intake with 11-state ConsultStatus machine, (2) AI Historian integration with camelCase LocalizerRequest contract, (3) Background Localizer with Bedrock-powered differential generation, (4) Red Flag Escalation with 20+ pattern detector wired into useRealtimeSession, (5) Patient Web Tools — interactive SVG body map (26 regions), finger tapping test, accelerometer tremor detector, (6) SDNE Integration linking XR exam results to consults, (7) Unified Report Generator with pure-function builder and physician-facing viewer. New tables: `patient_body_map_markers`, `patient_device_measurements`, `consult_reports`, plus SDNE columns on `neurology_consults`. Migrations 036–038 pending against RDS. See `docs/HANDOFF_2026-03-20_neuro-intake-engine.md`.

- **AI Triage Consistency Improvements (2026-03-12)**: Pushed triage scoring consistency from 96% toward 99%+ target. Added clinical anchoring examples (2-3 neurology-specific examples per score level for all 5 dimensions) and tie-breaking rules to the system prompt. Changed production defaults: temperature 0.2→0 (greedy decoding), aligned maxTokens to 3000 across both route.ts and runTriage.ts. Added token usage passthrough in `invokeBedrockJSON` and logging to `triage_sessions` table (new `ai_input_tokens`/`ai_output_tokens` columns). Updated playbook pseudocode to reflect deterministic red flag escalation, synced system prompt section, replaced OpenAI model references with Bedrock Sonnet 4.6. Added Triage section to `docs/AI_PROMPTS_AND_MODELS.md`.

- **Wearable Narrative Enhancements (2026-03-05)**: Added "Generate 30-Day Summary" button to PatientTimeline header for longitudinal narrative generation. Added regenerate (refresh) buttons to ClinicalNarrativePanel and LongitudinalSummaryBanner. Added auto-generation logic that detects assessments without narratives on data load/poll/patient-switch and generates them sequentially with progress indicator.

- **AI Clinical Narrative Pipeline (2026-03-04)**: Split DiseaseTrack into MotorTrack + CognitiveTrack. Built 2-stage AI pipeline (Bedrock extraction → narrative) for `analyze-assessment`. "Generate AI Clinical Interpretation" buttons on each assessment card produce clinical narratives with severity-flagged findings. See `docs/HANDOFF_2026-03-04_clinical-narrative-pipeline.md`.

- **Wearable Dashboard Data Fixes (2026-03-02)**: Fixed 6 data display issues on `/wearable` for real Apple Watch data: resting HR 0 treated as null, sleep fallback to total bar when stages unavailable, server-side rolling 7-day averages, auto-computed baselines from actual data, diagnosis-aware Disease Track (ET vs PD), 15-minute auto-refresh polling. See `docs/HANDOFF_2026-03-02_wearable-dashboard-fixes.md`.

- **SevaroMonitor iOS Sleep Fixes (2026-03-02)**: Fixed overnight sleep split (6 PM-to-6 PM window instead of midnight) and added per-stage sleep breakdown (Deep, Light, REM, Awake) to HealthKit collection. Changes in `blondarb/SevaroMonitor` repo.

- **Live Follow-Up Agent Phase A: SMS (2026-02-25)**: Implemented real-phone SMS demo for Follow-Up Agent. User enters phone number on conversation page, receives a real Twilio text, replies via SMS, and the clinician dashboard updates via polling. New files: `twilioClient.ts` (send/validate), `conversationEngine.ts` (shared AI turn logic), `send-sms/route.ts` (initiate), `twilio-sms/route.ts` (webhook), `LiveDemoPanel.tsx` (UI). Migration 031 fixes schema mismatches and adds `followup_phone_sessions` table. Refactored `message/route.ts` to use shared engine. `ClinicianDashboard` now accepts `liveSessionId` prop for Realtime subscription. See `docs/plans/2026-02-25-live-followup-sms-plan.md` for implementation plan.

- **Cockpit/Dashboard Separation (2026-02-25)**: Separated Clinician Cockpit and Operations Dashboard into distinct tools. Cockpit (`/physician`) redesigned as a 2-column layout: Schedule (~380px, with week-strip nav, mini-month grid, prep badges) | Time-Phased Briefing (Morning/Midday/End of Day with phase-specific narratives, icons, gradients). Notifications moved to a bell-triggered 380px slide-over drawer with enhanced cards showing inline clinical data (vitals, wearable readings). Breadcrumb bar ("< Home | Clinician Cockpit | Demo") added for navigation. Dashboard (`/dashboard`) renamed to Operations Dashboard with new Zone 1 Operational Summary. Homepage rearranged to 4+3 layout. See `docs/plans/2026-02-25-cockpit-dashboard-separation-design.md`.

- **Live Follow-Up Agent Design (2026-02-25)**: Design doc for real phone demo — Twilio SMS + OpenAI Realtime voice. User enters phone number, gets a real text, can reply or call back. Dashboard updates in real-time. See `docs/plans/2026-02-25-live-followup-agent-design.md`. Playbook `04_post_visit_agent.md` updated with Phase 1.5 roadmap.

- **Physician Workspace Card Breakout**: Replaced single "Physician Workspace" homepage card with 3 cards:
  - **Clinician Dashboard** → `/dashboard` (Command Center — 5-zone AI dashboard)
  - **My Schedule** → `/physician` (schedule-first via `initialViewMode="appointments"`, click patient for inline chart swap)
  - **Documentation** → `/ehr` (lands directly on patient chart via `initialViewMode="chart"`, random patient selection, supports `?patient=ID`)
  - Command Center card moved from Ongoing Care track to Clinician track (Ongoing Care now has 2 cards: Follow-Up Agent + Wearable)
  - `ClinicalNote` now accepts `initialViewMode` prop (`'cockpit' | 'appointments' | 'chart'`)
  - `fetchDashboardData()` accepts optional `patientId` for specific patient loading
  - Client wrapper components (`PhysicianPageWrapper.tsx`, `EhrPageWrapper.tsx`) handle Server→Client icon serialization boundary

Full changelog: [`docs/CHANGELOG.md`](docs/CHANGELOG.md)

## Body of Work

**Status**: Active — verified July 13, 2026

### Recent
- **Clara stroke-in-evolution round-2 red-team fix (Jul 13)** — Round-1 fixes verified live. Round 2 (`1630e8b`) found one more under-triage: plainly-phrased worsening ("weakness a couple days ago, but this morning it clearly got worse") wasn't tripping the worsening override, which only caught emphatic phrasing ("suddenly worse"). Strengthened `claraRulebook.ts` so any worsening/progression/spread/new-deficit forces EMERGENT regardless of a >24h base, however plainly it's phrased.
- **Clara stroke-alert dispatch voice-flow + red-team tightening (Jul 13)** — New voice flow (`5ad8492`): the immediate page to the on-call team never waits for a full history, and the ONLY step-down — routing to the STAT line instead — requires an affirmative "more than 24h and stable" plus a spoken confirm-back; any hesitation, correction, or new/worse finding keeps the page. A same-day red-team pass (`49df017`) found the downgrade path still under-triaged wake-up/found-down presentations and hedged or minimized LKW framing ("not sure," "he thinks," "a bit"); tightened the rulebook wording and added Gate-0 floor patterns for wake-up-with-deficit and post-tPA-with-decline so those cases stay EMERGENT. Both merged to `main` same day.
- **Clara Gate-0 stroke-alert detection + MRN read-back fix (Jul 13)** — Live-test finding (`ee43fea`): Gate-0's deterministic floor didn't fire on how a stroke is actually called in ("stroke alert," "code stroke," "activate stroke") or on lateralized weakness in noun form ("right-sided weakness"), letting those calls ride entirely on the LLM; widened the stroke bank to catch both (over-triage direction, no tier changes). Follow-up (`50f29f6`) removed an absolute override that locked stroke-alert calls to EMERGENT even once LKW was confirmed subacute >24h (the existing >24h→STAT2 rule now applies as designed), and extended the digit-by-digit MRN/FIN read-back rule to cover recaps, not just the initial read-back (Clara had been dropping a digit reciting MRNs as a single number).
- **Clara: missing identifiers never block the connection (Jul 12)** — Unknown patient identifiers (age, DOB, MRN, name) no longer stall or gate the physician connection; Clara asks each once, accepts "I don't know," and proceeds — including a John/Jane Doe fallback when the name is unknown. Prompt-only change in `CLARA_VOICE_INSTRUCTIONS` (session/route.ts).
- **Clara rounding: group/census vs specific-patient branch (Jul 12)** — New ROUNDING section in `CLARA_VOICE_INSTRUCTIONS` determines the round's shape before asking for identifiers: group/census rounds skip per-patient identifiers (confirm facility/unit and connect), specific-patient rounds still collect name + MRN/FIN + location each. Same tier/destination either way (ROUNDING → MD2) — no threshold/routing change.
- **Clara iOS audio: definitive clicking fix via AudioWorklet continuous player (Jul 12)** — After 7+ iterations on the `/rnd/clara` iOS crackle bug, root-caused to PCM worklet underruns (confirmed via new diagnostics counters in `clara_test_sessions`). AudioWorklet continuous `PcmPlayer` rewrite: single running AudioWorkletNode (no per-chunk `createBufferSource`), jitter buffer, pre-roll cushion, declick ramps, continuous 24k→native resampler — eliminates the underrun-on-small-chunk pattern. Diagnostics: `underruns`/`primes`/`maxQueued`/`pushes`/`samplesIn`/`blocksRendered` per session, surfaced in `ClaraVoiceTestView` debug line and `metadata.audioDiagnostics` in `clara_test_sessions`.
- **Duplicate-transcript relay root cause found + fixed (Jul 12)** — Nova Sonic emits every assistant turn twice: `SPECULATIVE` (at `contentStart`, when audio starts) then a byte-identical `FINAL` (when turn ends). Relay was forwarding both, causing duplicates beyond the client's 8s dedup window on replies longer than ~8s. Relay fix: track `generationStage` per TEXT contentId; forward SPECULATIVE only for assistant turns, FINAL only for user turns. `frame-probe.mjs` updated with `--url`/`--origin` flags for verifying deployed (not just localhost) relays. One-shot `deploy.sh` (ECR→ECS `force-new-deployment`) committed for relay redeploy. Client backstops retain windowed dedup.
- **Clara v2: tier-first triage + protocol hardening (Jul 12)** — Multiple protocol refinements from live testing: tier-first triage with reflexive onset question before classification; cumulative per-turn grading (vs single-turn); Synapse one-call framing; NP/PA routing path; CT-return "verify-in-system" flow; LKW + blood thinner questions scoped to stroke-only; non-emergent EEG = intake + notify + end flow. Echo-guard and half-duplex both prototyped and reverted (added latency without solving barge-in).
- **Clara shareable invite links + physician-to-physician persona (Jul 12)** — Signed token-based shareable links for no-login tester access (invitable URL, no Cognito required); concise physician-to-physician persona (teleneurology on-call framing); emergent "connect-while-gathering" flow (announces connecting to neurologist immediately while continuing to collect details); welcoming first-time-tester banner.
- **Clara demo loop: repeatable multi-scenario + preset scenarios + mobile-first (Jul 11)** — `/rnd/clara` now supports a repeatable back-to-back demo flow: 7 one-tap preset consult scripts (`presetScenarios.ts`, inpatient/facility teleneurology only — no outpatient), a scenario script banner during the call, a "Test another scenario" reset (new `resetSession()` in `useClaraVoiceSession`) that clears the finished call without losing the logged `clara_test_sessions` row, and a mobile-first sticky bottom action bar. Each call is still its own new session row — reset only clears client UI state.
- **Clara results + feedback page (Jul 11)** — Post-call results/review layer on `/rnd/clara`: `clara_test_feedback` table (migration 049, unapplied), `/api/ai/clara/feedback` + `/api/ai/clara/log` GET, `ClaraDecisionCard` (confidence/rationale/routing target + 👍/👎 feedback), review page at `/rnd/clara/results`. Feature-flagged/password-gated same as the rest of Clara; no PHI.
- **Nova historian closing statement + conversation quality fixes (PR #154, Jul 11)** — Three fixes from Riya's A/B testing: (1) Nova Sonic missing closing statement — new `VoiceProvider.nudgeClosing()` injects closing prompt via systemText relay frame after `sendToolResult` (no relay redeploy needed); (2) over-acknowledging — CRITICAL RULE 7 reworded to vary reactions and weave answers into next question; (3) re-asking answered questions — CRITICAL RULE 11 added to treat volunteered details as answered.

### In Progress
- Nova Sonic end-to-end live testing (relay deploy.sh committed for ECR→ECS one-shot redeploy; on-device verification still needed to confirm greeting + full interview flow with relay fix live)

### Planned
- Real-time voice streaming during encounters (AWS Transcribe Medical, HIPAA-eligible — next priority after triage async+polling shipped May 2)
- Diagnosis plan coverage expansion (98 plans in DB, 148/166 diagnoses covered — 18 diagnoses still missing)
- Speaker diarization UI (P2)
- Recommendation reconciliation engine (P2)
- Inpatient clinical scales (GCS, mRS, FOUR Score, Hunt & Hess, ICH, CAM-ICU, RASS)

### Known Issues
- Bedrock Amplify SSR env var wiring requires `next.config` inline for runtime access
- 18 neurology diagnoses still lack treatment plans in the database

## Documentation Update Policy

**IMPORTANT: Every commit must include updates to relevant documentation.** Documentation is never "a follow-up task" — it ships with the code.

**On every commit, review and update as needed:**

1. **CLAUDE.md** (this file) - Add to "Recent Changes" section for any notable change
2. **docs/IMPLEMENTATION_STATUS.md** - Mark features as COMPLETE/PENDING
3. **docs/CONSOLIDATED_ROADMAP.md** - Update status and completion dates
4. **Relevant PRD or handoff docs** in `docs/` - Keep feature specs accurate
5. **qa/TEST_CASES.yaml** - Add or update test cases for new/changed behavior
6. **API docs** (`docs/API_CONTRACTS.md`, `docs/AI_PROMPTS_AND_MODELS.md`) - Update if endpoints or models change

**Rule: If code changes, docs change in the same commit.** No exceptions. This prevents documentation drift and keeps the project source of truth reliable.

## QA Rules of Engagement

All test artifacts live in `qa/`. See those files for full details — this section is the short reference.

| File | Purpose |
|------|--------|
| `qa/TEST_RUNBOOK.md` | Stable baseline test plan (smoke + regression + mobile + role-based) |
| `qa/TEST_CASES.yaml` | Structured test cases with IDs, preconditions, steps, expected results |
| `qa/BUG_TEMPLATE.md` | Bug report template (repro, expected/actual, env, logs) |
| `qa/RELEASE_CHECKLIST.md` | Pre-deploy and post-deploy checks |
| `qa/runs/RUN_TEMPLATE.md` | Per-release run log template (copy, fill, save as `RUN-YYYY-MM-DD-NNN.md`) |

**Key rules:**
1. **Stable baseline + mission brief** — The runbook is stable. Each release gets a short mission brief in `qa/runs/` listing only the delta. Do not recreate the full plan each run.
2. **Every deploy runs smoke suite** (S1-S5) plus the mission brief's focus cases.
3. **VS Code (Claude Code)** = planner. **Chrome (Claude Code for Chrome)** = executor.
4. **Mobile-first**: Every run includes at least one 375px check (E1).
5. **Versioned**: `runbook_version` and `test_cases_version` tracked in file headers. Bump when flows/cases change.
