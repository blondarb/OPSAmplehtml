# HANDOFF — OPSAmplehtml

_Single source of truth for cross-AI handoffs. Claude (Cowork/Claude Code) and
ChatGPT/Codex read this FIRST and update it LAST. Plain language. No PHI (initials
only). No secrets._

## Snapshot (keep current)
- September 6 triage release reconciliation: clinical candidate `301ddc2` is locally integrated with newer main `bdcef4a` (#219), preserving the committed Historian and Launchpad work. Migration 061 is already live; only 063 remains unapplied. Read-only live checks at 23:12 UTC confirmed working sandbox SSO, intact governance, 52 cases / 26 reviews / 312 legacy AI runs, two archived studies, no fresh study, and an encrypted available backup with current point-in-time recovery. Earlier SSO/unknown-schema blockers are superseded. The exact GitHub/Amplify publication still requires direct destination authorization after automatic review rejected the previous push. No clinical push, PR, live 063 or deployment occurred in this preparation. Existing clinical/migration evidence remains valid for unchanged relevant source; combined-source TypeScript passed, while complete candidate CI and deployed acceptance remain gates. Pool hardening and the other owner's uncommitted authority-scan optimization remain separate. Asana Triage tool is Explore/incomplete. See the refreshed September 6 release approval receipt in the owning Codex task outputs.
- **2026-09-06 (Claude) — `/launchpad` project index ADDED (#219 merged):** Cognito-gated page listing every project with its live app / test / results / admin / installer / phone links (`src/data/launchpad.json`, 14 projects, 59 links — the SOURCE OF TRUTH for "where is X"; sevaro-context/projects/launchpad.json is a pointer). Open items per project are pulled live from the Asana Portfolio card's incomplete subtasks via `GET /api/launchpad/open-items` (Cognito-gated, 2-min cache). **Prod gap:** the SSR role `OPSAmple-AmplifySSR` cannot read `sevaro/asana/pat` yet — the page renders, the open-items column says why. Steve runs `scripts/ops/launchpad-asana-iam-policy.sh --apply` (or sets `ASANA_PAT` in Amplify + rebuild). Unknown URLs still flagged on the page: Sevaro Ops Board, LicenseCred entry route, RHTP host.
- **2026-09-06 (Claude, weekend historian depth program) — MERGED and deployed to main, every new lever flag-OFF:** #207 prompt depth (Phase 3 asked-directly gate, turn-cap/patient-ending overrides), #208 pre-close coverage gate (`NEXT_PUBLIC_HISTORIAN_PRECLOSE_GATE`) + the single localizer push now lives inside `useRealtimeSession.runLocalizer`, #209 off-request eval worker (`HISTORIAN_EVAL_MODE=queue`; unset = inline chain unchanged), #210/#211 attending-review Step 4 server + client (`HISTORIAN_ATTENDING_ENABLED`, `_INTERVAL`), #212 `NEXT_PUBLIC_HISTORIAN_PATIENT_STEER` (lets `/patient/*` run the localizer for Henry's private steer; panel stays clinician-only), #213 final differential v3 (exclusions require PATIENT evidence, `confidence_note` on the physician card, `unassessed` screen, 6000-token budget; live gate 7/7).
  - Until `NEXT_PUBLIC_HISTORIAN_PATIENT_STEER` is on, `/patient/historian` runs the prompt alone — no localizer, no attending review. Only `/consult/triage-historian` exercises them.
  - Human steps, batched for Steve: `cd infrastructure/triage-worker && sam build && sam deploy` (new HistorianEval queue/DLQ/worker/dispatcher + log groups + alarms); Amplify env flips need a REBUILD (`next.config.ts` env is build-time); psql migrations 060 (#203 — `/rnd/historian` 500s until applied) and 062 (pending-index, `CONCURRENTLY`, outside a transaction). Migration 061 (#206) was applied September 5; do not rerun it.
  - Open PR #181 rewrites the same `save_interview_output` handler and contextHint block — reconcile manually, never accept-both. Nova reject-path ordering (tool result → injected note) is reasoned, not live-verified.
- September 5 clinical-policy correction: approved local implementation and synthetic verification are complete on `fix/triage-clinical-policy-20260905`, based on main `8582bde`. Immediate action wording, source chronology, scorer/evaluation parity, a governed MS destination, and independent action/timing/service labels are implemented. The 25-note/20-group source-only package is ready for study setup; it has no independent clinician labels or live-model accuracy results. See `docs/triage-validation/2026-09-05-clinical-policy-corrections.md` and `qa/runs/RUN-2026-09-05-CLINICAL-POLICY.md`. This correction is local, not pushed, deployed or clinically validated; its migration is now 063 and is unapplied outside disposable fixtures.
- Earlier September 5 receipt records authorized POC deployment PR206 / Amplify job470 at `9ed901c` and application of migration 061, preserving 52 cases, 26 reviews and 312 AI runs in two archives. That completed step supersedes the older local-only 061 notes below. It was not repeated in this correction; later main changes through `8582bde` were taken as the source base.
- Status: Active — verified July 13, 2026 (per CLAUDE.md "Body of Work").
- Driver this week: Clara AI phone-triage stroke-alert red-teaming and dispatch-flow hardening, plus Nova Sonic relay/audio fixes (per CLAUDE.md "Recent", Jul 11–13).
- Lives in: https://github.com/blondarb/OPSAmplehtml.git

## Open threads / next actions
- [ ] Resume the prepared clinical release and migration 063 after the publication/access gates. Select two independent neurologists and an adjudicator, create a fresh study, and import `qa/triage-sentinel/clinical-policy-study-cases.json` after authorized setup. Finish immutable adjudication, canonical AI-versus-reference comparison, exact-revision results selection, statistics (especially insufficient-data/unequal-rater handling), notification-free intake/OCR evidence and trusted artifact provenance. OAuth state/redirect/PKCE, deployed security, partner API adapters and real-note study approval remain open. Do not rerun 061 or reinterpret historical scores as independent validation.
- [ ] Create a governed real-note testing path: PhysioNet MIMIC-IV-Note credentialing, selected n2c2 access, and DUA-compatible compute that does not send restricted notes to an unapproved third-party API.
- [ ] Draft a silent retrospective AI-triage validation protocol and obtain a formal institutional QI-versus-research/IRB determination before receiving partner referral notes.
- [ ] Harden the executive demo into one synthetic post-discharge journey: referral triage → informed AI historian → optional SENSE/SDNE → source-labeled clinician prep report, with a precomputed fallback for latency.
- [ ] Decide whether the historian should receive the raw referral, a bounded source-grounded referral packet, or retrieval access; current implementation passes triage summary/priority/red flags rather than the entire referral text.
- [ ] Verify the remaining cross-product handoffs end-to-end: SENSE/SDNE result return, consult report to EHR/chart prep, and Sevaro Evidence Engine desktop-scribe consumption.

## Decisions log (append-only, newest first)
- **2026-09-06 (Claude) — No vitest test may reach a live Bedrock endpoint.** `tests/setup/bedrockGuard.ts` (vitest `setupFiles`) fails any test whose code path calls `BedrockRuntimeClient.send` / `BedrockAgentRuntimeClient.send`, even when production code swallows the error. Stub the model hook (`reduceNarrative`, `mapChunk`, `extractSafety`) or `vi.mock('@/lib/bedrock')`; a test-file `vi.mock` of the SDK keeps precedence. Deliberate live runs opt in with the existing `HISTORIAN_EVAL_LIVE=1` gate (historian `*.gate.test.ts`, per `docs/HISTORIAN_VALIDATION_ROLLOUT.md`) or the generic `VITEST_ALLOW_LIVE_BEDROCK=1`. Why: unstubbed helpers were passing while spending 2–4 s in the AWS credential chain (4.2 s against the 5 s timeout) and could have made metered calls from a dev machine with a `[default]` profile. Other AWS SDK clients are not guarded.

## Session log (append-only, newest first)
### 2026-09-08 · Claude Code · Localizer steer stopped re-suggesting already-asked questions (PR, unmerged)
- Why: live steer went on in prod 2026-09-07 and repeated questions on the 3-turn localizer cadence — the same location question 3x and the same onset question 3x across a 40-question interview. Root cause: `STEER_GENERATOR_PROMPT` (Step 3a of `/api/ai/historian/localizer`) never saw which questions Henry had already asked, so it kept re-suggesting the same extraction gap, and Henry (via the private attending-hint tool, `src/lib/historian/novaSteer.ts`) repeated a question the patient had already answered. Historian RULE 11 ("never re-ask") only governs Henry's own prompt, not the localizer's suggestions feeding the hint.
- Did: added `extractAskedQuestions(turns, opts?)` (`src/lib/historian/askedQuestions.ts`) — pure helper that pulls question-mark sentences out of assistant transcript turns, dedupes case-insensitively (keeping the most recent occurrence's position), and caps the list (`max` 40 entries, `maxLen` 160 chars, both overridable). Wired into the route's `generatorInput` as `questionsAlreadyAsked` (computed from `body.fullTranscript ?? transcript`, same source already used for the attending-review Step 4 window) and added one rule to `STEER_GENERATOR_PROMPT` telling it never to re-suggest a covered gap, even reworded, and to return an empty `followUpQuestions` array if every gap is covered. `QUESTION_GENERATOR_PROMPT` (the full/detail differential prompt) is unchanged — same shared `generatorInput` object, but no prompt rule references the new field there.
- Checked but left alone: the attending-review path (`buildAttendingPrompt` / `attendingGaps.ts`) already receives the *full* transcript (`buildAttendingTranscriptWindow` over `body.fullTranscript ?? transcript`, 60k-char budget) and already instructs "A topic already asked directly in the transcript is NOT a gap, even if the answer is missing" — it does not have this bug, so it was not touched.
- Verified: `tests/historian/askedQuestions.test.ts` (new, 7 cases) + one new case in `tests/historian/localizerStep3Split.test.ts` asserting `questionsAlreadyAsked` reaches both the steer and detail Bedrock calls. `npx vitest run tests/historian tests/historian-eval`: 1043 passed / 16 skipped / 0 failed across 68 files (3 skipped files), no timeouts. `npx eslint` on changed files clean except the pre-existing `no-explicit-any` at route.ts:780 (untouched line). `npx tsc --noEmit -p tsconfig.json` clean.
- Not merged — PR open against main, held per the batched-rollout carve-out (patient/pilot-facing prod surface). Branch `fix/historian-steer-dedupe-asked-questions`.
### 2026-09-08 · Claude Code · Localizer now keyed by the server-minted historian session id, not `consultId ?? 'ephemeral'` (PR, unmerged)
- Did: PR #231 (merged 2026-09-07) made the localizer route upsert `historian_localizer_results` by the `sessionId` it's sent and made the runs route join it on `historian_sessions.id::text` — but `useRealtimeSession.ts`'s two localizer fetches (steer near the old line 515, detail near the old line 572) still sent `options.consultId ?? 'ephemeral'`. `/patient/historian` has no consult, so every localizer call there was keyed `'ephemeral'`, which never matches a real `historian_sessions.id` — patient-route runs still showed no live-localizer Signals on `/rnd/historian`. Both call sites now send `resolveLocalizerSessionId(serverSessionIdRef.current, options.consultId)` (new helper, `src/lib/historian/localizerSessionKey.ts`) — the server-minted historian session id (from `POST /api/ai/historian/session`, stored in `serverSessionIdRef`) wins, then `consultId`, then `'ephemeral'` only when neither is available yet. That id is also what `persistLocalizerResults`'s consult-linked lookup already expects (`neurology_consults.historian_session_id = sessionId`), so it's correct for both the standalone and consult-linked cases.
- Rows written to `historian_localizer_results` before this fix are keyed `'ephemeral'` (all sharing one row, `run_count` incrementing) — those do not retroactively join to any `historian_sessions.id`; only new runs after this deploy pick up the correct key.
- Tests: new `tests/historian/localizerSessionKey.test.ts` (precedence: server id / consultId / ephemeral). Updated `tests/historian/attendingClientWiring.test.ts`'s source-slice-eval harness to supply `serverSessionIdRef` + `resolveLocalizerSessionId` (it evals the real `runLocalizer` source, so it needed the new dependency). Full `tests/historian` + `tests/historian-eval`: 1038 passed, 16 skipped, no regressions.
- Not merged — PR open against main, held per the batched-rollout carve-out (patient/pilot-facing prod surface). Migration 064 (from PR #231) is a separate, still-unapplied prerequisite for either key to actually persist anywhere.

### 2026-09-07 · Claude Code · Session-keyed Localizer persistence for standalone historian sessions (PR, unmerged)
- Did: `persistLocalizerResults()` (`src/app/api/ai/historian/localizer/route.ts`) previously only wrote localizer_* onto `neurology_consults`, so `/patient/historian` standalone sessions (no consult row) never persisted anything and `/rnd/historian`'s runs route never showed live-localizer signal for them. Added `historian_localizer_results` (migration 064, `session_id TEXT PRIMARY KEY`) — persist now always upserts there by session id, then still runs the pre-existing consult-linked update when a consult is found. `src/app/api/ai/historian/runs/route.ts` LEFT JOINs the new table and COALESCEs (consult value wins) into the same `localizer_*` response fields.
- **Migration 064 is NOT applied** — same manual-psql pattern as 056/060/062/063. Until Steve runs `psql $RDS_URL -f migrations/064_historian_localizer_results.sql`, both the persist upsert and the runs-route join fail open on Postgres 42P01 (undefined_table): the upsert is swallowed and logged once (`console.info`, not error), and the runs query retries once with the join dropped. No behavior regression pre-migration; standalone-session signal only appears once it's applied.
- Not merged — PR open against main, held per the batched-rollout carve-out (patient/pilot-facing prod surface).

### 2026-09-07 · Claude Code · Deterministic false-closing (RULE 12 drift) counter for the historian eval
- Did: `src/lib/historian/eval/deterministicChecks.ts` gained `countFalseClosings` — scans every non-final ASSISTANT turn against `FALSE_CLOSING_PATTERNS` ("one last thing", "finally,", "wrap up", etc.) and reports the hit count/turn indexes; `runDeterministicChecks` now threads `falseClosings` through `DeterministicCheckResult` and flags an issue at `FALSE_CLOSING_ISSUE_THRESHOLD` (2+, since the preclose gate legitimately produces one false close per session). `report.ts`'s aggregate summary line now sums `deterministicFalseClosingCount` alongside the existing diagnosis-leak count. Tests added to `tests/historian-eval/deterministicChecks.test.ts`; branch `feat/historian-eval-false-closing-counter`.

### 2026-09-07 · Claude Code · `/rnd/historian` runs table pending-eval indicator + auto-refresh
- Did: the Signals cell in `HistorianRunsView.tsx` now shows an amber "analysis pending" badge (status `pending`/`queued`), a rose "analysis failed" badge (`error`), or a slate "no analysis (short transcript)" badge (`insufficient_transcript`) whenever `resolveDifferentials` has no `final` source yet — previously the cell showed nothing during the worker's ~60-120s post-interview eval window, reading as "no differential ran." The view now also auto-polls `/api/ai/historian/runs` every 15s (skipping ticks while the tab is hidden) while any loaded run is pending/queued and its `queued_at`/`created_at` is under 15 minutes old, via a new exported `hasRecentPendingRun` helper. Test: `tests/historian/HistorianRunsView.pending.test.tsx` — badge rendering per status plus the pending/window/resolve lifecycle of `hasRecentPendingRun` (this repo's test suite has no jsdom/RTL, so the polling *decision* logic is unit-tested directly rather than mounting the component with fake timers, consistent with every other `HistorianRunsView`/`DifferentialCard` test using `renderToStaticMarkup`). `npx vitest run tests/historian tests/historian-eval`: 1019 passed / 16 skipped, no regressions. `npx tsc --noEmit` and `npm run lint` clean for changed files (two pre-existing `no-explicit-any` lint errors in this file predate this change). Branch `fix/historian-runs-pending-indicator`, PR open against main, not merged (Amplify `main` is a patient/pilot-facing production surface).
### 2026-09-06 · Claude Code · Exhaustive triage scoring test headroom (vitest timeout flake)
- Did: partitioned `tests/triage/scoringExhaustive.test.ts` — the single `it` over 5^5 score vectors × 2^3 booleans (25,000 vectors) ran 1.1–2.0 s idle and timed out at 5.5 s in a contended full-suite run against vitest's 5 s default. Now 25 `it.each` slices keyed by (symptom_acuity, diagnostic_concern), 1,000 vectors each, plus one case proving the slices cover the 5×5 grid exactly once. Per-vector assertions are verbatim; vector set identical (25 × 1,000 = 3,125 × 8). Slowest slice ~85 ms alone, 182 ms inside the full suite. No timeout or config change.
- Profiled first: object construction + both scoring calls cost 4–30 ms for all 25,000 vectors (`node --cpu-prof`, and an in-vitest probe: 3.5–20 ms without `expect`, ~980 ms with). ~99% of the runtime is vitest's per-vector `expect()` machinery, so hoisting scoring work would have bought nothing — hence partition, not micro-optimization.
- Verified: 12 scoring mutants (each floor threshold, red-flag override, an added functional_impairment floor, schedulingLocked, dataQuality swap, carePathway, emergent early-return) — old and new test agree on every verdict (12/12 caught by both, identical verdicts, 0 survivors). Full suite: 3,510 passed / 0 failed / 25 skipped across 242 files (4 skipped) in 24.2 s; `tsc --noEmit` and eslint clean.
- Found, not fixed: `tests/triage/floorParity.test.ts` is a single `it` over 6,250 vectors — same shape, ~4× smaller; candidate for the same slicing if it ever shows up in a slow run.
- Files/links touched: `tests/triage/scoringExhaustive.test.ts`; branch `fix/triage-scoring-exhaustive-headroom` (PR #223).
- Scratch, uncommitted, deliberately left in place per Steve: `tests/triage/_origScoringExhaustive.test.ts` and `tests/triage/_origcheck/scoringExhaustiveOrig.test.ts` — both byte-identical to the origin/main version of the test (`git show origin/main:tests/triage/scoringExhaustive.test.ts`), used for the side-by-side mutation run. The mutation sweep deleted them once before the deny reached the session; restored from that content. Do not commit them; `/end` stages all untracked files, so remove or ignore before running it.
### 2026-09-06 · Claude · vitest live-Bedrock guard + long-packet test stubs
- Did: `captureSafetyOutcome` (`tests/triage/longPacketPartialSafetyHold.test.ts`) and `mapperOutcome` (`tests/triage/longPacketWorkerSafetyHold.test.ts`) now pass a synthetic `reduceNarrative`. Without it, `reduceLongPacketModelOutcomes` fell back to the real `runLongPacketNarrativeReducer` → `invokeBedrockClinicalTool` → `BedrockRuntimeClient.send()` whenever the synthetic signal produced a narrative unit, and the pipeline's `catch` hid it as `narrative_reducer_failed`. Three tests went 4160 / 1023 / 2123 ms → 3–41 ms. Added `tests/setup/bedrockGuard.ts` (see Decisions): with the guard and without the stubs exactly those three tests fail, naming `BedrockRuntimeClient.send(InvokeModelCommand)`. A four-lens adversarial review (bypass, false positives, fix semantics, CI parity; three refuters per finding) found one real gap — the guard would have blocked the documented `HISTORIAN_EVAL_LIVE=1` historian live gates — so the guard honors that flag too; the opt-in paths were verified offline (guard subclass present without a flag, absent with either flag).
- Verification: both files × 3 runs green (64/64); full `npx vitest run --exclude "tests/simulated-patients/**"` green except the pre-existing `sourceExtractionAuthority` 5 s timeouts (~2.9 s alone, identical with and without the guard; over 5 s only under load) that PR #220 fixes. `bedrockClinicalOutput` and `independentDdx` (test-file SDK mocks) still pass under the guard. Guard file type-checks under the repo tsconfig + `@types/node` and lints clean (CI's `tsc` excludes `tests/`).
- Next: merge #220, then this PR's CI is deterministic. Extend the guard to other outbound clients (S3/DynamoDB/Secrets Manager) only if a similar silent-network path shows up.

### 2026-09-06 · Claude Code · Triage authority validation cost (vitest timeout flake)
- Did: root-caused the once-off failure of `sourceExtractionAuthority.test.ts › retains verified model-only same_day_clinician_review …` — deterministic test running ~3.9 s inside a full suite against vitest's 5 s default (877 ms alone). Cost was 24 emergency-gateway scans of a 9-chunk packet per test: the authority validator scanned the rebuilt plan twice (envelope + model-artifact validation) and `selectEvaluationSpan` reclassified adjacent-sentence experiencers once per rule. Fix: pass the envelope's gateway through (single scan, with a plan-pairing guard that fails closed) and a per-scan experiencer cache in the gateway. Byte-identical gateway output over 3,372 corpus inputs plus a 177-case adversarial adjacency harness; in-suite test time ~1.0–1.7 s. Regression contract: exactly one scan per validation.
- Found, not fixed (separate task chip): `longPacketPartialSafetyHold.test.ts` `captureSafetyOutcome` and `longPacketWorkerSafetyHold.test.ts` `mapperOutcome` omit `reduceNarrative`, so those tests make a real AWS Bedrock call (bimodal 56 ms / 4.07 s, next latent timeout). Also noted: `hasPriorYearDocumentDate` falls back to wall-clock year when `decisionAsOf` is absent (unreachable by these fixtures).
- Files/links touched: `src/lib/triage/emergencyGateway.ts`, `src/lib/triage/longPacketIngestion.ts`, `src/lib/triage/sourceExtractionAuthority.ts`, `tests/triage/sourceExtractionAuthority.test.ts`; branch `fix/triage-authority-validation-cost` (PR opened this session).

### 2026-09-06 · ChatGPT · Refreshed clinical release against newer main and live metadata
- Did: reconciled DONE and Triage Coordination's earlier PR206/061 deployment with the still-local clinical correction; integrated newer committed main through `bdcef4a` without runtime conflicts. Resolved only HANDOFF.md by preserving both owners' entries. Inspected live hosting, schema, aggregate counts and backup metadata without reading referral records or identities.
- Files/links touched: HANDOFF.md merge resolution; refreshed approval receipt, selected live metadata and PR body in the owning Codex task. Original clinical and migration evidence remains preserved in its prior manifest.
- Decisions: publish the clinical correction through a PR to `blondarb/OPSAmplehtml` main and the existing `app.neuroplans.app` Amplify lane only under accepted direct destination approval. Apply only migration 063 after immediate transaction preflight and preservation checks. Preserve active work in the saved checkout and the separate capacity branch. No SAM/IAM changes, study creation, reviewer grants, paid evaluation or clinical scheduling activation are included.
- Verification: live 061 governance is present and enabled; all three 063 schema markers are absent; existing data remains 52 cases / 26 reviews / 312 legacy AI runs in two archived studies. Ten archive-reader memberships remain; no fresh study, attempts or receipts exist. Backup is encrypted/available with seven-day retention and point-in-time recovery current to 23:09 UTC. Main `bdcef4a` has successful repository CI and Amplify job 490 succeeded. Combined-source TypeScript passed. Previous local clinical/migration/build evidence is retained only for its exact unchanged scope; remote CI for the combined candidate and deployed S1–S5/375px acceptance remain required.
- Open questions / needs Claude: the prior GitHub push was rejected by automatic approval review for exact destination authorization; do not retry indirectly. Earlier AWS-expired statements below are historical and superseded by working read-only access. Independent neurologist labels, adjudication and clinical validation are still outstanding.
- Next: present the locally committed integration as one complete source/migration/destination/checks approval, then proceed only when that direct approval is accepted. Recheck main and live state immediately before publication; never rerun applied 061 or overwrite newer owners' work.

### 2026-09-06 · ChatGPT · Clinical release integration prepared; publication held
- Did: integrated approved clinical policy source on current main, preserved Historian changes, renamed triage migration 062 to 063 without changing its SQL, and corrected two old UI assertions to require immediate-review wording and hidden outpatient workup.
- Files/links touched: migration 063, affected review-route error/import references, output-panel tests, clinical-policy integration note, HANDOFF.md. Release evidence and receipt are in the owning September 5 Codex triage task outputs.
- Decisions: publish to the existing POC only after the exact external destination authorization is accepted and migration preflight/CI/deployed checks pass. Automatic review rejected the GitHub push; do not bypass or delegate that write. No new infrastructure, paid model calls, study assignments or clinical scheduling activation. Keep capacity changes separate.
- Verification: 43 affected review API tests and disposable PostgreSQL governance passed. Full local source run: 3,482 passed, seven failures, 28 skipped; two old assertions were corrected and five loopback tests rerun with socket permission. All 30 tests in the two affected files then passed. TypeScript and sanitized Next production build passed; original actual-PG completion and 375px UI evidence remain tied to unchanged relevant source. No clean full-suite rerun or remote candidate CI is claimed. Existing results-route lint debt remains.
- Open questions / needs Claude: direct publication approval is pending, sandbox SSO expired, live 063 schema/backup preflight and deployed S1–S5/375px acceptance remain. Existing 061 was already applied September 5; do not repeat it. Independent clinician labels, adjudication and clinical validation remain outstanding. Preserve the untracked HANDOFF 2.md sync copy; it was not staged or deleted.
- Next: resume the prepared publication at the approval/access gate; verify current main before any external write, use only migration 063, and preserve all validation records. Do not incidentally deploy the separate Historian SAM changes.

### 2026-09-05 · ChatGPT · Clinical policy repairs and independent counterexample labels
- Did: implemented Steve's approved clinical corrections and counterexample suite in an isolated checkout from `8582bde`. Shared action-aware presentation now covers polling, batch/results, reports, consult persistence and notification text; holds suppress outpatient workup/routing. Added digest-bound source chronology and a server-internal, confirmation-bound MS window prototype with no HTTP activation. Corrected scorer guidance and governed MS destination; ordinary notes retain the full source and packet evaluation reuses production projection helpers.
- Files/links touched: clinical policy/persistence/validation source and tests; migration 062; 25 synthetic notes covering 20 groups, source-only study import and coverage map; `docs/triage-validation/2026-09-05-clinical-policy-corrections.md`; `qa/runs/RUN-2026-09-05-CLINICAL-POLICY.md`. User-facing receipt and exact commit/evidence manifest are in the September 5 Codex triage project's `outputs/` folder.
- Decisions: preserve existing weights, cutoffs, archives and immutable original labels. Record action/timing/services as canonical new reviewer labels, with a constrained legacy comparison tier. Results distinguish these labels from older descriptive tier statistics and explicitly do not claim AI clinical validation. Keep failed model branches held; local availability cannot alter clinical timing. Independent review found and resolved contradictory labels, packet projection drift, the missing prompt destination and combined comparison cases.
- Verification: 1,824 triage tests passed; all 12 actual-PostgreSQL completion tests passed separately; disposable governance/062 checks passed with historical preservation. Final affected UI/results rerun: 47 passed. TypeScript and final sanitized Next production build passed. Browser with all APIs mocked passed save/advance, reset, locked original, correction request and 375px no-overflow; screenshots visually inspected. Changed-file lint has zero new findings: the same 50 existing results-route explicit-any errors, zero warnings. No paid model runs or real clinician labels were used.
- Open questions / needs Claude: names of two independent neurologists and an adjudicator remain outstanding. Migration 062, study setup/access, adjudication and canonical AI comparison remain open. This local correction made no push, deployment, live database change, membership grant, clinical intake, scheduling change or external-service write. Earlier POC deployment/061 status is recorded in Snapshot; do not repeat it.
- Next: review the local commit and clinical-policy package, then perform separately authorized integration and source-only study setup. Use the 25 cases to test the policy and reviewer instrument before a distinct governed retrospective cohort. A dated local Asana queue block records source readiness and remaining gates; no Asana API write.

### 2026-09-06 · Claude · /launchpad project index
- Did: added `src/data/launchpad.json` (validated by `src/lib/launchpad/data.ts`; a bad kind/stage/auth key fails `pnpm test`), `src/lib/launchpad/asana.ts` (PAT from `ASANA_PAT` env → Secrets Manager `sevaro/asana/pat`, plain string or `{token}`), `GET /api/launchpad/open-items`, `/launchpad` page (`LaunchpadView`), 9 unit tests, IAM helper script for Steve. Verified locally on :3017 (page renders, sort = High priority then most mature stage, unauthenticated state explains itself); real Asana pull verified out-of-process: 12 cards, 63 open items, 567 ms.
- Gotcha: `globals.css` sets `a { color: var(--surface-primary) }`, so every anchor inside a dark card needs its own Tailwind text colour — `inherit` does not survive.
- Next: Steve grants the SSR role read on `sevaro/asana/*`; fill the three unknown URLs in launchpad.json; refresh the file at each `/end` when a URL changes.
### 2026-09-05 · ChatGPT · Existing evaluation recovery and reviewer simplification
- Did: reconciled existing evaluation infrastructure rather than replacing it; merged current main into the isolated branch; renamed the unapplied triage migration to 061 because Localizer owns 060. Implemented source-only short review/save-next/locked correction requests, explicit physician versus nurse roles, historical read-only archive mapping, immutable source-bound attempts/receipts and isolated synthetic scorer/ensemble execution. Narrow guideline prompt repairs, organization capability routing and a disabled review-only translation prototype are included.
- Files/links touched: source commit `68f437a`; merge `7375e21`; migration 061; validation routes/UI/scripts/tests; organization routing and translation modules; `docs/triage-validation/2026-09-05-administrative-package.md` and JSON examples.
- Decisions: historical ratings are preserved with unknown provenance rather than relabeled independent. Blanket seeder holds were refined to draft source import; clinical intake is not used for evaluation because persistence can enqueue notifications. Exact built-in synthetic sources only; no paid model calls. Model receipt identity now matches execution. Clinical urgency never decreases to fit local availability; translations never become authoritative automatically.
- Verification: independent architecture consultation and one final safety review completed; identified input/model/receipt binding issues were repaired. 62 focused tests passed plus targeted 8-test evaluator rerun including one new demographics case. TypeScript and isolated production build passed; build required a results Suspense correction. Disposable PostgreSQL v4 checks passed (historical preservation, roles/phases, full-input hash and attempt binding). Mocked browser verified save/advance, locked label and 320px no-overflow; screenshot capture timed out twice, so no screenshot visual signoff claimed. Lint remains blocked by 50 preexisting results-route explicit-any errors and one untouched admin warning.
- Open questions / needs Claude: review local source before integration; actual historical inventory/mapping, migration, grants, adjudication UI/storage, corrected statistics, complete intake/OCR evaluation and deployed security remain gated. Source commit in receipts remains operator-asserted pending build/artifact verification. No PHI, push, deployment, live migration, actual user grant, paid inference or external service write occurred.
- Next: use the administrative package for a short synthetic reviewer exercise after separate setup approval; then complete approved independent validation and one partner integration. Do not mark clinically validated. A dated local Asana queue block records the corrected state; no Asana API write.

### 2026-09-05 · ChatGPT · Triage security and blinded-validation foundations
- Did: reviewed triage readiness, then implemented explicitly authorized local foundations from fresh `origin/main` `4813ff0`. Patched Next.js/eslint-config-next to 15.5.24; secured patient lookup and provider messages/threads with tenant and participant checks; duplicate full-name/DOB lookup fails closed. Billing export now returns authorized 503 until its tenant schema is verified.
- Files/links touched: local commits `5ad8ae1`, `c4ce9fb`, `79c7979`, `d49e693`; migration 060, triage validation routes/UI/tests, emergency gateway v4, system prompt, and `docs/triage-validation/2026-09-05-validation-packet.md`. Worktree: `work/triage-foundations` under the September 5 Codex project; full path is in its implementation report. Review and final evidence are in the sibling project `outputs/` folder.
- Decisions: study membership is explicit and tenant-bound, cases freeze after draft, labels are insert-only, results remain blinded until complete independent ratings and formal unblinding. Legacy scoring-only auto/seed/rerun endpoints are held with 409; no replacement model runner was activated. Raw old-date headers never lower emergency authority. Removed the ambulatory/stable cauda-equina outpatient exemption. One independent safety review identified additional fixes, which were addressed before completion.
- Verification: 48 focused access/validation tests plus 48 production-path/PDF integration tests passed; gateway worker reported 446 affected tests passed. TypeScript and isolated production build (`next build --no-lint`, cloud credentials disabled) passed. Actual migration trigger checks passed in a fresh synthetic PostgreSQL fixture; test server stopped. Separate lint: existing results route has 50 explicit-any errors both before and after this change, plus one existing UI warning; new safeguards have no lint errors. No live schema, clinical data, paid model run, or installed/deployed acceptance was used.
- Open questions / needs Claude: source-only local commits, no push or PR. Migration is unapplied and creates no grants/mappings. Verify live schema and define authorized provisioning before activation. Full-pipeline receipts/runner, structured short-form/adjudication, statistical correctness, trusted document-time metadata, OAuth/secret handling, OCR/partner integration and specialty-specific clinical validation remain separate work. Passing tests does not validate triage accuracy.
- Next: review local commits, select clinical owner/two raters/adjudicator, implement the missing evaluation runner and instrument, then separately govern study activation and real-note access. Do not re-enable legacy model endpoints or assume partner trial readiness.

### 2026-08-03 · Claude Code · Part 4 (/patient/historian) redesign — SHIPPED (and Parts 2–3 pushed after Steve's "ship it")
- Context: Steve signed off on the /triage redesign ("ship it") → pushed 0c7ef1f; Part 4 followed the same day and shipped on his second "ship it".
- Did: historian rebuilt on the shared nn system as a 4-step patient flow (Your visit → Consent & identity → Interview → Summary) with a persistent step indicator, 17px patient type scale, ≥48px targets, one primary action per screen. Consent+identity gate renders as an in-page step via a new `presentation="page"` variant on HistorianConsentDisclosure (the dark modal branch is byte-identical and still serves /consult EmbeddedHistorian); startSession()/mic remains reachable only through onConfirm — re-verified in-browser (Begin disabled until checkbox+name+DOB; mic never requested; Cancel returns to step 1). Interview screen extracted to a pure presentational component (src/components/historian/HistorianInterviewStep.tsx) so it can be rendered without a live voice session: question always as text ("Question N of up to 23", derived from assistant turns), live "What we heard" box (current or last patient utterance), mic state row (listening / assistant speaking / wrapping up + elapsed), correction hint ("say 'please repeat that'…"), Show Transcript + End Interview. Report view gains theme="clinical" (light chrome + patient tab; /consult keeps dark) and the patient surface now DEFAULTS to the Patient Report tab (plain-language summary first, per brief Part 4). Engine selector hidden behind ?internal=1 (verified hidden by default, visible with the param; ?voice=nova deep link unaffected). MD View toggle + LocalizerPanel REMOVED from the patient surface and enableLocalizer set false there (physician-only diagnostic content must not render on the patient page; /consult keeps its own localizer). Safety-escalation crisis screen deliberately untouched.
- Deferred (need logic/hook changes, not presentation): true Pause and a Repeat-question button (useRealtimeSession exposes neither — the voice agent handles spoken "please repeat" conversationally, which the hint surfaces); correct-before-submission on the summary (save fires on complete today); dark inner cards of the physician tab inside the clinical theme (IntakeReviewSection/transcript viewer restyle is a follow-up).
- Verification: tsc clean; eslint clean (one PRE-EXISTING exhaustive-deps warning in NeurologicHistorian, untouched); full vitest 2,743/0. NOTE for future sessions: a mid-verification run showed 19 failures across 6 heavy triage-engine files (scoringExhaustive, sourceExtractionAuthority, route safety) — all environmental CPU starvation from the running `next dev`; each passes in isolation and the full suite is green with the dev server stopped. Diagnose before touching assertions. Browser: steps 1–2 verified live (gate logic stepwise), interview step via temporary static harness (deleted — it wrote to public/ which deploys), 375px no-horizontal-scroll on both pages, zero console errors.
- Next: run qa/runs/RUN-2026-08-03-001.md (now includes the historian delta) against the deployed site; follow-ups above + physician-tab inner restyle; validate pages remain dark by design.

### 2026-08-03 · Claude Code · Neuro Navigator redesign Parts 2–3 (/triage) — COMMITTED LOCALLY, NOT PUSHED (awaiting Steve's sign-off)
- Context: same-day continuation after the Part 1 correctness fixes deployed (verified live). Parts 2–3 of docs/design/redesign-brief-triage-historian.md; visual target docs/design/neuro-navigator-reference-mockup.html (both copied into the repo this session). Part 4 (/patient/historian) NOT started — Steve's instruction: sign-off on /triage first.
- Part 2 (shared design system): src/styles/neuro-navigator.css — all tokens + component classes scoped under `.nn` so the rest of the app is untouched; light clinical palette, one teal accent, lightness-ordered tier scale t1–t7 (+washes), 5-size type scale, hairline elevation, :focus-visible rings, reduced-motion guard. Every ink/wash pair computed ≥4.5:1 (script-verified; ink-3 darkened to #666f78 to clear the page background). src/lib/triage/tierPresentation.ts = presentation-only tier map ("Tier N of 7", names, color vars) + published dimension weights for display; TIER_DISPLAY stays for legacy dark surfaces (validate pages, batch panels, compact chips).
- Part 3 (/triage): two-pane layout (input left, recommendation right, stacked <860px; extraction review takes a full-width row); input panel now mounted UNCONDITIONALLY and visible beside progress/result (source_replacement lifecycle already resets state — verified before relying on it). Result pane is rubric-forward per the brief: red flags above the score, tier card = number+name+timeframe (never color alone), "How this was scored" card with weight superscripts/bars/rationales + "Weighted total X/5 → Tier N of 7 — Name" + confidence, routing kv card, "Start before the visit", stability caveat, actions at bottom. Emergent = full takeover restyled (t1 wash, "Emergent — do not schedule"). Insufficient-data renders as Tier 7 of 7. New TriageProgress: staged display for the 50–60 s call (elapsed clock, named stages, long-packet real progress via detail; SSE still off per PR #111 gotcha). AlgorithmModal light + Esc-close + aria, tier rows use tier tokens, added the Tier 7 row (presentation of existing insufficient_data behavior — thresholds untouched). Executor subagent converted the 9-file tail (loaders, preview modal, upload zone, extraction review, ingress safety alert, safety review, emergency action, physician override) — copy byte-identical, verified.
- Tests: 3 source/parity contract tests updated to the new structure WITHOUT weakening (old anchors referenced removed markup): canonicalFullResultParity + canonicalProductionPathParity now pin 'How this was scored'/'Weighted total'; pageSourceContract pins the stronger unconditional-mount + review-only-hidden rule. NOTE: executor initially misdiagnosed the parity failures as pre-existing via a stash-baseline that reverted only ITS files — the failures were from the driver's TriageOutputPanel heading removal. Fixed lint (unused needsExtraction after button-color unification).
- Verification: tsc clean; eslint clean on all touched files; FULL vitest 2,743 passed / 0 failed (only exclusion: tests/simulated-patients, needs a live app); browser: two-pane desktop, 375px stack with zero horizontal scroll (E1), keyboard focus ring = 2px accent (checked computed style), zero console errors; result/emergent/algorithm/progress states screenshotted via a temporary static-render harness (deleted after capture — it wrote to public/, must never be committed or it would deploy).
- QA: qa/runs/RUN-2026-08-03-001.md drafted (delta mission brief per qa-rules; execute S1–S7 + T-series + E1 after this deploys).
- Next: (1) Steve sign-off on /triage → push (Amplify deploys) → run the QA brief; (2) Part 4 /patient/historian to the mockup (consent/identity gate from Part 1 restyles into the step flow; engine toggle behind internal route per brief); (3) consider porting the validate pages to nn later — out of scope now.

### 2026-08-03 · Claude Code · Partner-demo correctness fixes on /triage + /patient/historian (redesign brief Part 1, §1.1–1.4)
- Context: both pages are shown to Mayo Clinic Aug 7 and Novant Health Aug 12. Brief's Part 1 correctness fixes only — no visual redesign (Parts 2–4 not started), no triage logic/rubric/weights/threshold/override changes.
- Did: (1.1) Removed the "calculates a triage tier deterministically" claim from /triage intro and AlgorithmModal; replaced with accurate copy (fixed five-dimension rubric, published weights, fixed tier boundaries + red-flag overrides; borderline notes may score differently across runs). Added a visible "Stability" caveat card to the result pane under Dimension Scores. Also corrected "deterministic/near-deterministic temp=0" claims on /triage/validate results + admin pages (temp=0 = lowest variance, not repeatable on borderline cases — matches the engineering record). (1.2) Upgraded HistorianConsentDisclosure into a full consent + identity gate: explicit unchecked consent checkbox, emergency redirect box (stroke symptoms/seizure/thunderclap/new weakness → 911/ED), draft-reviewed-by-clinician + not-a-diagnosis statement, and full name + DOB fields required via new `requireIdentity` prop (passed on /patient/historian; /consult EmbeddedHistorian keeps checkbox-only gate). startSession()/mic remains unreachable until confirm. Typed identity is attestation-only: held in component state, never sent to the voice model, transcript, or DB (no BAA on either voice engine — deliberately no new data path). (1.3) Deleted TriageHelpGuide ("Testing Guide & What's Next" + both GitHub links) from /triage; retitled "AI Triage Tool"→"Neuro Navigator" and removed the "Demo" badge on both pages' FeatureSubHeader; homepage journey card "AI-Powered Triage"→"Neuro Navigator". (1.4) Paste field now capped client-side at FILE_CONSTRAINTS.MAX_TEXT_LENGTH (50,000) with live counter (was displayed against 5,000,000) and a no-truncation block+message when over; oversized-upload rejection already existed client-side (10MB in applyReferralFileSelection) — unchanged.
- Files: src/app/triage/page.tsx, src/components/triage/{AlgorithmModal,TriageOutputPanel,TriageInputPanel}.tsx, src/app/triage/validate/{results,admin}/page.tsx, src/components/{HistorianConsentDisclosure,NeurologicHistorian}.tsx, src/components/homepage/journeyData.ts, tests/triage/triageInputPolicy.test.ts (contract extended to pin the new cap); deleted src/components/triage/TriageHelpGuide.tsx + tests/triage/triageHelpGuide.test.tsx.
- Verification: tsc clean; full vitest 2,743 passed / 0 failed (tests/simulated-patients/runner needs a live localhost app — pre-existing environmental skip/fail); browser-verified both pages on local dev: over-limit paste (54,400 chars) blocks submit with message and zero truncation, consent gate blocks Begin until checkbox+name+DOB, mic never requested pre-consent, zero console errors. Pre-existing lint error (unused Trash2 import in validate/admin) left untouched.
- Decisions: paste cap blocks submit rather than truncating (preserves the never-silently-drop-a-critical-tail property; oversized text routes to the upload path, which still runs the long-packet emergency screen). Validate-page temp=0 copy now says "lowest variance, not fully repeatable on borderline cases". Voice-engine toggle + "Try a Demo"/"Load Sample" labels + validation-study link left in place — Parts 2–4 scope.
- Next: brief Parts 2–4 (shared design system, /triage layout to the reference mockup, /patient/historian large-type patient flow, engine selector behind internal route). Reference mockup + brief live in ~/Downloads (neuro-navigator-reference-mockup.html, redesign-brief-triage-historian.md) — copy into docs/ before Part 2 work.

### 2026-07-21 · Claude Code · Synapse 3.0 "One Surface" concept published
- Did: published `/concepts/synapse-3/unified-surface.html` (self-contained, fictional data) + new first homepage Concepts card (Orbit icon); tweaked old "Synapse 3.0?" card dek to mark provenance. 7 role lenses on one time-spine incl. mid-session additions neuro hospitalist + EEG reader (real anchor: EEG Report Builder); Dr. Vega demos the legal multi-role day; "Daylight" light design language (full departure from 2.0). Locally verified over http (all lenses, toggles, canvas swaps, zero console errors).
- Next: Amplify auto-deploys on push; session log mirrors to Asana card "Unified app / Synapse 3.0". Concept only — nothing wired to engines.

### 2026-07-19 · Claude Code · Triage 403 fix + false-hold root cause and fix
- Did: (1) Fixed "Access denied" on /triage — `clinical_access_memberships` (migration 048) was EMPTY in prod; provisioned both steve@sevaro.com Cognito subs as `clinician`, tenant `default`, directly in RDS. No provisioning path exists yet (no seed/admin UI); teammates still 403 until provisioned. (2) Root-caused the "Human review required — scoring blocked" false holds: the safety-extraction prompt permits `no_time_critical_signal` alongside non-time-sensitive critical unknowns, but the validator demanded `same_day_clinician_review` for ANY unknown and discarded the branch on mismatch → fusion `undetermined` → unrecoverable hold (adjudicator is downgrade-forbidden by design). Reproduced live with the exact prod referral texts (Gutierrez: 3 benign unknowns, 0 signals, deterministic kill; Sandoval: intermittent scorer JSON flake). Fix: validator now floors `care_pathway` from the model's own signals (emergency signal ⇒ emergency_now; immediate-review signal ⇒ ≥ same_day) instead of throwing, never downgrades a stated pathway, logs when flooring fires; prompt gained the matching consistency rule; scorer gets one retry on non-timeout failures.
- Files: `src/lib/triage/modelSafetyExtraction.ts`, `src/lib/triage/modelSafetyExtractor.ts`, `src/lib/triage/processTriageInBackground.ts`, `tests/triage/modelSafetyExtraction.test.ts`, `CLAUDE.md`.
- Verification: triage suite 1,760 passed; tsc + lint clean; live Bedrock repro of both failures now passes; offline sentinel releaseGateEligible; live sentinel run + Bedrock cross-family review (gpt-oss-120b + DeepSeek-R1) same session — both models' Critical/High claims verified as false positives against the code (terminal failedModelBranch catch intact; flooring only ever raises), their observability suggestion adopted (floor warn log).
- Decisions: benign, non-time-sensitive unknowns no longer force a same-day hold (restores the prompt's signed-off intent; no tier definitions changed; understated pathways now floor upward instead of dying into a generic hold — strictly safer for the emergency direction). Adjudicator remains downgrade-forbidden — untouched, flagged as an open dial.
- Also: reason-specific denial messages on all 13 triage routes (`clinicalAccessDeniedMessage()` — 401 "Please sign in" vs 403 "not provisioned" vs 503), because a signed-out Chrome profile looked identical to a provisioning failure; extraction quality spot-checked in prod (Gutierrez summary is note-specific, confidence high) — "triager doesn't read the note" impression was the hold wall, not the extractor.
- Next: provisioning path for `clinical_access_memberships` (seed or admin script) before team testing; consider moving the outpatient scorer to the strict tool-output path like the safety extractor.
- Did: verified the intended product journey against current code and authoritative data/governance sources. The integrated `/consult` flow now embeds triage, AI Historian, patient tools, optional SDNE linkage, and a unified source-labeled report. Confirmed that historian context currently includes triage priority, chief complaint, summary, red flags, and prior intake—not the full raw referral. Reviewed current MIMIC-IV-Note and n2c2 access steps and federal QI/research and HIPAA deidentification guidance. No clinical data were accessed and no product code or deployment changed.
- Files/links touched: `HANDOFF.md`; read `src/lib/consult/contextBuilder.ts`, `src/components/consult/ConsultPipelineView.tsx`, `src/components/consult/HistorianStepPanel.tsx`, `src/components/consult/EmbeddedHistorian.tsx`, `src/lib/consult/report/report-builder.ts`, `src/app/sdne/page.tsx`, and relevant project documentation; reviewed official PhysioNet/MIMIC, Harvard n2c2, HHS OHRP, and HHS OCR guidance.
- Decisions: treat the overall product story as referral triage → informed patient history → optional digital exam → unified clinician preparation/documentation. Do not describe the full-note-to-historian or final desktop-scribe handoff as fully proven yet. Treat 200–500 retrospective referrals as an initial silent validation study, not definitive clinical validation, and obtain a formal institutional QI-versus-research/IRB determination. Do not place MIMIC/n2c2 notes into the current hosted model pipeline until the applicable DUA and compute arrangement are confirmed compatible.
- Open questions / needs Claude: select the institutional IRB/HRPP sponsor and validation partner; decide whether the historian needs full raw text or bounded source-grounded retrieval; verify the consult-report-to-Sevaro-desktop-scribe handoff; confirm live SENSE/SDNE result return and current demo latency.
- Next: create two concise artifacts if Steve approves: (1) MIMIC/n2c2 access and compliant-compute checklist, and (2) a one-page retrospective validation/QI-IRB concept with endpoints, labeling plan, and data-flow diagram. Then scope a polished five-minute administrator/finance demo using synthetic data only.

### 2026-07-13 · ChatGPT · Real clinical-note dataset research
- Did: searched authoritative sources for real, deidentified clinical notes that could strengthen triage testing. Confirmed that MIMIC-IV-Note and n2c2/i2b2 provide controlled-access real notes, but primarily hospital discharge summaries, progress reports, and radiology reports rather than outpatient neurology referrals. Found published use of real outpatient neurology clinic letters, but no downloadable public corpus; identified NeuroDiscovery AI as a possible governed collaboration source. No notes or patient data were downloaded.
- Files/links touched: `HANDOFF.md`; reviewed PhysioNet MIMIC-IV-Note, Harvard n2c2 data sets, the peer-reviewed ExECT neurology clinic-letter study, and NeuroDiscovery AI's data-access materials.
- Decisions: use MIMIC-IV-Note and n2c2 only as real-world documentation robustness sets, not as proof of outpatient referral-triage accuracy. Treat true validation as a governed partner study using deidentified historical referral notes with independent neurologist labels. Do not use random internet notes or weak-provenance scraped collections as validation data.
- Open questions / needs Claude: whether Steve wants a one-page access/application checklist and a partner validation protocol; legal, privacy, DUA, and possible IRB requirements must be confirmed before receiving partner data.
- Next: apply for MIMIC-IV-Note and selected n2c2 corpora for offline robustness testing; separately recruit one or more neurology partners for a silent retrospective validation set that matches the intended referral workflow.

### 2026-07-13 · ChatGPT · Triage demo and validation-partner readiness review
- Did: verified the curated triage safety architecture is contained in current `origin/main`; confirmed the live `/triage` page is publicly visible but both anonymous create and extract calls correctly return 401; reran the full offline sentinel on current code (29/29 evaluable synthetic cases acceptable, all software gates pass) and 404 focused sentinel/emergency/route-safety tests passed. Reviewed the July 12 classifier bake-off and production-readiness audit. No product code, database, deployment, or access policy changed.
- Files/links touched: `HANDOFF.md`; read-only review of triage code, `docs/clinical-safety/`, `qa/triage-sentinel/`, `qa/triage-validation/BAKEOFF_2026-07-12.md`, and the live `app.neuroplans.app/triage` surface. Generated a temporary sentinel report under `/tmp` only.
- Decisions: the tool is ready for controlled synthetic demonstrations and to begin governed validation-partner work; it is not ready for anonymous arbitrary-text access or clinical reliance. Preserve clinician authorization and add a separately scoped demo/partner access boundary rather than opening the clinical endpoints.
- Open questions / needs Claude: migrations 048 and 050–055 are documented as not yet applied; AWS credentials were unavailable to inspect the Amplify job directly; checked-in live sentinel reports cover only 1–2 case subsets despite a CLAUDE.md summary claiming a 26/26 live run; the classifier remains deliberately over-triage-prone and the 26-case expected labels still need independent clinical sign-off.
- Next: if Steve approves, design an expiring signed demo link limited to built-in synthetic referrals, then a separate authenticated partner-validation workflow with versioned cases, independent labels, feedback capture, and no clinical routing or patient binding.

### 2026-07-13 · ChatGPT · AI intake outpatient scenario expansion
- Did: reviewed the four existing synthetic outpatient intake scenarios and the recently improved shared Henry/Nova voice-intake flow. Started design work for adding two nonredundant outpatient scenarios; no application code or deployment changed.
- Files/links touched: `HANDOFF.md`; reviewed `src/lib/historianTypes.ts`, `src/components/NeurologicHistorian.tsx`, and recent Historian/intake commits.
- Decisions: Steve approved new tremor/parkinsonism and new memory/cognitive concern as the two additional scenarios. Keep this as a small scenario-library change on the existing public intake route; preserve synthetic-only use and the current consent flow.
- Open questions / needs Claude: awaiting Steve's approval of the exact card/guidance design; no application code has changed yet.
- Next: approve the concise design, then document it and implement with focused tests.

### 2026-07-13 · ChatGPT · AI voice intake agent public demo link check
- Did: confirmed the recently improved AI voice intake agent (internally named the AI Historian) at `https://app.neuroplans.app/patient/historian` is live and already bypasses Cognito via the existing `/patient` public-route rule; confirmed the page shows synthetic scenarios and supports `?scenario=headache_new` for a preselected demo. No product code or deployment changed.
- Files/links touched: `HANDOFF.md`; reviewed `src/middleware.ts`, `src/app/patient/historian/page.tsx`, `src/components/NeurologicHistorian.tsx`, the Historian session/save routes, and the live site response.
- Decisions: the existing preselected scenario link is sufficient for Steve's immediate no-password demo. Treat it as synthetic-only: completed conversations are currently recorded/transcribed and written to `historian_sessions`.
- Open questions / needs Claude: for broad sharing, decide whether to add a dedicated `/demo/historian` boundary that rejects real-patient context, avoids clinical DB persistence/notifications, labels synthetic-only use, and adds abuse/cost controls.
- Next: Steve tests the preselected headache link on his phone; implement the dedicated demo boundary only if he wants a hardened, broadly shareable version.

### 2026-07-13 · Claude Code · Handoff system initialized
- Did: created HANDOFF.md + AGENTS.md; pointed CLAUDE.md at HANDOFF.md
- Next: both AIs read HANDOFF.md first, update it last
