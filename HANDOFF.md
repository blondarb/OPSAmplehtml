# HANDOFF — OPSAmplehtml

_Single source of truth for cross-AI handoffs. Claude (Cowork/Claude Code) and
ChatGPT/Codex read this FIRST and update it LAST. Plain language. No PHI (initials
only). No secrets._

## Snapshot (keep current)
- Status: Draft PR #181 now includes production runtime limits and the PHI-free contract harness at code commit `332a60e`: continuation through 26 exchanges, emergency finalization at 26, one wrap instruction at 45, and a silent hard stop at 60. Remote CI passed on that exact executable source. Nothing here is deployed, migration-applied, privacy/BAA-approved, or clinically validated.
- Driver this week: review the published continuation, then run the remaining live full-length synthetic Nova/ASR, persistence, worker, and physician-browser acceptance gates before any real-patient rollout.
- Lives in: https://github.com/blondarb/OPSAmplehtml.git

## Open threads / next actions
- [ ] Review the published Comprehensive runtime-guard update at `332a60e`. The PHI-free contract suite passes continuation through 26, emergency finalization at 26, one wrap at 45, and a silent output-suppressed hard stop at 60; these are deterministic policy/hook tests, not live Nova/ASR evidence.
- [ ] Apply migration 059 and the historian worker stack only in an approved non-production environment, then complete live Nova-only synthetic acceptance: full history coverage beyond 25 exchanges, semantic emergency recognition after turn 25, 45/60 wrap and save behavior, transcript/final-save persistence, worker retry/failure behavior, and physician-only differential browser delivery. The earlier startup/content-filter, referral-first, patient-reported-age-second, and no-premature-tool smoke passed with PHI-free synthesized audio. Do not deploy for real patients until the privacy/BAA boundary is approved.
- [ ] Create a governed real-note testing path: PhysioNet MIMIC-IV-Note credentialing, selected n2c2 access, and DUA-compatible compute that does not send restricted notes to an unapproved third-party API.
- [ ] Draft a silent retrospective AI-triage validation protocol and obtain a formal institutional QI-versus-research/IRB determination before receiving partner referral notes.
- [ ] Harden the executive demo into one synthetic post-discharge journey: referral triage → informed AI historian → optional SENSE/SDNE → source-labeled clinician prep report, with a precomputed fallback for latency.
- [ ] Decide whether the historian should receive the raw referral, a bounded source-grounded referral packet, or retrieval access; current implementation passes triage summary/priority/red flags rather than the entire referral text.
- [ ] Verify the remaining cross-product handoffs end-to-end: SENSE/SDNE result return, consult report to EHR/chart prep, and Sevaro Evidence Engine desktop-scribe consumption.

## Decisions log (append-only, newest first)

## Session log (append-only, newest first)
### 2026-08-21 · ChatGPT · Comprehensive runtime limits and PHI-free contract harness — DRAFT PR #181
- Did: Moved the 45/60 exchange policy into the production historian hook through a shared stateful guard; incomplete natural saves remain rejected, while explicit patient stop, safety escalation, and the hard ceiling preserve a partial history. Added one-shot wrap/finalization, shared model/transcript safety effects, narrow stop-request matching, terminal suppression of later interview/localizer/scale events, and provider-level audio suppression for the silent hard stop. The smoke command now runs three deterministic PHI-free local scenarios by default; `--live` is explicit and accepts only fixed in-code-generated synthetic TTS.
- Files/links touched: `src/lib/historian/*` runtime policies, `src/hooks/useRealtimeSession.ts`, voice-provider output suppression, `src/lib/historianPrompts.ts`, the Nova smoke script, focused tests, and this `HANDOFF.md`.
- Decisions: Safety takes priority over patient stop and turn limits. Ambiguous phrases such as "that's all" terminate only as whole utterances, not inside medication/list/section answers. At exchange 60, queued/future assistant audio is suppressed while the provider remains open only for the text-only partial save; no closing nudge is sent. Provider/IAM/network unavailability is `NOT_RUN`, while content rejection, behavioral timeout, and model/configuration errors are `FAIL`.
- Open questions / needs Claude: Live full-length Nova/ASR semantic behavior, deployed transcript/final-save persistence, worker execution, and physician-browser delivery remain NOT_RUN. Privacy/BAA approval and clinical validation remain blocked; no real patient data should be used.
- Next: Review the published draft update. In an approved non-production environment, execute the remaining fixed synthetic live-provider, persistence, worker, and browser gates.
- Verification: local three-scenario contract PASS; historian Vitest 637 passed / 14 skipped; TypeScript and focused ESLint PASS; remote CI passed TypeScript, the full non-simulated-patient Vitest suite, and the production build on code commit `332a60e`; independent safety re-review found no remaining High blocker. No new live Nova call, database/API write, deployment, or patient-data run occurred.

### 2026-08-20 · ChatGPT · Comprehensive Historian E2E source published — DRAFT PR #181
- Did: completed and published the Nova-only Comprehensive patient-history source flow: clinician-created one-time links, server-side date-of-birth verification before context disclosure, referral-first/age-second opening, 17-domain coverage audit without the Standard 25-exchange cap, durable one-based transcript persistence, deterministic tenant-bound emergency alerts, governed revoke/reissue, transactional final save, durable post-save DDx worker, clinician failure/retry state, and physician-only history/DDx reporting. Removed clinician notification chrome from the public invitation surface. Scores remain explicitly labeled uncalibrated model estimates, not confidence intervals or validated probabilities.
- Files/links touched: draft PR https://github.com/blondarb/OPSAmplehtml/pull/181 at source commit `54bf323`; migration 059; historian app/API/components/hooks; Nova relay opening nudge; durable worker SAM template; focused tests; this `HANDOFF.md`.
- Decisions: use the server-bound invitation and HttpOnly grant as the authority for Comprehensive patient sessions. A refresh after an in-progress interview fails closed to clinician revoke/reissue rather than attempting unsafe transcript reconstruction or storing PHI in browser persistence. Keep all diagnosis/scoring off the patient surface. Do not deploy migration/worker/app/relay or use real patients from this draft.
- Open questions / needs Claude: review PR #181. Privacy/BAA approval remains unresolved. Full synthetic >25-turn, post-turn-25 emergency, 45/60 wrap, deployed transcript/final-save persistence, worker execution, and physician report browser acceptance remain required release gates.
- Next: review/merge only after the source review; then deploy to an approved non-production environment and execute the remaining PHI-free E2E acceptance matrix. Real-patient rollout and clinical reliance remain blocked.
- Verification: TypeScript PASS; focused historian/eval Vitest 667 passed / 14 skipped; Nova relay 26/26 plus relay build PASS; live PHI-free Nova referral-first/age-second/no-premature-tool smoke PASS; SAM lint validation PASS; Next production build PASS with only the repository's pre-existing ESLint option warning; mobile Playwright screenshots PASS using synthetic mocked invitation context; independent safety review found no remaining High/Critical code defect in identity/PHI, tenant alerting, transcript integrity, recovery, or physician-only DDx boundaries.

### 2026-08-20 · ChatGPT · Live Nova first-two-question acceptance — PASS
- Did: refreshed the approved AWS SSO session through the remote device-code flow and exercised Nova 2 Sonic through the production relay class, Comprehensive prompt, and Nova tool adapter. Replaced the misleading text-injection patient reply with a PHI-free synthesized 16 kHz PCM reply sent through the real audio/VAD path. Relaxed an overly literal exact-wording instruction that caused Nova to repeat a semantically completed referral question; the prompt now permits natural paraphrasing, accepts any relevant answer, and moves immediately to age.
- Files/links touched: `scripts/nova-historian-comprehensive-smoke.ts`, `tests/historian/novaComprehensiveSmokeContract.test.ts`, `src/lib/historianPrompts.ts`, `src/lib/__tests__/historianPrompts.test.ts`, and this `HANDOFF.md`.
- Decisions: the live result validates conversation ordering only, not transcription accuracy or clinical history quality. Nova's ASR materially misheard portions of the synthetic sentence, so no transcription-quality claim is warranted. The harness remains PHI-free, performs no application API/DB persistence, and redacts AWS principal/account details.
- Open questions / needs Claude: review the local uncommitted diff. Remaining rollout gates include >25-exchange history coverage, post-turn-25 emergency behavior, 45/60-exchange wrap, persistence and physician-only post-save differential, browser acceptance, privacy/BAA approval, and clinical validation.
- Next: extend the reusable Nova smoke into a deterministic multi-turn PHI-free scenario and run the remaining behavioral gates; do not push or deploy without Steve's separate approval.
- Verification: live Nova PASS for startup/content-filter, referral reason first, patient-reported age second, and no premature tool call. TypeScript passed; focused historian suite 621 passed / 14 skipped; Nova relay 20/20; smoke contract 6/6; `git diff --check` clean. Focused ESLint surfaced only pre-existing hook `any` errors and dependency warnings outside the changed lines.

### 2026-08-20 · ChatGPT · Nova-only Comprehensive Historian gate — LOCAL WIP, LIVE BLOCKED
- Did: made Comprehensive v1 resolve to Nova server-side after clinician/admin authorization, regardless of stale client or default provider settings; Standard provider behavior is unchanged. Hid the engine selector in Comprehensive mode. Added a reusable PHI-free Nova 2 Sonic smoke harness that uses the real relay session class plus production prompt/tool builders, injects synthetic text only, performs no DB/API persistence, validates referral-first/age-second/no-premature-tool behavior, and redacts AWS identity/account details from failures.
- Files/links touched: prior Comprehensive Historian local diff plus `package.json`, `scripts/nova-historian-comprehensive-smoke.ts`, `tests/historian/novaComprehensiveSmokeContract.test.ts`, provider-resolution tests, and this `HANDOFF.md`.
- Decisions: Comprehensive v1 is Nova-only. Do not silently fall back to OpenAI. A provider-authorization failure is a hard NOT_RUN, not evidence about Nova behavior.
- Open questions / needs Claude: live Nova content-filter and first-two-question behavior remain untested. The authenticated `sevaro-scribe-triage` profile was denied Nova Sonic streaming permission; `sevaro-sandbox` was not authenticated in this shell. Refreshing SSO or changing IAM requires Steve's approval.
- Next: with Steve's approval, refresh the normal authorized Nova profile and run `AWS_PROFILE=<approved-profile> npm run historian:nova-smoke`; then extend to >25-turn/emergency synthetic acceptance if the two-turn smoke passes. No push, deployment, DB write, or live model session occurred.
- Verification: TypeScript passed; focused Comprehensive/provider tests passed; Nova relay unit tests 20/20; independent safety review found no blocking/high code findings. Live Nova release gate = NOT_RUN due IAM denial.

### 2026-08-20 · ChatGPT · Comprehensive Historian mode — LOCAL WIP, NOT SHIPPED
- Did: added an opt-in `comprehensive` interview contract on the authenticated clinician mirror. The server requires clinician/admin clinical access, applies and returns a versioned prompt contract, asks why the patient was referred first and patient-reported age second, expands complaint-directed neurologic history coverage, removes the Standard 25-exchange ceiling while instructing wrap-up by 45 and completion by 60, and persists mode/prompt provenance plus covered/missing history domains in structured output. Standard mode remains capped at 25; referral clarification remains scope-locked. Live diagnostic suggestions are suppressed on the Comprehensive interview screen; post-save differential stays physician/QA-only. Existing likelihood percentages are now labeled consistently as uncalibrated model estimates, not statistical confidence intervals or validated probabilities.
- Files/links touched: `src/lib/historianTypes.ts`, `src/lib/historianPrompts.ts`, `src/app/api/ai/historian/session/{route.ts,__tests__/route.test.ts}`, `src/hooks/useRealtimeSession.ts`, `src/components/NeurologicHistorian.tsx`, `src/components/historian/{HistorianInterviewStep,DifferentialCard,DdxComparisonCard}.tsx`, focused historian tests, and this `HANDOFF.md`.
- Decisions: do not fabricate per-patient confidence intervals; numeric intervals require a clinician-labeled calibration study. Keep Comprehensive off anonymous `/patient/*` entry points and require server-side clinical authorization. Preserve the exact emergency/no-diagnosis boundary and keep diagnoses off the patient interview surface.
- Open questions / needs Claude: review the local diff; then, only with Steve's deployment approval, run synthetic live-provider/content-filter and browser acceptance. Adult/guardian eligibility, real-patient voice-provider privacy/BAA approval, and durable execution of the fire-and-forget differential evaluator remain unresolved rollout gates.
- Next: synthetic live acceptance on both providers and a clinician review of history completeness/differential presentation; no push or deployment occurred. Local evidence: TypeScript clean; historian-focused Vitest 614 passed / 14 skipped; focused ESLint has no errors and one pre-existing `activeConfig` dependency warning; independent safety review found no remaining blocking/high findings for local handoff.

### 2026-08-03 · Claude Code · Part 4 (/patient/historian) redesign — SHIPPED (and Parts 2–3 pushed after Steve's "ship it")
- Context: Steve signed off on the /triage redesign ("ship it") → pushed 0c7ef1f; Part 4 followed the same day and shipped on his second "ship it".
- Did: historian rebuilt on the shared nn system as a 4-step patient flow (Your visit → Consent & identity → Interview → Summary) with a persistent step indicator, 17px patient type scale, ≥48px targets, one primary action per screen. Consent+identity gate renders as an in-page step via a new `presentation="page"` variant on HistorianConsentDisclosure (the dark modal branch is byte-identical and still serves /consult EmbeddedHistorian); startSession()/mic remains reachable only through onConfirm — re-verified in-browser (Begin disabled until checkbox+name+DOB; mic never requested; Cancel returns to step 1). Interview screen extracted to a pure presentational component (src/components/historian/HistorianInterviewStep.tsx) so it can be rendered without a live voice session: question always as text ("Question N of up to 23", derived from assistant turns), live "What we heard" box (current or last patient utterance), mic state row (listening / assistant speaking / wrapping up + elapsed), correction hint ("say 'please repeat that'…"), Show Transcript + End Interview. Report view gains theme="clinical" (light chrome + patient tab; /consult keeps dark) and the patient surface now DEFAULTS to the Patient Report tab (plain-language summary first, per brief Part 4). Engine selector hidden behind ?internal=1 (verified hidden by default, visible with the param; ?voice=nova deep link unaffected). MD View toggle + LocalizerPanel REMOVED from the patient surface and enableLocalizer set false there (physician-only diagnostic content must not render on the patient page; /consult keeps its own localizer). Safety-escalation crisis screen deliberately untouched.
- Deferred (need logic/hook changes, not presentation): true Pause and a Repeat-question button (useRealtimeSession exposes neither — the voice agent handles spoken "please repeat" conversationally, which the hint surfaces); correct-before-submission on the summary (save fires on complete today); dark inner cards of the physician tab inside the clinical theme (IntakeReviewSection/transcript viewer restyle is a follow-up).
- Verification: tsc clean; eslint clean (one PRE-EXISTING exhaustive-deps warning in NeurologicHistorian, untouched); full vitest 2,743/0. NOTE for future sessions: a mid-verification run showed 19 failures across 6 heavy triage-engine files (scoringExhaustive, sourceExtractionAuthority, route safety) — all environmental CPU starvation from the running `next dev`; each passes in isolation and the full suite is green with the dev server stopped. Diagnose before touching assertions. Browser: steps 1–2 verified live (gate logic stepwise), interview step via temporary static harness (deleted — it wrote to public/ which deploys), 375px no-horizontal-scroll on both pages, zero console errors.
- Next: run qa/runs/RUN-2026-08-03-001.md (now includes the historian delta) against the deployed site; follow-ups above + physician-tab inner restyle; validate pages remain dark by design.

### 2026-08-03 · Claude Code · Neuro Navigator redesign Parts 2–3 (/triage) — COMMITTED LOCALLY, NOT PUSHED (awaiting Steve's sign-off)
- Context: same-day continuation after the Part 1 correctness fixes deployed (verified live). Parts 2–3 of docs/design/redesign-brief-triage-historian.md; visual target docs/design/neuro-navigator-reference-mockup.html (both copied into the repo this session). Part 4 (/patient/historian) NOT started — Steve's instruction: sign-off on /triage first.
- Part 2 (shared design system): src/styles/neuro-navigator.css — all tokens + component classes scoped under `.nn` so the rest of the app is untouched; light clinical palette, one teal accent, lightness-ordered tier scale t1–t7 (+washes), 5-size type scale, hairline elevation, :focus-visible rings, reduced-motion guard. Every ink/wash pair computed ≥4.5:1 (script-verified; ink-3 darkened to #666f78 to clear the page background). src/lib/triage/tierPresentation.ts = presentation-only tier map ("Tier N of 7", names, color vars) + published dimension weights for display; TIER_DISPLAY stays for legacy dark surfaces (validate pages, batch panels, compact chips).
- Part 3 (/triage): two-pane layout (input left, recommendation right, stacked <860px; extraction review takes a full-width row); input panel now mounted UNCONDITIONALLY and visible beside progress/result (source_replacement lifecycle already resets state — verified before relying on it). Result pane is rubric-forward per the brief: red flags above the score, tier card = number+name+timeframe (never color alone), "How this was scored" card with weight superscripts/bars/rationales + "Weighted total X/5 → Tier N of 7 — Name" + confidence, routing kv card, "Start before the visit", stability caveat, actions at bottom. Emergent = full takeover restyled (t1 wash, "Emergent — do not schedule"). Insufficient-data renders as Tier 7 of 7. New TriageProgress: staged display for the 50–60 s call (elapsed clock, named stages, long-packet real progress via detail; SSE still off per PR #111 gotcha). AlgorithmModal light + Esc-close + aria, tier rows use tier tokens, added the Tier 7 row (presentation of existing insufficient_data behavior — thresholds untouched). Executor subagent converted the 9-file tail (loaders, preview modal, upload zone, extraction review, ingress safety alert, safety review, emergency action, physician override) — copy byte-identical, verified.
- Tests: 3 source/parity contract tests updated to the new structure WITHOUT weakening (old anchors referenced removed markup): canonicalFullResultParity + canonicalProductionPathParity now pin 'How this was scored'/'Weighted total'; pageSourceContract pins the stronger unconditional-mount + review-only-hidden rule. NOTE: executor initially misdiagnosed the parity failures as pre-existing via a stash-baseline that reverted only ITS files — the failures were from the driver's TriageOutputPanel heading removal. Fixed lint (unused needsExtraction after button-color unification).
- Verification: tsc clean; eslint clean on all touched files; FULL vitest 2,743 passed / 0 failed (only exclusion: tests/simulated-patients, needs a live app); browser: two-pane desktop, 375px stack with zero horizontal scroll (E1), keyboard focus ring = 2px accent (checked computed style), zero console errors; result/emergent/algorithm/progress states screenshotted via a temporary static-render harness (deleted after capture — it wrote to public/, must never be committed or it would deploy).
- QA: qa/runs/RUN-2026-08-03-001.md drafted (delta mission brief per qa-rules; execute S1–S7 + T-series + E1 after this deploys).
- Next: (1) Steve sign-off on /triage → push (Amplify deploys) → run the QA brief; (2) Part 4 /patient/historian to the mockup (consent/identity gate from Part 1 restyles into the step flow; engine toggle behind internal route per brief); (3) consider porting the validate pages to nn later — out of scope now.

### 2026-08-03 · Claude Code · Partner-demo correctness fixes on /triage + /patient/historian (redesign brief Part 1, §1.1–1.4)
- Context: both pages are shown to Mayo Clinic Aug 7 and Novant Health Aug 12. Brief's Part 1 correctness fixes only — no visual redesign (Parts 2–4 not started), no triage logic/rubric/weights/threshold/override changes.
- Did: (1.1) Removed the "calculates a triage tier deterministically" claim from /triage intro and AlgorithmModal; replaced with accurate copy (fixed five-dimension rubric, published weights, fixed tier boundaries + red-flag overrides; borderline notes may score differently across runs). Added a visible "Stability" caveat card to the result pane under Dimension Scores. Also corrected "deterministic/near-deterministic temp=0" claims on /triage/validate results + admin pages (temp=0 = lowest variance, not repeatable on borderline cases — matches the engineering record). (1.2) Upgraded HistorianConsentDisclosure into a full consent + identity gate: explicit unchecked consent checkbox, emergency redirect box (stroke symptoms/seizure/thunderclap/new weakness → 911/ED), draft-reviewed-by-clinician + not-a-diagnosis statement, and full name + DOB fields required via new `requireIdentity` prop (passed on /patient/historian; /consult EmbeddedHistorian keeps checkbox-only gate). startSession()/mic remains unreachable until confirm. Typed identity is attestation-only: held in component state, never sent to the voice model, transcript, or DB (no BAA on either voice engine — deliberately no new data path). (1.3) Deleted TriageHelpGuide ("Testing Guide & What's Next" + both GitHub links) from /triage; retitled "AI Triage Tool"→"Neuro Navigator" and removed the "Demo" badge on both pages' FeatureSubHeader; homepage journey card "AI-Powered Triage"→"Neuro Navigator". (1.4) Paste field now capped client-side at FILE_CONSTRAINTS.MAX_TEXT_LENGTH (50,000) with live counter (was displayed against 5,000,000) and a no-truncation block+message when over; oversized-upload rejection already existed client-side (10MB in applyReferralFileSelection) — unchanged.
- Files: src/app/triage/page.tsx, src/components/triage/{AlgorithmModal,TriageOutputPanel,TriageInputPanel}.tsx, src/app/triage/validate/{results,admin}/page.tsx, src/components/{HistorianConsentDisclosure,NeurologicHistorian}.tsx, src/components/homepage/journeyData.ts, tests/triage/triageInputPolicy.test.ts (contract extended to pin the new cap); deleted src/components/triage/TriageHelpGuide.tsx + tests/triage/triageHelpGuide.test.tsx.
- Verification: tsc clean; full vitest 2,743 passed / 0 failed (tests/simulated-patients/runner needs a live localhost app — pre-existing environmental skip/fail); browser-verified both pages on local dev: over-limit paste (54,400 chars) blocks submit with message and zero truncation, consent gate blocks Begin until checkbox+name+DOB, mic never requested pre-consent, zero console errors. Pre-existing lint error (unused Trash2 import in validate/admin) left untouched.
- Decisions: paste cap blocks submit rather than truncating (preserves the never-silently-drop-a-critical-tail property; oversized text routes to the upload path, which still runs the long-packet emergency screen). Validate-page temp=0 copy now says "lowest variance, not fully repeatable on borderline cases". Voice-engine toggle + "Try a Demo"/"Load Sample" labels + validation-study link left in place — Parts 2–4 scope.
- Next: brief Parts 2–4 (shared design system, /triage layout to the reference mockup, /patient/historian large-type patient flow, engine selector behind internal route). Reference mockup + brief live in ~/Downloads (neuro-navigator-reference-mockup.html, redesign-brief-triage-historian.md) — copy into docs/ before Part 2 work.

### 2026-07-21 · Claude Code · Synapse 3.0 "One Surface" concept published
- Did: published `/concepts/synapse-3/unified-surface.html` (self-contained, fictional data) + new first homepage Concepts card (Orbit icon); tweaked old "Synapse 3.0?" card dek to mark provenance. 7 role lenses on one time-spine incl. mid-session additions neuro hospitalist + EEG reader (real anchor: EEG Report Builder); Dr. Vega demos the legal multi-role day; "Daylight" light design language (full departure from 2.0). Locally verified over http (all lenses, toggles, canvas swaps, zero console errors).
- Next: Amplify auto-deploys on push; session log mirrors to Asana card "Unified app / Synapse 3.0". Concept only — nothing wired to engines.

### 2026-07-19 · Claude Code · Triage 403 fix + false-hold root cause and fix
- Did: (1) Fixed "Access denied" on /triage — `clinical_access_memberships` (migration 048) was EMPTY in prod; provisioned both steve@sevaro.com Cognito subs as `clinician`, tenant `default`, directly in RDS. No provisioning path exists yet (no seed/admin UI); teammates still 403 until provisioned. (2) Root-caused the "Human review required — scoring blocked" false holds: the safety-extraction prompt permits `no_time_critical_signal` alongside non-time-sensitive critical unknowns, but the validator demanded `same_day_clinician_review` for ANY unknown and discarded the branch on mismatch → fusion `undetermined` → unrecoverable hold (adjudicator is downgrade-forbidden by design). Reproduced live with the exact prod referral texts (Gutierrez: 3 benign unknowns, 0 signals, deterministic kill; Sandoval: intermittent scorer JSON flake). Fix: validator now floors `care_pathway` from the model's own signals (emergency signal ⇒ emergency_now; immediate-review signal ⇒ ≥ same_day) instead of throwing, never downgrades a stated pathway, logs when flooring fires; prompt gained the matching consistency rule; scorer gets one retry on non-timeout failures.
- Files: `src/lib/triage/modelSafetyExtraction.ts`, `src/lib/triage/modelSafetyExtractor.ts`, `src/lib/triage/processTriageInBackground.ts`, `tests/triage/modelSafetyExtraction.test.ts`, `CLAUDE.md`.
- Verification: triage suite 1,760 passed; tsc + lint clean; live Bedrock repro of both failures now passes; offline sentinel releaseGateEligible; live sentinel run + Bedrock cross-family review (gpt-oss-120b + DeepSeek-R1) same session — both models' Critical/High claims verified as false positives against the code (terminal failedModelBranch catch intact; flooring only ever raises), their observability suggestion adopted (floor warn log).
- Decisions: benign, non-time-sensitive unknowns no longer force a same-day hold (restores the prompt's signed-off intent; no tier definitions changed; understated pathways now floor upward instead of dying into a generic hold — strictly safer for the emergency direction). Adjudicator remains downgrade-forbidden — untouched, flagged as an open dial.
- Also: reason-specific denial messages on all 13 triage routes (`clinicalAccessDeniedMessage()` — 401 "Please sign in" vs 403 "not provisioned" vs 503), because a signed-out Chrome profile looked identical to a provisioning failure; extraction quality spot-checked in prod (Gutierrez summary is note-specific, confidence high) — "triager doesn't read the note" impression was the hold wall, not the extractor.
- Next: provisioning path for `clinical_access_memberships` (seed or admin script) before team testing; consider moving the outpatient scorer to the strict tool-output path like the safety extractor.
- Did: verified the intended product journey against current code and authoritative data/governance sources. The integrated `/consult` flow now embeds triage, AI Historian, patient tools, optional SDNE linkage, and a unified source-labeled report. Confirmed that historian context currently includes triage priority, chief complaint, summary, red flags, and prior intake—not the full raw referral. Reviewed current MIMIC-IV-Note and n2c2 access steps and federal QI/research and HIPAA deidentification guidance. No clinical data were accessed and no product code or deployment changed.
- Files/links touched: `HANDOFF.md`; read `src/lib/consult/contextBuilder.ts`, `src/components/consult/ConsultPipelineView.tsx`, `src/components/consult/HistorianStepPanel.tsx`, `src/components/consult/EmbeddedHistorian.tsx`, `src/lib/consult/report/report-builder.ts`, `src/app/sdne/page.tsx`, and relevant project documentation; reviewed official PhysioNet/MIMIC, Harvard n2c2, HHS OHRP, and HHS OCR guidance.
- Decisions: treat the overall product story as referral triage → informed patient history → optional digital exam → unified clinician preparation/documentation. Do not describe the full-note-to-historian or final desktop-scribe handoff as fully proven yet. Treat 200–500 retrospective referrals as an initial silent validation study, not definitive clinical validation, and obtain a formal institutional QI-versus-research/IRB determination. Do not place MIMIC/n2c2 notes into the current hosted model pipeline until the applicable DUA and compute arrangement are confirmed compatible.
- Open questions / needs Claude: select the institutional IRB/HRPP sponsor and validation partner; decide whether the historian needs full raw text or bounded source-grounded retrieval; verify the consult-report-to-Sevaro-desktop-scribe handoff; confirm live SENSE/SDNE result return and current demo latency.
- Next: create two concise artifacts if Steve approves: (1) MIMIC/n2c2 access and compliant-compute checklist, and (2) a one-page retrospective validation/QI-IRB concept with endpoints, labeling plan, and data-flow diagram. Then scope a polished five-minute administrator/finance demo using synthetic data only.

### 2026-07-13 · ChatGPT · Real clinical-note dataset research
- Did: searched authoritative sources for real, deidentified clinical notes that could strengthen triage testing. Confirmed that MIMIC-IV-Note and n2c2/i2b2 provide controlled-access real notes, but primarily hospital discharge summaries, progress reports, and radiology reports rather than outpatient neurology referrals. Found published use of real outpatient neurology clinic letters, but no downloadable public corpus; identified NeuroDiscovery AI as a possible governed collaboration source. No notes or patient data were downloaded.
- Files/links touched: `HANDOFF.md`; reviewed PhysioNet MIMIC-IV-Note, Harvard n2c2 data sets, the peer-reviewed ExECT neurology clinic-letter study, and NeuroDiscovery AI's data-access materials.
- Decisions: use MIMIC-IV-Note and n2c2 only as real-world documentation robustness sets, not as proof of outpatient referral-triage accuracy. Treat true validation as a governed partner study using deidentified historical referral notes with independent neurologist labels. Do not use random internet notes or weak-provenance scraped collections as validation data.
- Open questions / needs Claude: whether Steve wants a one-page access/application checklist and a partner validation protocol; legal, privacy, DUA, and possible IRB requirements must be confirmed before receiving partner data.
- Next: apply for MIMIC-IV-Note and selected n2c2 corpora for offline robustness testing; separately recruit one or more neurology partners for a silent retrospective validation set that matches the intended referral workflow.

### 2026-07-13 · ChatGPT · Triage demo and validation-partner readiness review
- Did: verified the curated triage safety architecture is contained in current `origin/main`; confirmed the live `/triage` page is publicly visible but both anonymous create and extract calls correctly return 401; reran the full offline sentinel on current code (29/29 evaluable synthetic cases acceptable, all software gates pass) and 404 focused sentinel/emergency/route-safety tests passed. Reviewed the July 12 classifier bake-off and production-readiness audit. No product code, database, deployment, or access policy changed.
- Files/links touched: `HANDOFF.md`; read-only review of triage code, `docs/clinical-safety/`, `qa/triage-sentinel/`, `qa/triage-validation/BAKEOFF_2026-07-12.md`, and the live `app.neuroplans.app/triage` surface. Generated a temporary sentinel report under `/tmp` only.
- Decisions: the tool is ready for controlled synthetic demonstrations and to begin governed validation-partner work; it is not ready for anonymous arbitrary-text access or clinical reliance. Preserve clinician authorization and add a separately scoped demo/partner access boundary rather than opening the clinical endpoints.
- Open questions / needs Claude: migrations 048 and 050–055 are documented as not yet applied; AWS credentials were unavailable to inspect the Amplify job directly; checked-in live sentinel reports cover only 1–2 case subsets despite a CLAUDE.md summary claiming a 26/26 live run; the classifier remains deliberately over-triage-prone and the 26-case expected labels still need independent clinical sign-off.
- Next: if Steve approves, design an expiring signed demo link limited to built-in synthetic referrals, then a separate authenticated partner-validation workflow with versioned cases, independent labels, feedback capture, and no clinical routing or patient binding.

### 2026-07-13 · ChatGPT · AI intake outpatient scenario expansion
- Did: reviewed the four existing synthetic outpatient intake scenarios and the recently improved shared Henry/Nova voice-intake flow. Started design work for adding two nonredundant outpatient scenarios; no application code or deployment changed.
- Files/links touched: `HANDOFF.md`; reviewed `src/lib/historianTypes.ts`, `src/components/NeurologicHistorian.tsx`, and recent Historian/intake commits.
- Decisions: Steve approved new tremor/parkinsonism and new memory/cognitive concern as the two additional scenarios. Keep this as a small scenario-library change on the existing public intake route; preserve synthetic-only use and the current consent flow.
- Open questions / needs Claude: awaiting Steve's approval of the exact card/guidance design; no application code has changed yet.
- Next: approve the concise design, then document it and implement with focused tests.

### 2026-07-13 · ChatGPT · AI voice intake agent public demo link check
- Did: confirmed the recently improved AI voice intake agent (internally named the AI Historian) at `https://app.neuroplans.app/patient/historian` is live and already bypasses Cognito via the existing `/patient` public-route rule; confirmed the page shows synthetic scenarios and supports `?scenario=headache_new` for a preselected demo. No product code or deployment changed.
- Files/links touched: `HANDOFF.md`; reviewed `src/middleware.ts`, `src/app/patient/historian/page.tsx`, `src/components/NeurologicHistorian.tsx`, the Historian session/save routes, and the live site response.
- Decisions: the existing preselected scenario link is sufficient for Steve's immediate no-password demo. Treat it as synthetic-only: completed conversations are currently recorded/transcribed and written to `historian_sessions`.
- Open questions / needs Claude: for broad sharing, decide whether to add a dedicated `/demo/historian` boundary that rejects real-patient context, avoids clinical DB persistence/notifications, labels synthetic-only use, and adds abuse/cost controls.
- Next: Steve tests the preselected headache link on his phone; implement the dedicated demo boundary only if he wants a hardened, broadly shareable version.

### 2026-07-13 · Claude Code · Handoff system initialized
- Did: created HANDOFF.md + AGENTS.md; pointed CLAUDE.md at HANDOFF.md
- Next: both AIs read HANDOFF.md first, update it last
